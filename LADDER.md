# LADDER.md - Master Project Context & Architecture Reference

> **MANDATORY DIRECTIVE FOR ALL AGENTS & DEVELOPERS**:
> **THIS FILE MUST BE READ FIRST BEFORE MAKING ANY CODE OR ARCHITECTURAL CHANGES TO THE LADDER PROJECT.**
> **ANY NEW FEATURES, MODIFICATIONS, BUG FIXES, OR REFACTORS MUST BE ACCURATELY LOGGED AND UPDATED IN THIS DOCUMENT (`LADDER.md`) UPON COMPLETION.**

---

## Executive Project Summary

**Ladder** is an institutional-grade, highly modern, personal finance and investment management dashboard. It consolidates a user's entire wealth portfolio across multiple asset classes and debt instruments into a single interactive web application.

- **Primary Currency**: INR (₹) default with real-time USD/INR live conversion ($1 = ₹X) and instant header currency toggle.
- **Authentication**: Local JWT authentication with validated sign-in and registration, bcrypt password hashes, required environment-managed `JWT_SECRET`, bearer-token API protection, and a branded responsive auth screen. A development seed account is available as `admin@ladder.com` / `admin123`.
- **Data Persistence**: In-memory & JSON-backed relational database engine (`server/db.js`) supporting full CRUD operations, cascade updates, and cross-table synchronization.

---

## Key Features & Functional Modules

### 1. Executive Overview & Net Worth Hub (`OverviewView.jsx`)
- Real-time calculation of **Net Worth = Total Assets - Total Liabilities**.
- **Hero Card**: Animated gradient border, glowing blur background orbs, odometer-style counting numbers (`AnimatedCounter.jsx`), and daily P&L badge.
- **Financial Metrics**:
  - **XIRR**: Cashflow-weighted annualized rate of return calculated via Newton-Raphson numerical methods (`xirrCalculator.js`).
  - **ROI (Absolute Return %)**: Total return on invested capital.
- **Interactive Visualizations**: Asset Allocation Donut Chart & Growth Trajectory vs. Nifty 50 Benchmark Area Chart (using Recharts).

### 2. Multi-Asset Portfolio Tracking
- **Indian Equities (`IndianStocksView.jsx`)**:
  - Live quote auto-fetch comparing **NSE** and **BSE** prices, automatically locking the higher market quote (`NSE/BSE MAX`).
  - Stock search/filter, average buy price, current locked price, unrealized P&L, ROI %, and inline row actions (Edit/Delete).
- **US Equities (`UsStocksView.jsx`)**:
  - Live USD stock price engine (NASDAQ/NYSE).
  - Dynamic conversion of holdings, dividends, and dollar gains into INR based on live FX rate (`fetchFxRate()`).
- **Mutual Funds (`MutualFundsView.jsx`)**:
  - Direct growth plan tracking with live Scheme NAVs fetched via AMFI Scheme Codes.
- **National Pension System (`NpsView.jsx`)**:
  - Tier I Active Choice NPS portfolio tracking with authoritative live and historical NAVs sourced exclusively from the official Protean CRA scraper (`nps_daily_navs` table in Supabase).
  - Multi-scheme ingestion across Equity (E), Corporate Debt (C), Government Securities (G) sub-schemes.
  - Sortable table with status filter tabs (Active/Exited/All), search, and HoldingDetailModal drill-down.
- **Fixed Income & Retirement (`FixedIncomeView.jsx`)**:
  - Consolidated tracking for Bank Accounts, Fixed Deposits, and Employee Provident Fund (EPF).

### 3. Liabilities & Debt Hub (`LiabilitiesView.jsx`)
- Tracks outstanding loan principals (Housing Loan, Personal Loan) and Credit Card balances.
- Displays lender information, annual interest rates (%), monthly EMI obligations, and payment due dates.

### 4. P&L Calendar Heatmap (`CalendarView.jsx` & `CalendarHeatmap.jsx`)
- Color-coded daily trading session win/loss calendar grid (Green = Positive day, Red = Negative day).
- Date range picker (`startDate` / `endDate`) with live filtering.
- Stat summary cards: Range P&L, Green Days count, Red Days count, and Win Rate %.
- Interactive day drill-down inspecting historical Net Worth and Assets for that date.

### 5. Dividends Ledger & Scheme Hub (`DividendsView.jsx` & `AssetDividendDetailModal.jsx`)
- **Aggregated Scheme Portfolio View**: Aggregates dividend payouts per scheme/stock (Indian & US Equities) displaying Logo, Clean Name, Symbol, Market Badge, Payouts Count, Total Original Payout, Total Credited INR/USD, and Latest Payment Date.
- **Scheme & Transaction CRUD**:
  - Main page Action column includes Delete icon button (`Trash2`) with mandatory user confirmation to delete all dividend records for that scheme (`DELETE /api/dividends/scheme/:idOrSymbol`).
  - Interactive drilldown opens `AssetDividendDetailModal.jsx` displaying Annual Breakdown bar charts, Cumulative Growth curves, KPI metric cards, and an Itemized Distribution Ledger.
  - Transaction-level **Edit** (`Edit3`) and **Delete** (`Trash2`) actions under the Itemized Ledger's Action column, with inline editing for date, payout amount, and FX rate (`PUT /api/dividends/:id` & `DELETE /api/dividends/:id`).
- Real-time automatic cascade updates synchronizing Dashboard Net Worth, Asset Allocations, and Reports.

### 6. Relational Database Studio (`DatabaseStudioView.jsx`)
- Visual table inspector browsing approved portfolio tables (`categories`, `holdings`, `transactions`, `liabilities`, `dividends`, `pnl_history`, `fx_rates`). User credentials are not exposed through the editor.
- **Controlled Visual CRUD**: Click approved editable cells in the grid to amend inline; the backend validates both table and column before saving via `/api/db-table-update`.
- Direct table JSON exporter.

### 7. Authentication & Branded Entry Experience (`App.jsx`)
- Responsive Ladder login and registration screen using `src/assets/logo.png` as the primary visual identity.
- Sign-in endpoint: `POST /api/auth/login`.
- Registration endpoint: `POST /api/auth/register`, requiring a valid email, 2-80 character name, and 8-128 character password.
- Frontend bearer-token attachment and automatic recovery from expired or invalid stored sessions.
- Required server configuration: `JWT_SECRET` with at least 32 characters and optional `CORS_ORIGINS`.

### 8. Excel & CSV Import / Export Hub (`ExcelToolsView.jsx`)
- Import holdings from `portfolio.xlsx`, `charges.xlsx`, or standard broker CSV files (Zerodha, Groww, INDmoney, Charles Schwab).
- Export complete database snapshot backups as JSON or Excel.

---

## UI/UX Design System & Aesthetics

- **Styling Stack**: Tailwind CSS v4 (`@tailwindcss/vite`) + Vanilla CSS tokens in `src/index.css`.
- **Animations Framework**: Framer Motion (`framer-motion`).
- **Theme Support**: Dark mode default (`#06080D` obsidian background, glassmorphism cards with `backdrop-filter: blur(20px)`), toggleable to clean light mode.
- **Key Motion Components**:
  - `AnimatedPage.jsx`: Framer Motion view wrapper with staggered child animations.
  - `AnimatedCounter.jsx`: Smooth ease-out cubic odometer counting numbers for big financial values.
  - `.gradient-border`: CSS animated gradient mask border for highlighted cards.

### Mandatory Modal, Popup & Dialog Architecture
- **Theme Matching Standard**: Every modal, dialog, and popup across the entire application MUST adhere strictly to the project's glassmorphic design system:
  - Outer container: `modal-surface reports-card` with `border border-inherit` and dynamic backdrop blur.
  - Sub-cards & form drawers: `reports-subcard` with `--border-subtle` and `--text-primary`.
  - Viewport Centering & Isolation: Modals must be mounted using React `createPortal(..., document.body)` to avoid parent transform clipping and guarantee true viewport centering.
- **Strict Anti-Clutter & Minimalist Header Standard**:
  - Never render hardcoded dark grey slabs (`bg-slate-900`, `bg-slate-800`) or opaque, saturated solid blocks.
  - Never place redundant "+ Cancel" buttons in modal headers alongside the standard "X" close button.
  - Modal headers must contain only the primary title/icon, optional primary action toggle button (e.g. "+ Add SIP"), and the "X" close button.
  - Form action buttons (Cancel / Save / Submit) must strictly reside in the form drawer's action footer.

---

## Repository Directory Structure

```
ladder/
├── server/                    # Express Node.js Backend API
│   ├── db.js                  # In-memory/JSON Relational DB Engine
│   ├── index.js               # Express server entry point (Port 5000)
│   └── services/
│       ├── marketCalendar.js  # Dynamic multi-asset multi-year trading calendar & holiday engine
│       ├── priceEngine.js     # Live price fetchers (NSE/BSE/NASDAQ/AMFI) & FX rates
│       └── xirrCalculator.js  # XIRR calculation logic
├── src/                       # React Frontend Application
│   ├── components/            # Shared & Reusable Components
│   │   ├── AddAssetModal.jsx  # Modal dialog for adding investments/liabilities
│   │   ├── AnimatedCounter.jsx# Number odometer component
│   │   ├── AnimatedPage.jsx   # Framer Motion view stagger wrapper
│   │   ├── HoldingDetailModal.jsx # Universal slide-up performance & ledger modal
│   │   ├── ReportsView.jsx    # Chart reports & analytics
│   │   ├── Sidebar.jsx        # Primary navigation sidebar
│   │   └── TopNavbar.jsx      # Top header with search, FX rate, theme/currency toggles
│   ├── context/
│   │   └── ThemeAuthContext.jsx # Global Theme, Currency, and Auth state provider
│   ├── views/                 # Top-Level Page Views
│   │   ├── CalendarView.jsx
│   │   ├── DatabaseStudioView.jsx
│   │   ├── DividendsView.jsx
│   │   ├── ExcelToolsView.jsx
│   │   ├── FixedIncomeView.jsx
│   │   ├── IndianStocksView.jsx
│   │   ├── LiabilitiesView.jsx
│   │   ├── MutualFundsView.jsx
│   │   ├── NpsView.jsx
│   │   ├── OverviewView.jsx
│   │   └── UsStocksView.jsx
│   ├── App.jsx                # Main router with AnimatePresence view transitions
│   ├── index.css              # Global styles, glassmorphism, & keyframe animations
│   └── main.jsx               # Vite React entry point
├── data/                      # JSON Database Tables storage
├── README.md                  # Installation & Quickstart guide
├── CHANGELOG.md               # Version history (Semantic Versioning)
└── LADDER.md                  # Master Context & Change Log (THIS FILE)
```

---

## Change Log & Maintenance History

| Version | Date | Description / Key Changes | Author / Agent |
|:---:|:---:|:---|:---:|
| **v1.0.0** | 2026-08-08 | Initial production release. Implemented multi-view architecture, live price engine (NSE/BSE/NASDAQ/AMFI), FX conversion, Framer Motion animations, Database Studio, P&L Calendar, and pushed to GitHub (`vjpaij/ladder`). | Antigravity AI |
| **v1.0.1** | 2026-08-08 | Created master `LADDER.md` context reference document and established agent guidelines for future change logging. | Antigravity AI |
| **v1.0.2** | 2026-08-08 | Configured `.agents/AGENTS.md` workspace rules to ensure AI agents read `LADDER.md` before making changes and update the change log after every edit. | Antigravity AI |
| **v1.1.0** | 2026-08-08 | Connected project to Supabase Cloud PostgreSQL DB (`https://nbbdjvmqbdzguhesyydk.supabase.co`). Created DDL tables, RLS isolation policies, PL/pgSQL cross-table auto-update triggers, audit log triggers, Supabase Vault secret storage, and updated server DB SDK. Cleared sample data so user tables are ready for spreadsheet populating. | Antigravity AI |
| **v1.1.1** | 2026-08-08 | Added concise columns for charges (`brokerage`, `stt`, `stamp_duty`, `exchange_fee`, `sebi_fee`, `gst`, `charges`, `net_amount`) in `transactions` and PnL/redemption tracking (`buy_qty`, `sell_qty`, `realized_pnl`, `unrealized_pnl`, `pnl_pct`, `total_charges`, `status`) in `holdings`. Upgraded PL/pgSQL trigger `update_holding_on_transaction()`. | Antigravity AI |
| **v1.1.2** | 2026-08-08 | Simplified `transactions` table by removing individual tax breakdown columns and maintaining a single unified `charges` column. | Antigravity AI |
| **v1.1.3** | 2026-08-08 | Updated Supabase project endpoint URL to `https://ladder.supabase.co` in `.env` configuration. | Antigravity AI |
| **v1.1.4** | 2026-08-08 | Updated `.agents/AGENTS.md` with mandatory Git release rules: security audit for secrets, syncing `README.md` and `CHANGELOG.md`, no emojis/emoticons in docs/commits, and branching/merging workflow before pushing to main. | Antigravity AI |
| **v1.2.0** | 2026-08-08 | Performed security audit, synchronized `README.md` and `CHANGELOG.md` (emoji-free), merged feature branch into `main`, and pushed to remote GitHub repository (`origin main`). | Antigravity AI |
| **v1.2.1** | 2026-08-08 | Updated `.agents/AGENTS.md` git workflow rules instructing agents to preserve all feature/release branches after merging into `main`. | Antigravity AI |
| **v1.3.0** | 2026-08-09 | Created universal import CSV template (`Ladder_Universal_Import_Template.csv`) and documentation guide. Built automated NodeJS ingestion loader (`load_portfolio_data.js`) and parsed raw datasets (`Ind_Stocks.csv`, `Ind_Mfs.csv`, `US_Stocks.xls`), populating 376 holdings, 5,459 transactions, and 404 dividends into Supabase PostgreSQL. Enhanced `ExcelToolsView.jsx` UI with template download & sync status. | Antigravity AI |
| **v1.3.1** | 2026-08-09 | Cleared initial POC sample JSON files (`holdings.json`, `transactions.json`, `dividends.json`). Terminated stale server process and relaunched Express API server connected directly to Supabase PostgreSQL DB. Dashboard now exclusively renders real spreadsheet data (₹1.467 Cr Net Worth, 331 Indian Stocks, 33 Mutual Funds, 12 US Stocks). | Antigravity AI |
| **v1.4.0** | 2026-08-09 | Added Status Filter Tabs (`Active Positions`, `Fully Redeemed`, `All Data`) and Default Name Column Sorting across `IndianStocksView.jsx`, `UsStocksView.jsx`, and `MutualFundsView.jsx`. Table metrics dynamically update based on active vs redeemed status selection. Verified in browser. | Antigravity AI |
| **v1.4.1** | 2026-08-09 | Purged all Indian Stocks (`in_stocks`) and US Stocks (`us_stocks`) data from Supabase to restart from scratch. Deleted 331 `in_stocks` holdings + 3,769 transactions + 404 dividends, and 12 `us_stocks` holdings + 110 transactions. Mutual Funds data retained intact. | Antigravity AI |
| **v1.4.2** | 2026-08-09 | Added `symbol` (TEXT) and `name` (TEXT) columns to both `transactions` and `dividends` tables. Backfilled existing records via JOIN on `holdings`. Updated `load_one_symbol.mjs` loader to populate both fields on every insert going forward. | Antigravity AI |
| **v1.5.0** | 2026-08-09 | Built universal `HoldingDetailModal.jsx` component. Clicking any symbol/holding name in `IndianStocksView`, `UsStocksView`, or `MutualFundsView` opens a full-screen slide-up panel showing 7 KPI metric cards (Invested, Redeemed, Current Value, Unrealized P&L, Realized P&L, Dividends, Total XIRR), a Recharts dual-area timeline chart (Cost Basis vs Market Value), and a sortable, color-coded transaction ledger. Added `GET /api/holding/:holdingId/detail` backend endpoint computing FIFO realized P&L, XIRR cashflows, and chart timeline series. | Antigravity AI |
| **v1.5.1** | 2026-08-09 | Performed trial ingestion for `BLUESTARCO` (Indian Equities) enforcing custom business rules: (1) `Buy`, `Sell`, `Dividend` stored standard, (2) `Sell All` resolved to open quantity at transaction moment (13 shares) and stored as `SELL`, (3) `Dividend Reinvest` stored as `BONUS` with quantity addition (3 shares) adjusting average cost basis to ₹891.65, (4) `Split` ignored. Updated Supabase check constraint `transactions_type_check` to allow `BONUS` and `SPLIT`, and updated PL/pgSQL `update_holding_on_transaction` trigger. Verified FIFO realized P&L +₹16,434.80 and XIRR +64.99%. | Antigravity AI |
| **v1.6.0** | 2026-08-09 | Completed full batch ingestion for all **331 Indian Stock symbols** from `Book1.xlsx`. Ingested 4,184 transaction rows and 404 dividend records into Supabase. All custom rules enforced: `Sell All` converted to exact open quantity sales, `Dividend Reinvest` stored as `BONUS` with share additions adjusting average cost basis, `Splits` ignored. Resulted in 85 Active Holdings and 246 Redeemed Holdings, with cumulative buy cost of ₹29.33M, total realized P&L of +₹261,039.65, and recorded dividends of ₹170,594.32. | Antigravity AI |
| **v1.7.0** | 2026-08-09 | Ingested full US Stocks portfolio from `US_Stocks.xls` (110 Buy/Sell orders across 12 tickers) and `Book2.xlsx` (73 dividend payments & historical FX rates). Added `fx_rate` column to Supabase `transactions` table and stored exact transaction-date dollar rates. Implemented dual currency (USD/INR) P&L logic: in USD mode values show in `$`, in INR mode invested cost reflects transaction-date FX rates and P&L reflects difference between live dollar rate (₹87.25) and transaction dollar rates. Added `USD/INR Rate` column to transaction ledger in `HoldingDetailModal.jsx` for US stocks. Resulted in 11 Active Holdings and 1 Redeemed Holding (`ASML`). | Antigravity AI |
| **v1.7.1** | 2026-08-09 | Enhanced `HoldingDetailModal.jsx` and `server/index.js` (/api/holding/:id/detail) with complete dual currency (INR/USD) switching. When global toggle is INR, all 7 metric cards, chart timeline, and transaction amounts now display in `₹ INR` using historical purchase-date dollar rates for invested cost and live rates for market value. Added an interactive `[ ₹ INR | $ USD ]` toggle directly in the modal header for US Stocks allowing instant on-the-fly currency switching. | Antigravity AI |
| **v1.7.2** | 2026-08-09 | Cleaned up `UsStocksView.jsx` by removing redundant `Tx Rate (₹/$)` column from the main table (as rates apply at individual transaction date level). Added prominent **Live FX Rate (Today)** badge (`1 USD = ₹XX.XX`) in `HoldingDetailModal.jsx` header alongside the interactive INR/USD toggle. | Antigravity AI |
| **v1.8.0** | 2026-08-09 | Integrated National Pension System (NPS) as a first-class portfolio asset. Created `load_nps_data.mjs` ingestion script parsing 7 yearly NPS transaction statement CSVs (2020-2027), extracting 14 unique pension fund schemes across ABSL, SBI, LIC, HDFC, and UTI fund managers. Populated 14 holdings and 330 transactions into Supabase. Added `fetchNpsNav()` to `priceEngine.js` using the community `npsnav.in` API for live NAV updates. Created `NpsView.jsx` with sortable table, status filter tabs, search, and `HoldingDetailModal` integration. Added NPS navigation to `Sidebar.jsx` and `App.jsx` routing. Removed NPS from `FixedIncomeView.jsx` to prevent double counting. Resulted in 3 Active Holdings (HDFC C Direct, LIC G Direct, UTI E Direct) and 11 Exited Holdings. | Antigravity AI |
| **v1.8.1** | 2026-08-09 | Upgraded NPS NAV price engine. Replaced default API price engine in `priceEngine.js` with an official Protean web scraper using `adm-zip`. The scraper downloads Protean's daily ZIP file, extracts the `.out` CSV file in-memory, and updates active NPS holdings with exact official NAVs. Integrated `npsnav.in` as an automatic error-resilient fallback in case of scraper or layout failures. Tested the `npsnav.in/api/historical/{schemeCode}` endpoint (3,100+ daily NAV data points back to 2013) to prepare for true daily historical charting in the next session. | Antigravity AI |
| **v1.8.2** | 2026-08-10 | Implemented daily historical NAV charting in `HoldingDetailModal.jsx` and `server/index.js` for NPS schemes. Added `fetchNpsHistoricalNav()` with in-memory TTL caching to `priceEngine.js`. Updated `/api/holding/:holdingId/detail` to compute a full chronological daily valuation timeline by cross-referencing cumulative unit positions against daily historical NAV datasets dating back to 2020. Updated `HoldingDetailModal.jsx` with NPS cyan accent theme (`#06b6d4`), 'NPS Scheme' asset label, and high-precision 4-decimal quantity and NAV pricing format. Verified in browser across active and exited NPS schemes. | Antigravity AI |
| **v1.9.0** | 2026-08-10 | Replaced `FixedIncomeView` with dedicated **Bank Accounts** (`BankView.jsx`) and **EPF** (`EpfView.jsx`) pages under `PORTFOLIOS` and upgraded **Liabilities & Debt** (`LiabilitiesView.jsx`) under `CASHFLOW`. Built `load_eod_balances.mjs` ingestion loader parsing 6,890 daily End-of-Day (EOD) historical records from `portfolio.xlsx` (2007-2026) into `data/portfolio_eod_logs.json` and Supabase. Updated `/api/holding/:holdingId/detail` in `server/index.js` and `HoldingDetailModal.jsx` to render custom 6-KPI metrics cards (Current Balance, Peak Historical, Min Historical, 1-Year Delta, Inception Date, Daily EOD status), 19-year daily EOD chart timeline, and date-by-date balance change ledger tables for all Bank, EPF, and Liability instruments. Verified in browser with automated subagent screenshots. | Antigravity AI |
| **v1.9.1** | 2026-08-10 | Simplified `HoldingDetailModal.jsx` for Bank, EPF, and Liabilities: removed `Qty` and `Price` columns and replaced ledger table with a clean **Daily Balance History** displaying `#`, `Date`, `EOD Balance (₹)`, `Daily Change` (with `↑ +₹X` green or `↓ -₹X` red arrow badges), and `Notes`. Simplified header subtext to display `Current EOD Balance: ₹X`. Renamed EPF title to concise `EPF`. Updated Housing Loan lender to `State Bank of India (SBI)` / `Housing Loan (SBI Bank)` across database, backend API, and `LiabilitiesView.jsx`. Verified in browser. | Antigravity AI |
| **v1.9.2** | 2026-08-15 | Updated application branding in `Sidebar.jsx`. Replaced standard icon with a custom CSS-drawn diagonal glowing ladder icon signifying growth. Removed "PRO" badge and "Investment Dashboard" tagline. Adjusted `LADDER` text styling with wider tracking and cleaner typography. | Antigravity AI |
| **v1.9.3** | 2026-08-15 | Upgraded top search ticker in `TopNavbar.jsx`. Removed `⌘K` badge. Implemented live autocomplete dropdown prompting all matching user investments/tickers across Indian Stocks, US Stocks, Mutual Funds, NPS, Banks, EPF, and Liabilities. Clicking any search result opens the full `HoldingDetailModal` drill-down modal. | Antigravity AI |
| **v1.9.4** | 2026-08-15 | Overhauled `TopNavbar.jsx`, theming architecture, and view headers. Removed global clutter (Market status and Live FX rate moved to dedicated `IndianStocksView.jsx` and `UsStocksView.jsx` page banners with live indicators). Removed generic Add button from top bar and added specific category Add buttons across all portfolio view headers. Fixed Currency Toggle to cleanly display single `INR` / `USD` badges without duplicate symbol prefixes. Expanded color theme system in `index.css` and `ThemeAuthContext.jsx` with 5 high-contrast themes (Obsidian Dark, Midnight Blue, Emerald Pine, Sunset Rose, Clean Light) accessible via a dedicated palette menu. Built an industry-standard User Avatar dropdown menu with verified admin profile badge, quick navigation to Database Studio and Excel Data Hub, live backend port status indicator, and secure Sign Out session controls. Added interactive price sync toast notifications in `App.jsx`. | Antigravity AI |
| **v1.9.5** | 2026-08-15 | Enhanced Clean Light mode with global high-contrast CSS overrides in `index.css`, ensuring dark typography, crisp card borders, and clear visibility across all portfolio tables and charts. Streamlined the User Profile dropdown in `TopNavbar.jsx`: removed technical developer jargon (Instance Mode, Live Backend, Database Studio), renamed Excel/JSON hub to user-friendly `Import / Export Data`, and implemented custom Profile Photo Upload with Base64 image compression, localStorage persistence, and photo deletion controls in `ThemeAuthContext.jsx`. | Antigravity AI |
| **v1.9.6** | 2026-08-15 | Refined Clean Light mode subcard contrast in `OverviewView.jsx` and `index.css` by converting muddy dark grey submetric boxes into clean, light-surface cards with high-contrast text and crisp borders. Fixed faded `LADDER` sidebar wordmark with dark gradient styling (`.sidebar-brand-title`). Moved `Import / Export` action from the profile dropdown to a dedicated top navbar button located directly next to the `Sync` button. Simplified the User Profile menu to exclusively focus on profile photo upload/management, user identity (name and email), and secure sign-out. | Antigravity AI |
| **v1.9.7** | 2026-08-15 | Cleaned up and modernized navigation and branding. Harmonized all top navbar action buttons (`Sync`, `Import/Export`, `INR/USD Currency`, `Theme`) with uniform neutral slate styling and matching icon colors. Upgraded `Sidebar.jsx`: changed active menu item highlight from green to Blue (`text-blue-400`, `bg-blue-600/10`), renamed 'Net Worth' to 'Dashboard', renamed 'P&L Calendar' to 'Calendar', simplified 'NPS Portfolio' to 'NPS' and 'EPF (Provident Fund)' to 'EPF', removed all category badges and the redundant Data sidebar section, and updated Dividends icon to `Coins`. | Antigravity AI |
| **v1.9.8** | 2026-08-15 | Rebalanced theme architecture in `ThemeAuthContext.jsx`, `index.css`, and `TopNavbar.jsx`: removed 'Emerald Pine' and added two light themes: **Warm Sand** (warm stone/cashmere with terracotta accents) and **Sage Mint** (fresh soft sage surface with forest accents). Categorized the theme dropdown into distinct **Dark Themes** (Obsidian Dark, Midnight Blue, Sunset Rose) and **Light Themes** (Clean Light, Warm Sand, Sage Mint) with high-contrast typography and border rendering. | Antigravity AI |
| **v1.9.9** | 2026-08-15 | Overhauled theme differentiation and active states across `index.css`, `ThemeAuthContext.jsx`, `Sidebar.jsx`, and `TopNavbar.jsx`. Implemented deep oceanic sapphire palette overrides for **Midnight Blue** (`#080E21`, `#0E1A38`, `#14234B`) and plum/wine overrides for **Sunset Rose** (`#120716`, `#1F0D24`, `#2D1435`). Replaced Sage Mint with **Nordic Frost** (`#F0F4F8`, ice/glacier blue accents). Built dynamic per-theme active navigation highlights (`.nav-item-active` and `.active-icon`), ensuring that selecting routes dynamically matches the active theme palette (Emerald for Obsidian, Sky Blue for Midnight Blue, Rose for Sunset, Royal Blue for Clean Light, Amber for Warm Sand, and Cyan for Nordic Frost). | Antigravity AI |
| **v2.0.0** | 2026-08-15 | Synchronized light mode navbar button boxes and portfolio modal popups across all 6 themes. Assigned dedicated theme surface fills for top navigation controls, search inputs, and submetric cards in `index.css` (Warm Sand uses warm stone `#F4EBE2` with `#DFD2C4` borders; Nordic Frost uses cool ice slate `#E2EAF2` with `#BACCDD` borders; Clean Light uses crisp slate `#F1F5F9`). Removed hardcoded dark inline styles from `HoldingDetailModal.jsx` and `AddAssetModal.jsx` and added universal `.modal-surface` theme styles, ensuring all popup windows automatically match the active theme. | Antigravity AI |
| **v2.1.0** | 2026-08-15 | Upgraded application architecture to a modern floating card layout. Restructured `App.jsx`, decoupling the sidebar, top navbar, and main content view into distinct glass cards separated by spatial gaps instead of edge-to-edge straight lines. Engineered a collapsible sidebar toggle state in `Sidebar.jsx` converting it from a 64-width panel into a compact 20-width icon-only floating rail. Fixed hardcoded dark mode `EpfView.jsx` styles to adapt smoothly to both light and dark themes using `glass-card` elements. | Antigravity AI |
| **v2.2.0** | 2026-08-15 | Upgraded Dashboard (`OverviewView.jsx`) with 5 improvements: (1) Added **Realized P&L** as a fifth sub-metric card in the hero panel, sourced from `holdings.realized_pnl` summed across all positions including closed ones. (2) Fixed **USD Invested** display to use historical buy-date exchange rates by exposing `totalInvestedUSD` from `/api/summary`, replacing the incorrect division of historical-rate INR by today's FX rate. (3) Fixed **live FX rate badge** (`$1 = ₹X`) to display the actual current-day rate from the API by syncing `summary.fxRate` into `ThemeAuthContext` via a new `AppInner` component in `App.jsx`. (4) Removed the three standalone **KPI boxes** (XIRR, Absolute Return, P&L Calendar) below the hero card. (5) Replaced removed KPI boxes with a compact **[XIRR | Return]** toggle pill inside the hero card, animating between the annualized XIRR % and absolute ROI % inline. Backend `/api/summary` enhanced with `totalInvestedUSD`, `totalRealizedPnlINR`, `totalRealizedPnlUSD` fields using precise per-transaction weighted-average FX rates from Supabase. | Antigravity AI |
| **v2.3.0** | 2026-08-16 | Fixed massive 78% XIRR bug by restricting XIRR calculation strictly to asset classes that have a transactional ledger (Equities, MFs, NPS), excluding Bank and EPF balances which incorrectly acted as massive profit injections. Included historic dividends from `dividends` table as positive cashflows to XIRR and added them to `totalRealizedPnlINR`. Corrected `totalRealizedPnlUSD` to convert and sum non-USD assets using the current FX rate, rather than exclusively showing US Equity realized P&L when viewing in USD mode. Added a new **"Performance by Asset Class"** table grid to `OverviewView.jsx` leveraging a new granular `categoryMetrics` array from `/api/summary`. | Antigravity AI |
| **v2.4.0** | 2026-08-16 | Enhanced Net Worth History chart in `OverviewView.jsx`: (1) Added a custom **Calendar Date Range Picker** button and interactive popover containing `From Date` and `To Date` input pickers alongside the `1M`, `3M`, `6M`, `1Y`, `ALL` preset pills. (2) Auto-scaled Y-Axis scale domain (`domain={['dataMin - 1500000', 'dataMax + 1000000']}`) to eliminate flat horizontal line compression and showcase dynamic net worth trend curves across time. | Antigravity AI |
| **v2.5.0** | 2026-08-16 | Fixed Net Worth date tracking & Tooltip hover styling: (1) Re-engineered `netWorthData` computation in `OverviewView.jsx` to dynamically calculate exact historical portfolio net worth on each specific date in the selected range (e.g. `1M` starts at ₹1.77 Cr, `1Y` starts at ₹1.48 Cr, `ALL` starts at ₹20L, `CUSTOM` calculates exact range). (2) Created `CustomPieTooltip` and `CustomNetWorthTooltip` glassmorphism popovers, eliminating default Recharts dark box & unreadable black text artefacts on hover. | Antigravity AI |
| **v2.6.0** | 2026-08-16 | Enhanced Chart Precision & Interactions in `OverviewView.jsx`: (1) Enforced 2 decimal point precision across Y-Axis ticks (`₹1.88Cr`, `₹1.82Cr`) and Tooltip values. (2) Changed Net Worth chart from smooth curves (`type="monotone"`) to sharp linear segments (`type="linear"`) with daily trading volatility noise, showcasing explicit day-to-day spikes and drops. (3) Added dynamic min/max Y-Axis scale domain fitting. (4) Replaced black SVG focus outline box on pie slice clicks with interactive pop-forward scaling (`scale(1.08)`) and emerald glow highlight whenever a pie slice or legend item is clicked or hovered. | Antigravity AI |
| **v2.7.0** | 2026-08-16 | Overhauled `CalendarView.jsx` & `/api/daily-pnl`: (1) Cleaned up page title to `Calendar` and removed statement subtext `"Daily gains & losses heatmap"`. (2) Sourced date-accurate daily wealth logs from `portfolio_eod_logs.json` (6,890+ daily session records from 2007 to 2026). (3) Added preset time pills `1M`, `3M`, `6M`, `1Y`, `2Y`, `3Y`, `5Y`, `10Y` (2016-2026), and `ALL` (2007-2026), alongside custom date pickers. (4) Built interactive month filter selector pills (`2026-08`, `2026-07`, etc.) for seamless navigation across multi-year trading histories. | Antigravity AI |
| **v2.8.0** | 2026-08-16 | Refined `CalendarView.jsx` UI & Portfolio Valuation Tracking: (1) Removed unwanted clutter ('Trading Sessions', 'span', '32 days', 'Filter Month'). (2) Adopted compact Dashboard-style date range controls with preset pills and calendar popover. (3) Formatted dates in clean human-readable format (`07 Aug 2026`). (4) Sourced complete portfolio valuation (Net Worth, Total Assets, Liabilities) across Bank, EPF, Loans, Equities, MFs, and NPS. (5) Redesigned selected day inspector with 3 dedicated metric cards (Net Worth, Total Assets, Liabilities). (6) Fixed mobile/narrow screen text truncation & overflow length across all grid cards. | Antigravity AI |
| **v2.9.0** | 2026-08-16 | Upgraded `CalendarView.jsx` aesthetics, state-matching borders, and interactive drill-down popup modal: (1) Synced card border colors with state (Red card = Red border, Green card = Green border, Neutral 0 day = Slate Blue card & border). (2) Displayed Total Portfolio Net Worth on top line of each card, followed by P&L gain/loss amount and percentage below. (3) Fixed bottom row border truncation with custom grid padding (`pb-4`). (4) Replaced Selected Valuation Date header with Window Net Performance summary displaying Range Net Change, Assets Growth, and Liabilities Delta. (5) Created interactive drill-down popup modal on card click displaying itemized asset & liability deltas and breakdown balances (Bank, Equities, MFs, US Stocks, EPF, NPS, Loans, Credit Cards). (6) Enlarged date font with bold mono styling (`07 Aug 2026`). | Antigravity AI |
| **v3.0.0** | 2026-08-16 | Polished Calendar UI & Clutter-Free Refactoring: (1) Removed subtitle "Daily portfolio valuations and Growth History" and all description comments. (2) Added padding wrapper (`p-2`) to grid container preventing selection ring clipping on the first row/outer boundaries. (3) Removed redundant "Portfolio" labels, rendering raw values directly. (4) Redesigned date headers with a highly creative, bold day layout (e.g. `07 Aug '26`). (5) Overhauled selected log detail popup to match the selected theme perfectly (Obsidian Dark, Midnight Blue, Sunset Rose, Clean Light, Warm Sand, Nordic Frost) using `.modal-surface` and glass subcards, restricting visibility to changed instruments only. | Antigravity AI |
| **v3.2.0** | 2026-08-16 | Standardized decimal point alignment & Portal modal fixes: (1) Standardized `formatMoney` inside `ThemeAuthContext.jsx` to enforce exactly 2 decimal places for INR currency globally, updating Indian Stocks, Mutual Funds, US Stocks, and Liabilities views. (2) Restructured the popup modal in `CalendarView.jsx` to render via React Portals (`createPortal`) directly into `document.body` with `AnimatePresence` nested internally. This fixes the Framer Motion layout shift and centers the modal relative to the viewport. (3) Customized modal backdrop with a lighter semi-transparent shade (`bg-black/40` with 2px blur) to prevent darkening the layout in light themes. (4) Re-designed summary cards with a sleek left accent bar (`border-l-4`) using the high-contrast `glass-subcard` token. | Antigravity AI |
| **v3.3.0** | 2026-08-16 | Calendar UX 5-Point Refinements & Instrument Drill-Down Accordion: (1) Renamed `Housing Loan (SBI)` to `Housing Loan` (removed SBI suffix). (2) Formatted loan balance amount in standard white/black text color matching other asset categories instead of Red. (3) Built interactive `>` chevron accordion on category rows, expanding to reveal underlying instruments and their specific valuations/deltas. (4) Eliminated `...` truncation on daily calendar cards by reducing grid columns and tightening character spacing (`tracking-tighter`). (5) Formatted `Win Rate` stats card to enforce 2 decimal places (`37.50%`). | Antigravity AI |
| **v3.4.0** | 2026-08-16 | Reverted Dropdown Accordion & Polished Summary Section Spacing & Theme Colors: (1) Reverted modal category dropdown accordion per user request, restoring a clean uncluttered list of changed balances. (2) Tightened vertical spacing between daily cards grid divider and Summary header (`mt-3 pt-3 space-y-3`), removing large empty blank gaps. (3) Enlarged Summary date range font (`text-xs sm:text-sm font-extrabold text-slate-400 font-mono`) for better legibility. (4) Updated Summary metric boxes (`NET WORTH CHANGE`, `ASSETS GROWTH`, `LIABILITIES DELTA`) to use `.glass-subcard` styling, ensuring backgrounds adapt to active light/dark themes. | Antigravity AI |
| **v3.5.0** | 2026-08-16 | Global icon and label refinement: (1) Replaced Indian Equities icon with `CandlestickChart`. (2) Renamed all instances of "Equities" to "Equity" across the project. (3) Differentiated confusing icons: Asset Allocation uses `Donut`, Reports uses `BarChart3`, NPS uses `Shield`, and EPF uses `Wallet`. (4) Standardized master context file naming from `LADDER.MD` to `LADDER.md`. (5) Integrated a live, persistent Date and Time clock stamp into `TopNavbar.jsx` visible across all pages. | Antigravity AI |
| **v3.5.1** | 2026-08-16 | Follow-up UI corrections: (1) Changed EPF icon from `Wallet` to `Briefcase` for better relevance. (2) Changed all instances of "Liabilities" to "Liability" globally across components, page headers, and API display names. (3) Fixed remaining backend API labels for Indian Equity and US Equity in `server/index.js`. | Antigravity AI |
| **v3.5.2** | 2026-08-16 | Enhanced Net Worth chart in Dashboard (`OverviewView.jsx`) with extended calendar range options (`2Y`, `3Y`, `5Y`, `10Y`) to match the `CalendarView` feature parity. Adjusted `ALL` range to calculate from inception up to 20 years. | Antigravity AI |
| **v3.5.3** | 2026-08-16 | Standardized Reports icon to use `PieChart` in `Sidebar.jsx` and cleaned up `ReportsView.jsx` header text to just "Report" without sub-taglines. | OpenRouter (Delegated) |
| **v3.6.0** | 2026-08-16 | Upgraded `ReportsView.jsx` to support dynamic asset filtering (Combined Portfolio, Consolidated Equity, Fixed Income). Added a new Market Cap chart and dynamic Sector filtering. Implemented benchmark selector (Nifty 50, S&P 500, Custom) for the Portfolio Growth trajectory chart. Updated backend `/api/holdings` routes in `server/index.js` to accept and persist `market_cap` metadata natively. | Antigravity AI |
| **v3.7.0** | 2026-08-16 | Major architecture refactor for Reports and Database. Created `asset_metadata_seed.sql` to instantiate the `asset_metadata` table in Supabase, separating Sector and Capitalisation mappings (Mega, Large, Mid, Small, Micro) from holdings. Updated `ReportsView.jsx` with strict tabbed architecture separating Equity charts (Market Cap, Sectors) from Fixed Income/NPS. Expanded benchmarks to Nifty 50, Nifty Midcap 150, Nifty 250, S&P 500, NASDAQ. | Antigravity AI |
| **v3.8.1** | 2026-08-21 | Made dashboard performance asset class rows clickable to navigate to their respective portfolio pages. Colored Assets in Net Worth hero card green. | Antigravity AI |
| **v3.8.2** | 2026-08-21 | Rendered HoldingDetailModal using React Portals to prevent viewport layout shift and scrolling when clicking bottom items. Added a global "Back" button to TopNavbar for easier navigation back to the Dashboard. | Antigravity AI |
| **v3.8.3** | 2026-08-21 | Moved global Back button to the bottom of the screen as a floating action button. Made portfolio data rows entirely clickable to open the detail modal. Reverted logo API to Hunter as Clearbit API was inaccessible. | Antigravity AI |
| **v3.8.4** | 2026-08-21 | Upgraded logo resolution algorithm to cascade across `icon.horse` (high-res favicons), `Google Favicon API` (128px), and finally `logos.hunter.io` to ensure robust, crisp, and high-quality logo rendering for all portfolio views. | Antigravity AI |
| **v3.8.6** | 2026-08-21 | Enhanced historical pricing engine: Implemented dynamic fetch of exact USD/INR daily exchange rates via Yahoo Finance (INR=X) for perfectly accurate historic conversion of US stocks. Added dynamic symbol remapping fallback for renamed Indian tickers (e.g. TATAMOTORS -> TMPV.NS, SWANENERGY -> 503310.BO). Fixed Calendar Heatmap `/api/daily-pnl` route to read high-fidelity exact EOD `total_wealth` values instead of legacy approximations. | Antigravity AI |
| **v3.9.0** | 2026-08-21 | Replaced sparse chart timeline rendering with a new 'Dense Timeline Engine' in `server/index.js`. The previous charting logic throttled datapoints to a maximum of 300, and fell back to sparse transaction-dates-only when Yahoo Finance failed to fetch historical prices. The new engine programmatically iterates through every single calendar date from the first transaction to the present day, calculating and carrying forward the last known valuation to create a mathematically flawless, high-fidelity daily charting dataset. This resolves the issue of Recharts skipping weekends and drawing rigid, non-interactive straight lines across transaction gaps. | OpenRouter (Delegated) |
| **v4.0.0** | 2026-08-21 | Added 'Actual Chart' functionality to `HoldingDetailModal.jsx` and updated Dense Timeline Engine to output pure asset prices alongside invested/value metrics. The Holding Detail Modal now features a tab toggle between 'Tracker Chart' (Invested vs Market Value) and 'Actual Chart' (pure historical price of the stock/scheme). The Actual Chart includes scatter-plot event overlays for BUY (green), SELL (red), DIVIDEND (amber), and BONUS (cyan) dates. Both charts now share a unified date range filter (`1M`, `3M`, `6M`, `1Y`, `ALL`, and `CUSTOM` picker) with responsive dynamic y-axis scaling. Built a custom tooltip for the Actual Chart to cleanly display the exact transaction quantities and amounts on event dates. | Antigravity AI |
| **v4.0.1** | 2026-08-21 | Refined Actual Chart tooltips: Implemented a 'magnetic' tooltip logic that snaps to nearby events within a 9-day window to solve high-density X-axis hover sensitivity. Switched active tracker dot to a distinct white/grey style to prevent confusion with BUY events. Fixed duplicate dividend displaying on tooltips caused by redundant table processing in `server/index.js`. Standardized global date formats to `DD-MM-YYYY` across all chart ticks, tooltips, and transaction ledgers. | Antigravity AI |
| **v4.0.2** | 2026-08-26 | Removed serial numbers (`#`) column from transaction ledgers in `HoldingDetailModal.jsx` across all portfolio asset types to streamline UI. Adjusted table layout and empty state colspan. | Gemini 3.1 Pro (Low) |
| **v4.0.3** | 2026-08-26 | Refactored investment metrics: `Total Invested` now includes BUY charges and excludes BONUS logic. `Total Redeemed` now subtracts SELL charges. Added `Current Invested` and `Total Charges` (with Buy/Sell breakdown) to `HoldingDetailModal`. Dividends are now accurately added to `Realized P&L` and registered as positive cashflows in the XIRR calculation array. Fixed a residual cost basis bug in the dense timeline that occurred when a holding was completely sold. | Gemini 3.1 Pro (Low) |
| **v4.0.4** | 2026-08-26 | Complete redesign of Performance Summary metrics in HoldingDetailModal. Replaced flat identical card grid with a two-tier visual hierarchy: (1) a featured Total P&L hero card with dynamic theme-aware gradient background (emerald/rose dark-glow in dark mode, subtle emerald/rose tinted surfaces in light themes), radial glow, and inline Unrealized/Realized breakdown separated by a vertical divider; (2) compact metric tiles with colored left accent strips (blue/indigo/cyan/white/amber/orange/green-red) for Invested, Redeemed, Cost Basis, Market Value, Dividends, Charges, and XIRR. | Gemini 3.6 Flash |
| **v4.0.5** | 2026-08-27 | Implemented comprehensive clutter-free UI updates across all portfolio views and HoldingDetailModal: (1) Cleaned up page banners across Indian Stocks, US Stocks, Mutual Funds, and NPS views, removing redundant sorting/mechanism labels and adding Invested Value stats alongside Active Value and Gain; (2) Upgraded Price columns across all portfolio tables (Indian Equity, US Equity, Mutual Funds, NPS, universal HoldingsTable) to display live prices with today's price movement (green/red change amount and percentage); (3) Upgraded HoldingDetailModal with a prominent live price and day change badge in the header, plus an institutional Market Snapshot bar displaying Open, Prev Close, Day High, Day Low, and an animated interactive 52-Week Range indicator bar with full light/dark theme adaptation. | Antigravity AI |
| **v4.0.6** | 2026-08-27 | Visual distinction and light mode aesthetic upgrade: (1) Added prominent `ACTIVE` and `EXITED`/`REDEEMED` status pill badges to all portfolio table rows across Indian Stocks, US Stocks, Mutual Funds, NPS, and HoldingsTable; (2) Styled closed/exited rows with desaturated logos, muted typography, and clean dashes for inactive investment values while active rows pop with vibrant high-contrast accents; (3) Added live price quotes and today's percentage changes to TopNavbar search autocomplete dropdown; (4) Enhanced light theme styling across table headers, input surfaces, and filter tabs for a luxury fintech visual experience. | Antigravity AI |
| **v4.0.7** | 2026-08-27 | Real-time live quote auto-engine and banner alignment upgrade: (1) Aligned banner stat boxes across US Stocks, Indian Stocks, Mutual Funds, and NPS views with matching top, middle, and bottom baselines across both Invested and Active Value columns; (2) Upgraded `priceEngine.js` to continuously pull real-time `regularMarketPrice` quotes during active US & Indian market hours; (3) Added automated 20-second background polling and window focus/visibility listeners in `App.jsx` and recurring 30-second live background scheduler in `server/index.js`, so all total portfolio values, gains, losses, and XIRR update dynamically with live market movements. | Antigravity AI |
| **v4.0.8** | 2026-08-27 | Parallelized high-speed live quote ticker engine: (1) Diagnosed price latency caused by sequential looping over 350+ holdings blocking the event loop; (2) Implemented `refreshActiveHoldingsPrices()` with parallel `Promise.all` fetching for active US stocks and concurrent batches for active Indian stocks (< 1-2s total execution time); (3) Introduced in-memory `liveQuoteCache` delivering real-time quotes to `/api/holdings` and `/api/summary` with zero latency; (4) Upgraded `App.jsx` polling interval to 5 seconds and server ticker loop to 10 seconds for real-time market ticking. | Antigravity AI |
| **v4.1.0** | 2026-08-28 | Removed 'All Data' filter tab across Indian Stocks, US Stocks, Mutual Funds, and NPS views, simplifying status navigation to 'Active Positions' and 'Fully Redeemed'. Overhauled Fully Redeemed row aesthetics with 100% opacity, crisp typography, full-color logos, and EXITED/REDEEMED status badges. Implemented dynamic table headers and closed position metrics displaying Shares/Units Sold, Avg Buy Price/NAV, Avg Sell Price/NAV, Invested Cost Basis, Total Redeemed Proceeds, and Realized P&L. | Antigravity AI |
| **v4.1.1** | 2026-08-28 | Enhanced quote timestamp tracking: (1) Added quoteDate/asOfDate extraction to `priceEngine.js` (from Yahoo Finance `regularMarketTime`, AMFI date, and Protean NPS `navDate`); (2) Displayed prominent `As of [Date]` timestamp badges in `HoldingDetailModal.jsx` header and across portfolio view page banners (Indian Equity, US Equity, Mutual Funds, NPS). | Antigravity AI |
| **v4.1.2** | 2026-08-28 | Accelerated live quote ticker engine: (1) Reduced backend `refreshActiveHoldingsPrices()` interval from 10s down to 3s in `server/index.js`; (2) Reduced frontend background polling in `App.jsx` from 5s down to 2s for near-instant tick updates during live market hours. | Antigravity AI |
| **v4.1.3** | 2026-08-28 | Resolved database connection error: Relaunched backend Express API server process (`node server/index.js`) on port 5000 connected to Supabase Cloud PostgreSQL database. Verified `/api/summary`, `/api/holdings` (397 holdings), and `/api/liabilities` returning HTTP 200 OK. | Antigravity AI |
| **v4.1.4** | 2026-08-28 | Fixed quote date resolution accuracy: (1) Added `formatCleanQuoteDate` parser in `priceEngine.js`; (2) Ensured Mutual Funds (AMFI) and NPS (Protean) return the exact published NAV date (e.g. 27 Aug 2026) rather than falling back to current system date; (3) Verified Axis Small Cap Fund returns 27 Aug 2026 while NSE stocks return 28 Aug 2026. | Antigravity AI |
| **v4.1.5** | 2026-08-28 | Universal quote date validation across all asset portfolios: (1) Added `formatQuoteBadgeDate` standardizer in `dateFormatter.js`; (2) Verified that Indian Stocks, US Stocks, Mutual Funds, NPS schemes, and EOD assets accurately extract and display their true underlying exchange/feed quote dates (e.g. 27 Aug 2026 for AMFI MFs vs 28 Aug 2026 for NSE/BSE stocks). | Antigravity AI |
| **v4.1.6** | 2026-08-28 | Fixed modal header alignment & exchange timezone accuracy: (1) Reordered `HoldingDetailModal.jsx` header so price, day change, and quote date are strictly anchored at the right before the close button across ALL portfolio categories; (2) Shifted US Stock FX badge and USD/INR toggle to the left of the price block; (3) Added exchange timezone awareness (`America/New_York` for US stocks) ensuring US quotes display true US trading session date (27 Aug 2026); (4) Fixed NPS fallback to extract official CRA published NAV date (27 Aug 2026). | Antigravity AI |
| **v4.1.7** | 2026-08-28 | Search bar runtime bug fix & Mandatory Regression Testing Protocol: (1) Fixed unhandled ReferenceError in `TopNavbar.jsx` by adding missing `formatMoney` destructuring from `useThemeAuth()`; (2) Built `ErrorBoundary.jsx` and wrapped top navigation header and primary view renderer to prevent full-screen blanking on runtime exceptions; (3) Documented mandatory Regression Testing and QA Protocol in `LADDER.md`. | Antigravity AI |
| **v4.2.0** | 2026-08-28 | Corporate Actions & Multi-Layer Validation Engine: (1) Diagnosed and fixed Stock Split bug in `load_all_indian_stocks.mjs` and `server/index.js` where stock splits (e.g. 10:1 split in WEBELSOLAR) were previously ignored, causing artificial over-sell deficits and bogus realized losses; (2) Implemented retroactive FIFO buy lot scaling, split multiplier calculations, and split-adjusted cost basis tracking; (3) Added same-day transaction priority sorting (BUY/BONUS -> SPLIT -> SELL -> SELL ALL -> DIVIDEND) eliminating 28 same-day selling race conditions; (4) Enhanced `HoldingDetailModal.jsx` transaction ledger to render dedicated indigo SPLIT badges (+X shares, descriptive ratio notes, and dash for price/amount) and cyan BONUS badges (+X shares, 0 cost basis); (5) Implemented multi-tier validation engine in backend `/api/add-investment` and frontend (`AddInvestmentView.jsx`, `AddAssetModal.jsx`) with datatype checks, null assertions, live over-sell guardrails, and real-time split/bonus position projection previews for active holdings. | Antigravity AI |
| **v4.2.1** | 2026-08-28 | Real-Time Live Ticker & USD/INR Forex Stream Upgrade: (1) Diagnosed and resolved live ticker freeze on screen: `/api/holdings` was previously omitting `current_price: currentPriceNum` in returned holding records, causing the frontend to retain stale database prices until navigation; (2) Integrated real-time live ticking USD/INR Forex feed (`INR=X`) into `priceEngine.js` `fetchFxRate()`, replacing static 24-hour daily snapshots; (3) Added dedicated real-time pulsing USD/INR Forex pill badge to `TopNavbar.jsx`; (4) Synchronized `/api/summary` portfolio valuation calculation with in-memory `liveQuoteCache` for instantaneous net worth, asset totals, and P&L tick updates; (5) Replaced overlapping `setInterval` with a self-scheduling non-overlapping ticker loop and optimized active Indian stock batching to complete in < 1.5 seconds. | Antigravity AI |
| **v4.2.2** | 2026-08-28 | Exact Decimal Precision & Live Modal Ticker Integration: (1) Fixed `.00` precision truncation across all Net Worth hero metrics (Net Worth, Assets, Liabilities, Invested, Gain, Day P&L) in `/api/summary` by replacing `Math.round()` with exact floating-point 2-decimal numbers (`Number(val.toFixed(2))`), restoring accurate paise/cent figures; (2) Integrated 2-second live polling into `HoldingDetailModal.jsx` so open detail views dynamically stream price ticks, day changes, and valuations in real-time; (3) Updated `/api/holding/:id/detail` to derive current quotes and valuations directly from in-memory `liveQuoteCache`. | Antigravity AI |
| **v4.2.3** | 2026-08-28 | Corporate Action Separator Bars & Split-Adjusted Chart Trajectory Fix: (1) Fixed historical chart distortion for stocks with stock splits and bonus issues (e.g. WEBELSOLAR, GAIL, FCL, TEMBO): historical buy transactions for open stocks are now stored and calculated in split-adjusted quantity and unit price from their acquisition date, preserving exact total invested amounts while aligning with split-adjusted historical market quotes; (2) Replaced artificial +shares ledger rows with clean, full-width Corporate Action event separator bars in `HoldingDetailModal.jsx` displaying date, corporate action type (Stock Split / Bonus Issue), and ratio; (3) Eliminated verbose clutter and notes from corporate action displays; (4) Upgraded dense timeline construction in `/api/holding/:id/detail` to track smooth, unbroken market value trajectories without artificial 10x drops or spikes. | Antigravity AI |
| **v4.2.4** | 2026-08-28 | Light/Dark Theme High-Contrast Polish & Global Real-Time FX Polling: (1) Enhanced Corporate Action event bars in `HoldingDetailModal.jsx` with centered alignment, crisp high-contrast theme styling (vibrant indigo/cyan backgrounds and dark text in light mode, luminous glow in dark mode), and bonus shares received text (e.g. `+100 Shares Received as Bonus`) replacing ratio text; (2) Fixed FX pill and currency toggle buttons in modal header to display high-contrast styling in light theme, eliminating grayed-out look; (3) Added dedicated `/api/fx-rate` backend endpoint and integrated continuous 3-second live Forex polling in `ThemeAuthContext.jsx` so live USD/INR exchange rate updates dynamically across all dashboard headers, modals, and conversion widgets. | Antigravity AI |
| **v4.2.5** | 2026-08-28 | Natural Date Alignment & Non-Repetitive Corporate Action Layout: (1) Restructured Corporate Action rows in `HoldingDetailModal.jsx` so the transaction Date is left-aligned in its natural first-column position matching the table header, while the corporate action badge and concise detail are centered across the remaining columns; (2) Removed repetitive wording so "Stock Split" and "Bonus" are not duplicated (e.g. `[Stock Split] Ratio 1:10` and `[Bonus Issue] +100 Shares Credited`). | Antigravity AI |
| **v4.2.6** | 2026-08-28 | Live Chart Tracker Extension & Real-Time Endpoint Anchoring: (1) Diagnosed why chart timelines appeared flat/stopped after August 21: offline historical daily price files were snapshot up to 21 Aug, causing the dense timeline to carry forward flat prices for August 22-28; (2) Updated `server/index.js` dense timeline engine to dynamically bind today's date directly to live ticking quotes from `liveQuoteCache` / Yahoo Finance, restoring active chart curve movement right up to the current second; (3) Appended live balance points to EOD asset timelines (Bank, EPF, Loans). | Antigravity AI |
| **v4.2.7** | 2026-08-28 | Calendar Heatmap & Daily PnL Synchronization: (1) Re-executed `scripts/rebuild_portfolio_eod.mjs` across all 6,900 daily EOD records (2007-2026), incorporating split-adjusted and bonus-adjusted stock positions from Supabase; (2) Verified that the Calendar Heatmap (`CalendarView.jsx` and `/api/daily-pnl`) displays smooth daily wealth and PnL trajectories on corporate action dates (e.g. Nov 2025 WEBELSOLAR split and Sep 2022 GAIL bonus) with zero artificial valuation spikes. | Antigravity AI |
| **v4.2.8** | 2026-08-28 | Actual & Tracker Chart High-Contrast Palette Upgrade: (1) Replaced the emerald green price line with an Electric Sky Blue palette (`#38bdf8` in Dark Theme, `#0284c7` in Light Theme) across both the Actual Chart (Asset Price curve) and Tracker Chart (Market Value area/stroke) in `HoldingDetailModal.jsx`; (2) Eliminates visual collision with emerald green BUY event dots (`#10b981`), allowing BUY signals, SELL signals (`#f43f5e`), DIVIDEND dots (`#f59e0b`), and BONUS badges to stand out with distinct clarity against the price curve. | Antigravity AI |
| **v4.2.9** | 2026-08-28 | Corporate Action Royal Purple & Fuchsia Pink Palette Upgrade: (1) Replaced cyan/indigo styles for corporate actions with Royal Purple (`#a855f7`) for Stock Splits and Vibrant Fuchsia Pink (`#ec4899`) for Bonus Issues across scatter event dots, tooltip badges, and transaction ledger rows in `HoldingDetailModal.jsx`; (2) Creates full visual separation between the Sky Blue price curve, Green BUY dots, Red SELL dots, Amber Dividend dots, Purple Split markers, and Pink Bonus markers in both Light and Dark themes. | Antigravity AI |
| **v4.3.0** | 2026-08-28 | Bonus Issue Warm Brown / Bronze Palette Upgrade: (1) Replaced pink styling for Bonus Issues with Warm Saddle Brown / Bronze (`#b45309`) across scatter event dots, tooltip badges, and transaction ledger rows in `HoldingDetailModal.jsx`; (2) Completely eliminates any proximity confusion between pink and bright red SELL signals (`#f43f5e`), giving Bonus Issues an earthy, distinct visual signature. | Antigravity AI |
| **v4.3.1** | 2026-08-28 | US Stocks Corporate Action & Dividend Ledger Parity: (1) Diagnosed and fixed omission in `load_all_us_stocks.mjs` where stock splits from `Book2.xlsx` (e.g. Arista Networks ANET 4:1 Split on 2024-12-04) were omitted during US equities ingestion; (2) Implemented retroactive FIFO buy lot scaling, split multiplier calculations, and split-adjusted cost basis tracking for US stocks matching Indian stocks parity; (3) Added same-day event priority sorting (BUY -> SPLIT -> SELL -> DIVIDEND) in US stocks ingestion; (4) Updated `/api/holding/:holdingId/detail` in `server/index.js` to interleave and format dividends (`DIVIDEND` type) chronologically into the Transaction Ledger with amber badges and dual USD/INR currency amounts; (5) Re-ingested all 12 US stocks and re-executed `scripts/rebuild_portfolio_eod.mjs` across all 6,900 daily records. | Antigravity AI |
| **v4.3.2** | 2026-08-28 | Combined Portfolio Performance Banners: Restored and upgraded the display of Combined Cost Basis and Overall P&L in the portfolio category views (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, `NpsView`). The new design introduces a distinct glowing combination block that aggregates Active Cost Basis, Redeemed Cost Basis, Unrealized P&L, and Realized P&L without cluttering the UI, displaying both Absolute % and XIRR % returns dynamically. | Antigravity AI |
| **v4.3.3** | 2026-08-28 | Clean & Aligned Category Banner & Tab Bar Refactor: (1) Streamlined the top banner across all 4 category views (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, `NpsView`) to feature a single, high-contrast Combined Performance card (`Combined Cost` and `Overall Return` with Absolute & XIRR percentages), eliminating redundant subtexts, extra cards, and layout clutter; (2) Relocated contextual Active / Redeemed metrics into the filter tab bar row as an inline summary badge (`Invested`, `Current Value`, `P&L` for Active; `Cost`, `Redeemed`, `Realized P&L` for Redeemed), ensuring perfect grid alignment with search boxes and filter tabs. | Antigravity AI |
| **v4.3.4** | 2026-08-28 | Ultra-Minimal Integrated Micro-Bar Architecture (Concept 3 Rollout): (1) Redesigned the header across `IndianStocksView`, `UsStocksView`, `MutualFundsView`, and `NpsView` to achieve maximum vertical breathing room: top banner contains only the live asset title badge and primary action button; (2) Implemented an ultra-sleek integrated micro-bar in the table controls row that seamlessly anchors the segmented tab switch (`Active` vs `Redeemed`) side-by-side with a high-density, clutter-free financial stat strip showing `Combined Cost`, `Net Return (Abs % | XIRR %)`, and live contextual tab metrics (`Invested → Value` or `Cost → Redeemed`); (3) Responsive layout dynamically flexes on all screen sizes with zero horizontal or vertical overflow. | Antigravity AI |
| **v4.3.5** | 2026-08-28 | Unified High-End Micro-Bar Styling (Pixel-Exact Preview Parity): (1) Polished the integrated control & performance bar in `IndianStocksView`, `UsStocksView`, `MutualFundsView`, and `NpsView` into a single cohesive glassmorphic container (`bg-slate-900/80 rounded-2xl border border-slate-800/80`); (2) Removed redundant duplicate contextual text, unifying segmented tabs (`Active` vs `Redeemed`), an elegant vertical divider, high-contrast financial stats (`Combined: Cost • Return [ +Abs% | XIRR% ]`), and integrated search box in one harmonious horizontal bar matching the visual preview mockup. | Antigravity AI |
| **v4.3.6** | 2026-08-28 | Dual-Compartment Split Header Card (Option C Implementation): (1) Implemented a single, high-contrast dual-compartment glass card positioned directly to the left of the primary action button across all 4 category views (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, and `NpsView`); (2) Left compartment displays `COMBINED` badge, complete cost basis formatted to 2 decimal places, net return, `Abs: +X.XX%`, and `XIRR: +X.XX%`; (3) Right compartment dynamically switches between `ACTIVE` (invested, current market value, net gain, and `Abs: +X.XX%`) and `REDEEM` (historical cost basis, realized redeemed proceeds, net realized P&L, and `Abs: +X.XX%`) when tabs are toggled; (4) Fully aligned micro-typography with zero clutter across light and dark themes. | Antigravity AI |
| **v4.3.7** | 2026-08-28 | Split Card Centered Headers & Color-Coded Precision Polish: (1) Updated both `COMBINED` and `ACTIVE` / `REDEEM` headers to be centered badges atop each compartment across `IndianStocksView`, `UsStocksView`, `MutualFundsView`, and `NpsView`; (2) Standardized fields to clean stacked rows: `Cost : <value>` and `Return : <value>` with exact 2-decimal precision; (3) Added independent color-coding (emerald for profit, rose for loss) to both `Abs: +X.XX%` and `XIRR: +X.XX%` on both sides; (4) Verified layout alignment and successful build. | Antigravity AI |
| **v4.3.8** | 2026-08-29 | Robust Multi-Tier XIRR Engine & US Equity Currency Toggle Fix: (1) Fixed missing `DollarSign` import and wired `toggleCurrency` in `UsStocksView.jsx`; (2) Diagnosed and eliminated mathematical artifacts in XIRR caused by pooling unsynchronized closed holding cashflows lacking exit SELL transactions; (3) Upgraded `/api/summary` in `server/index.js` to compute holding-level weighted XIRRs, exposing distinct `activeXirrPct`, `closedXirrPct`, and `xirrPct` (Combined); (4) Connected individual category views to display true Active XIRR (+18.93% for Indian Stocks) and Combined XIRR (+7.52%), eliminating negative rate anomalies on profitable portfolios. | Antigravity AI |
| **v4.3.9** | 2026-08-29 | Uniform Header Action Alignment & Global roiPct Resolution: (1) Fixed `roiPct is not defined` crash in `NpsView.jsx` and `MutualFundsView.jsx` by explicitly declaring `roiPct`; (2) Relocated `USD ($) / INR (₹)` currency toggle in `UsStocksView.jsx` from the top header to the table controls bar beside the search input; (3) Aligned top-right split returns card and primary action button (`+ Add Asset`) across all four asset categories (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, `NpsView`) with pixel-perfect consistency; (4) Validated clean build with 0 warnings/errors. | Antigravity AI |
| **v4.4.0** | 2026-08-29 | Price Column Typography & Alignment Precision Upgrade: (1) Fixed baseline misalignment and text wrapping in Price column where day changes and Unicode arrow symbols broke onto multiple lines; (2) Replaced unicode arrow characters with crisp Lucide `ArrowUp` and `ArrowDown` icons with `stroke-[3]` and `shrink-0` locked to the exact text baseline; (3) Applied `whitespace-nowrap inline-flex` across `UsStocksView.jsx` and `IndianStocksView.jsx` ensuring numbers, currency symbols, and percentages maintain horizontal alignment. | Antigravity AI |
| **v4.4.1** | 2026-08-29 | Holding Detail Modal KPI Terminology Clarification: (1) Replaced confusing and overlapping labels in `HoldingDetailModal.jsx` (`Invested`, `Redeemed`, `Cost Basis`, `Market Value`) with clear, unambiguous, intuitive terms: `Total Bought` (all historical purchase capital), `Total Sold` (all historical sale proceeds), `Current Cost` (cost basis of active shares currently held), and `Current Value` (live market valuation of active shares); (2) Verified clean build with 0 errors. | Antigravity AI |
| **v4.4.2** | 2026-08-29 | Calendar Spreadsheet Table View & Interactive View Switcher: (1) Added seamless view switcher in `CalendarView.jsx` allowing users to toggle between Heatmap Grid View and a Spreadsheet Table View; (2) Implemented animated, sortable table displaying Date (with weekday), Net Worth, Daily P&L, % Change, Total Assets, Total Liabilities, and Changed Categories activity indicators; (3) Enabled column sorting (Date, Net Worth, Daily P&L, % Change, Assets, Liabilities) defaulting to descending chronological order; (4) Maintained full drill-down parity: clicking any table row opens the exact daily valuation and balance change inspector modal; (5) Verified clean build with 0 errors. | Antigravity AI |
| **v4.4.3** | 2026-08-29 | Continuous Daily EOD Sync & Full portfolio.xlsx Multi-Column Spreadsheet View: (1) Diagnosed calendar cut-off on 21 Aug: historical price tables ended on Aug 21, and `rebuild_portfolio_eod.mjs` omitted bank/EPF/loan carry-forwards, dropping calendar net worth to 33L; (2) Upgraded `rebuild_portfolio_eod.mjs` to reconstruct all 6,912 daily records from inception (2007) right up to the current date (2026-08-29), carrying forward bank savings, EPF, and liabilities, aligning calendar net worth (₹1.884 Cr) with the dashboard; (3) Redesigned the Calendar Table view into an exact clone of `portfolio.xlsx` with frozen sticky Date column, category header groupings (Bank Savings, Investment Portfolios, Total Assets, Liabilities & Debt, Net Wealth & Performance), and all individual columns (`HDFC`, `IndusInd`, `IDFC`, `RBL`, `SBI`, `Savings`, `Mutual Funds`, `Indian Stocks`, `US Stocks`, `NPS`, `EPF`, `Total Assets`, `Loan`, `Credits`, `Debt`, `Net Wealth`, `Daily P&L`, `% Change`); (4) Restored true previous-day lookups in `/api/daily-pnl` so clicking any date reveals the exact asset and liability balance movements recorded on that day. | Antigravity AI |
| **v4.4.4** | 2026-08-29 | Automated Real-Time Daily Pricing Ingestion & Dynamic Multi-Asset Valuation Sync: (1) Fixed root cause of stagnant prices after 21 Aug by building and executing `scripts/sync_daily_prices.mjs` which dynamically ingests live daily market quotes for 86 Indian stocks (Yahoo Finance .NS/.BO), 11 US stocks, 16 Mutual Funds (MFAPI daily NAV), 3 NPS schemes (Protean/NPSNAV daily NAV), and daily USD/INR FX rates up to 29 Aug 2026; (2) Rebuilt EOD logs yielding true daily changing net worth (e.g. 25 Aug: ₹1.844 Cr [-₹1.13L], 26 Aug: ₹1.854 Cr [+₹1.00L], 27 Aug: ₹1.858 Cr [+₹34.3k], 28 Aug: ₹1.860 Cr [+₹21.7k], 29 Aug: ₹1.860 Cr [-₹1.7k]), perfectly matching the ₹1.86 Cr dashboard value; (3) Restarted backend server process so `/api/daily-pnl` serves all 18 portfolio columns without zeros; (4) Enhanced date click inspector with dual-tab view ('Changes' vs 'All Balances') to review both daily market movements and complete portfolio snapshot. | Antigravity AI |
| **v4.4.5** | 2026-08-29 | Weekend Carry-Forward Alignment & Unified Assets Table Header Refactor: (1) Corrected US stock & market asset valuation on weekends: aligned US market closing session (Friday night / Saturday early AM IST) so that Saturday and Sunday calendar entries properly carry forward Friday's finalized valuation without phantom weekend FX rate noise (29 Aug now cleanly reflects ₹1.860 Cr with ₹0.00 daily change); (2) Restructured the Spreadsheet Table Header in `CalendarView.jsx` to unify `Total Assets` inside a single top-level `ASSETS` header (spanning all 12 asset columns: 6 bank accounts + 5 investment portfolios + Total Assets), creating perfect symmetry with `LIABILITIES & DEBT` (spanning Loan + Credits + Debt); (3) Verified clean production build with 0 errors. | Antigravity AI |
| **v4.4.6** | 2026-08-29 | Supabase Cloud Database Historical EOD Migration & Complete Excel Decoupling: (1) Upgraded Supabase `pnl_history` table schema with all 18 granular asset/liability breakdown columns and unique date constraint; (2) Migrated all 6,912 daily historical records (2007 through 2026-08-29) from the spreadsheet directly into Supabase PostgreSQL Cloud; (3) Reconfigured EOD rebuild engine (`rebuild_portfolio_eod.mjs`) to load base records and persist daily updates directly to Supabase `pnl_history`, severing runtime dependency on `portfolio.xlsx` so the spreadsheet can be deleted cleanly without data loss. | Antigravity AI |
| **v4.4.7** | 2026-08-29 | Daily EOD Pricing Reconciliation & Mandatory Universal Page Synchronization Rule: (1) Updated `data/historical_prices.json` with latest settled Friday closing prices (2026-08-28) across active holdings; (2) Rebuilt all 6,912 historical EOD logs via `scripts/rebuild_portfolio_eod.mjs` and synchronized with Supabase `pnl_history` (29 Aug 2026 EOD net worth now precisely reflects ₹18,663,893.69, fully aligned with Dashboard Net Worth); (3) Enforced mandatory Cross-Functional Data Integrity rule across `LADDER.md` and `.agents/AGENTS.md` requiring all updates in any portfolio view or database table to immediately sync with all dependent views (Calendar, Dashboard, Holding Detail Modals, Category Banners, Reports). | Antigravity AI |
| **v4.4.8** | 2026-08-29 | Calendar Table View Action Column Removal: Removed redundant 'Action / View' column and header from the Calendar spreadsheet table in `CalendarView.jsx`. Entire row remains clickable for opening day valuation breakdown modal. | Antigravity AI |
| **v4.4.9** | 2026-08-29 | Non-Trading Day & Weekend Precision Alignment: (1) Fixed floating-point precision summation discrepancy between 28 Aug and 29 Aug in `scripts/rebuild_portfolio_eod.mjs`; (2) Weekend non-trading days now carry forward Friday's exact valuation, total assets, liabilities, and net worth with exactly `₹0.00` daily change (0.00%); (3) Synchronized with Supabase `pnl_history`; (4) Production build verified with 0 errors. | Antigravity AI |
| **v4.5.0** | 2026-08-29 | Zero-Variance Mathematical Parity Across Dashboard & Calendar: (1) Standardized per-holding 2-decimal rounded accumulation in `scripts/rebuild_portfolio_eod.mjs` to match `/api/holdings` and `/api/summary` exactly; (2) Rebuilt all 6,912 EOD logs; (3) Verified exact 1-to-1 zero-paise match (`₹18,663,893.70` Net Worth, `₹23,162,513.86` Total Assets, `₹44,98,620.16` Total Liabilities) across Dashboard, Calendar, and Supabase. | Antigravity AI |
| **v4.5.1** | 2026-08-29 | Centralized Shared Single-Source-of-Truth Valuation Engine: (1) Created `server/services/portfolioCalculator.js` (`computeHoldingValueINR` and `computePortfolioValuation`) as the single mathematical authority; (2) Refactored `server/index.js` (`/api/summary`, `/api/holdings`) and `scripts/rebuild_portfolio_eod.mjs` to consume the same shared module; (3) Zero mathematical divergence across all endpoints and views; (4) Verified build with 0 errors. | Antigravity AI |
| **v4.5.2** | 2026-08-29 | Dashboard Net Worth Chart True EOD Integration & Redundant FX Badge Cleanup: (1) Removed redundant `$1 = ₹95.38` badge from the Net Worth hero card in `OverviewView.jsx`; (2) Replaced the synthetic noise/math formula in the Net Worth History chart with true historical daily EOD logs fetched from `/api/daily-pnl` (e.g. 2007-2026 real daily records), ensuring the chart displays exact historical portfolio valuations across all range presets (`1M`, `3M`, `6M`, `1Y`, `2Y`, `3Y`, `5Y`, `10Y`, `ALL`); (3) Production build verified with 0 errors. | Antigravity AI |
| **v4.5.3** | 2026-08-29 | Dark Theme Native Date Picker Visibility Fix: Applied `[color-scheme:dark]` CSS property to all `<input type="date">` elements across `OverviewView.jsx`, `CalendarView.jsx`, and `AddInvestmentView.jsx` to ensure native browser calendar icons are inverted and clearly visible in dark mode. | Antigravity AI |
| **v4.5.4** | 2026-08-29 | Dynamic Positive/Negative Coloring for Net Worth Chart: Updated `OverviewView.jsx` to dynamically compare the start and end values of the selected timeframe. The area chart's stroke, fill gradient, and tooltip text now turn Red (`#EF4444`) if the final portfolio value is lower than the starting value of the range, and Emerald (`#10B981`) if it is higher. | Antigravity AI |
| **v4.5.5** | 2026-08-29 | Removed Hardcoded Daily PnL Fallbacks: Fixed a bug in `server/index.js` (`/api/summary`) where the daily PnL percentage was hardcoded to `0.82%` and the absolute change was falling back to an incorrect calculation (`totalGainINR * 0.008`) if the database fetch returned empty. The endpoint now correctly defaults to local `portfolio_eod_logs.json` as a fallback and accurately reports `0.00` change for non-trading weekend days. | Antigravity AI |
| **v4.5.6** | 2026-08-29 | Reinstated 2-Decimal Precision on Category Metrics: Fixed an issue in `server/index.js` where the `/api/summary` endpoint was using `Math.round()` on category `investedINR`, `currentINR`, `realizedINR`, and `unrealizedINR`. Replaced with strict `.toFixed(2)` float precision to ensure the dashboard's Performance table correctly renders fractional paise instead of defaulting to `.00`. | Antigravity AI |
| **v4.5.7** | 2026-08-29 | Pre-May 12 2023 Indian Equity Charges Population & Parity Sync: (1) Parsed `charges.xlsx` and populated pre-2023-05-12 transaction charges across 365 Indian Equity transactions in Supabase; (2) Handled multiple transactions on the same date by allocating total charges to the transaction with the highest trade value (`quantity * price`); (3) Applied confirmed symbol aliases (`HDFC` -> `HDFCBANK`, `MAHINDCIE` -> `CIEINDIA`, `HBLPOWER` -> `HBLENGINE`, `ZOMATO` -> `ETERNAL`, `GET&D` -> `GVT&D`, `SKIPPERPP` -> `SKIPPER`); (4) Recalculated `total_charges` across 105 affected holdings and re-synchronized all 6,912 daily EOD logs via `scripts/rebuild_portfolio_eod.mjs` and Supabase `pnl_history`; (5) Verified production build. | Gemini 3.7 Flash |
| **v4.5.8** | 2026-08-29 | Post-May 12 2023 Missing Indian Equity Charges Full Population: (1) Parsed remaining 735 records from `charges.xlsx` and populated charges for 615 previously unpopulated post-May 12 2023 Indian Equity transactions in Supabase; (2) Preserved already populated charges (24 transactions); (3) Re-aggregated and updated `total_charges` across 147 affected holdings; (4) Rebuilt all 6,912 daily EOD logs via `scripts/rebuild_portfolio_eod.mjs` and synced with Supabase `pnl_history`; (5) Verified production build. | Gemini 3.7 Flash |
| **v4.5.9** | 2026-08-29 | Dashboard Net Worth Chart DD-MM-YYYY Hover Date & 0-Baseline Reference Line: (1) Enhanced Net Worth area chart tooltip in `OverviewView.jsx` to render the complete `DD-MM-YYYY` calendar date on hover (e.g. `24-06-2026`); (2) Added a subtle dashed `ReferenceLine` at `y=0` when the selected timeframe's Y-domain spans across or below 0; (3) Added negative sign formatting to Y-axis tick values and tooltips; (4) Production build verified with 0 errors. | Gemini 3.7 Flash |
| **v4.6.0** | 2026-08-29 | Net Worth Chart Clean 0 Y-Axis Tick: (1) Removed the text label (`₹0 Baseline`) from the reference line in `OverviewView.jsx`; (2) Explicitly injected `0` into the Y-axis `ticks` array when the domain spans $\le 0$ and $\ge 0$, rendering a clean `0` tick directly on the vertical axis; (3) Verified production build. | Gemini 3.7 Flash |
| **v4.6.1** | 2026-08-29 | Calendar Multi-Granularity Engine (Daily / Monthly / Yearly): (1) Added a segmented Granularity selector `[ Daily | Monthly | Yearly ]` to `CalendarView.jsx`; (2) Implemented robust chronological aggregation mapping across all 18 portfolio asset/liability columns with exact period-end snapshots and true cumulative period P&L / % return calculations; (3) Enabled full interactive Heatmap Grid and Spreadsheet Table view rendering at monthly and annual scale; (4) Upgraded drill-down popup modal to dynamically render period labels and period category changes; (5) Production build verified with 0 errors. | Gemini 3.7 Flash |
| **v4.7.0** | 2026-08-29 | Institutional Portfolio Engine Upgrades: (1) Safety & Rollback: Created paginated backup and restore engine (`scripts/dump_db_snapshot.mjs` and `scripts/restore_db_snapshot.mjs`) generating verified snapshots past Supabase limits; (2) Universal Recalculation Engine: Built `recalculateHoldingState` in `server/services/recalculator.js` handling full chronological FIFO replay, corporate actions (bonus, stock split), inflows/outflows, and integrated with `/api/transactions/:id` and `/api/db-table-update`; (3) US Equity UX: Added explicit USD ($) trade price notice and live converted INR preview in `AddInvestmentView.jsx`; (4) Multi-Pass NPS & MF Engine: Built Protean CRA daily scraper, created `nps_daily_navs` table in Supabase, added `GET /api/nav/nps/:schemeCode`, added on-demand Refresh NAVs button and status badges to `MutualFundsView.jsx` and `NpsView.jsx`, and scheduled hourly GitHub Actions workflow `.github/workflows/daily_nav_sip_sync.yml`; (5) Delta-Ledger Migration: Backfilled 4,996 historical delta transactions for Bank accounts, EPF, Loans, and Credit Cards from 2007-2026 EOD logs into Supabase `transactions` table with foreign key reference `liability_id`, enabling full chronological balance reversibility; (6) Recurring SIP Engine: Created `sips` table in Supabase, `/api/sips` CRUD routes, automated execution engine (`processDueSips`), and interactive `SipManagerModal` in `MutualFundsView.jsx`; (7) Re-synchronized all 6,912 daily EOD logs via `scripts/rebuild_portfolio_eod.mjs` and Supabase `pnl_history`; (8) Verified production build. | Gemini 3.7 Flash |
| **v4.7.1** | 2026-08-29 | Sync Engine Bug Fix & Search Bar Width Expansion: (1) Fixed a bug in `server/services/priceEngine.js` where `fetchProteanNpsNavBatch` was returning a wrapper object rather than the `navMap` instance, causing `proteanNavMap.get is not a function` during price sync; (2) Expanded the top navigation search bar container in `TopNavbar.jsx` from `w-48 md:w-64` to `w-72 sm:w-80 md:w-96 lg:w-[380px]`, preventing placeholder text truncation (`Search tickers, investments, liabilities...`); (3) Tested live sync endpoint (`/api/refresh-prices`) and verified production build with 0 errors. | Gemini 3.7 Flash |
| **v4.7.2** | 2026-08-29 | Bank, EPF, Loan & Credit Card Transaction-Based UI Ingestion: (1) Upgraded `AddInvestmentView.jsx` to replace legacy static balance input forms with modern transaction-based forms for Bank Accounts (`Deposit/Credit` vs `Spend/Debit`), EPF (`Contribution` vs `Withdrawal`), Loans (`EMI Payment` vs `Borrow`), and Credit Cards (`Expense/Charge` vs `Bill Payment`); (2) Added live before/after balance and outstanding debt projection pills with dynamic formatting; (3) Added description/notes fields for transaction category tagging; (4) Verified production build with 0 errors. | Gemini 3.7 Flash |
| **v4.7.3** | 2026-08-29 | Audit-Driven Bug Fixes and Engine Hardening: (1) Fixed critical bug in `DELETE /api/transactions/:id`, `PUT /api/transactions/:id`, and `POST /api/db-table-update` where liability-linked transactions (loans, credit cards) were not triggering `recalculateHoldingState` because only `holding_id` was checked while `liability_id` was ignored; (2) Hardened `recalculator.js` to query transactions by `holding_id` / `liability_id` only, removing fragile `symbol`/`name` OR-matching that could cross-contaminate unrelated records; (3) Added `OPENING_BALANCE` support for liabilities in the recalculator; (4) Improved SIP engine: added `end_date` auto-closure, removed dangerous NAV fallback to 100, added skip-tracking for unavailable NAVs; (5) Fixed form reset after successful submission to use `getDefaultTxType()` per portfolio; (6) Added context-specific success messages per portfolio type; (7) Restricted GitHub Actions cron to NAV declaration window hours only (8 runs/day instead of 24); (8) Verified production build with 0 errors. | Claude Opus 4.6 |
| **v4.7.4** | 2026-08-30 | Price Sync Refactor & UI Polish: (1) Diagnosed Node.js crashes caused by sequential network requests timing out in `refreshAllHoldingsPrices`; (2) Refactored backend `priceEngine.js` to process US Equities, Indian Equities (in concurrent batches of 15), Mutual Funds, and NPS via parallel `Promise.all` with localized `try/catch` blocks, preventing cascading errors; (3) Updated frontend `TopNavbar.jsx` with a 3-second temporary emerald `Synced` UI checkmark post-refresh; (4) Wired real-time `quote_date` from `liveQuoteCache` back to `/api/holdings` output; (5) Fixed "Latest" badge rendering logic across `MutualFundsView.jsx`, `NpsView.jsx`, and `HoldingDetailModal.jsx` ensuring exact timestamp matching. | Antigravity AI |
| **v4.7.5** | 2026-08-30 | GitHub Actions Speedup & Cloud Sweep Parallelization: (1) Diagnosed latency in `syncAllMissingNavs()` caused by repetitive Protean CRA ZIP downloads and sequential MF requests; (2) Added 30-minute in-memory cache for `fetchProteanNpsNavBatch()` and parallelized `Promise.all` execution across all Mutual Funds and NPS schemes, slashing script runtime from 1m 21s down to 4.1s; (3) Upgraded GitHub Actions workflow `.github/workflows/daily_nav_sip_sync.yml` with `actions/cache@v4` on `node_modules` and deterministic `npm ci --omit=dev`, eliminating redundant npm package resolution overhead. | Antigravity AI |
| **v4.7.6** | 2026-08-30 | Universal Real-Time Single Source of Truth Valuation Engine: (1) Resolved architectural data sync discrepancy between Dashboard Net Worth and Calendar current day value by refactoring `/api/daily-pnl` to inject the live, real-time snapshot for the current date using `computePortfolioValuation` with live price feeds (`liveQuoteCache`), identical to `/api/summary`; (2) Updated `/api/summary` to compute `dayPnlINR` and `dayPnlPct` dynamically relative to yesterday's closing wealth; (3) Updated `/api/holding/:holdingId/detail` for Bank, EPF, and Debt instruments to dynamically reflect live database balances on today's timeline; (4) Added 5-second background polling in `CalendarView.jsx` ensuring all views remain in 100% dynamic lockstep without requiring batch scripts; (5) Verified exact 1-to-1 parity across Dashboard and Calendar. | Gemini 3.7 Flash |
| **v4.7.7** | 2026-08-30 | Bank Ledger Deduplication, Recalculator Query Pagination & Weekend Valuation Parity: (1) Diagnosed and resolved +₹8.59L artificial jump on Aug 30 caused by duplicate delta transactions and 1,000-row Supabase query truncation in `recalculator.js`; (2) Added chunked paginated transaction fetching to `server/services/recalculator.js` removing the 1,000-row limit; (3) Cleanly purged duplicate delta transactions and re-executed `scripts/migrate_balance_history_to_transactions.mjs`, reconciling HDFC Savings balance to exact ₹10,619.89; (4) Updated `data/historical_prices.json` with exact settled Friday closing prices (e.g. OIL @ ₹482.85, NPS NAVs); (5) Re-synchronized all 6,913 daily EOD logs via `scripts/rebuild_portfolio_eod.mjs`; (6) Verified Saturday (Aug 29) and Sunday (Aug 30) accurately report exactly ₹0.00 daily P&L and 0 changes with exact ₹18,708,675.57 Net Worth across all views. | Gemini 3.7 Flash |
| **v4.8.0** | 2026-08-30 | Reports Engine Overhaul: Market Cap, MF Look-Through, Sector Drill-Down, and Real-Time Benchmarks: (1) High-Contrast Tooltips: Replaced Recharts default tooltip with the executive dashboard glassmorphism popover featuring bold typography, color dots, formatted currency, and 2-decimal percentage readouts; (2) Top 10 + Other Grouping: Eliminated pie chart truncation by aggregating holdings beyond Top 10 into an `Other (X assets)` slice so 100% of allocation is accounted for; (3) Live Internet Market Cap Engine: Created `asset_metadata` Supabase table and automated sync script (`scripts/sync_asset_metadata.mjs`) categorizing all stocks from internet data into Mega, Large, Mid, Small, and Micro Cap; (4) Mutual Fund Look-Through: Created `mutual_fund_holdings` Supabase table and sync script (`scripts/sync_mf_holdings.mjs`) mapping underlying company holdings, weights, and allocated rupee values across all active schemes; (5) Interactive Sector Drill-Down: Enabled click-to-inspect on sector bars/slices opening a full breakdown of constituent companies, sources, and allocations; (6) Real-Time Growth Benchmarks: Fetched 2 years of daily historical data for Nifty 50, Nifty Midcap 150, Nifty Smallcap 250, S&P 500, and NASDAQ (`scripts/sync_index_history.mjs`), rendering date-by-date normalized trajectory with alpha calculation; (7) Dynamic Chart Switcher: Added toggles between Donut/Pie, Ranked Bar Chart, and Detailed List. | Gemini 3.7 Flash |
| **v4.8.1** | 2026-08-30 | Minimalist Fintech Polish, Scope-Aware Growth Trajectories, Ranked Progress Lists & Nightly Sync: (1) Minimalist Text Rule: Removed all verbose paragraphs and subtitles across headers and cards, establishing strict zero-unnecessary-text rule in `.agents/AGENTS.md`; (2) Calendar Date Range Picker: Added custom start/end date popover alongside preset pills (`1M`, `3M`, `6M`, `1Y`, `ALL`) in Growth vs Indices; (3) Floating Button Removal & Inline Back Button: Removed intrusive floating bottom button that blocked text, adding an inline `← Back` button in `TopNavbar.jsx`; (4) Scope-Aware Growth Engine: Updated `/api/reports/growth-benchmarks` to dynamically compute historical trajectories for selected Equity Hub scopes (e.g. Indian Stocks only vs All); (5) Ranked Progress List: Replaced overlapping horizontal Recharts bar charts with institutional animated ranked progress bars; (6) Fixed theme borders and card clipping; (7) Created `.github/workflows/nightly_metadata_sync.yml` running every night at 23:00 IST (17:30 UTC). | Gemini 3.7 Flash |
| **v4.8.2** | 2026-08-30 | 3D Interactive Depth Graph, Visible Benchmark Lines, Unified Sector Normalization & Full MF Expansion: (1) Light Theme Contrast: Upgraded text and border contrast across all filter buttons, tabs, and cards; (2) Removed Serial Numbers & Stabilized Layout: Removed `#1`, `#2` tags and eliminated list re-render jumping; (3) Benchmark Tracking Line: Converted chart to `ComposedChart` ensuring visible dashed benchmark lines with custom color markers; (4) 3D Interactive Graph: Replaced third toggle with animated 3D isometric extruded depth bars with lighting gradients and spring tilt physics; (5) Full Mutual Fund Company Expansion: Populated complete constituent company holdings across all active funds and grouped cash/debt items into `Cash, Debt & Other`; (6) Custom Glassmorphism Dropdown: Replaced plain rectangle `<select>` with luxury animated scheme selector; (7) Unified Broad Sectors: Implemented unified sector normalizer across US Stocks, Indian Stocks, and Mutual Funds eliminating "Mutual Funds" pseudo-sector. | Gemini 3.7 Flash |
| **v4.8.3** | 2026-08-30 | Pure Light Theme Surface Refactor & Clean Bar Chart Mode: (1) Fixed Light Theme Surfaces: Refactored `ReportsView.jsx` to use pure white cards (`bg-white`), crisp slate borders (`border-slate-200`), dark charcoal text (`text-slate-900`), and dark mode selectors (`dark:bg-slate-900`), eliminating dark muddy containers in Light mode; (2) Clean Bar Chart: Replaced 3D view with institutional Recharts Bar Chart mode (`Donut` vs `Bar Chart`) with non-squished names, clean Y-axis in Lakhs, and hover tooltips; (3) Verified production build and 100% financial integrity test suite. | Gemini 3.7 Flash |
| **v4.8.4** | 2026-08-30 | Chart Switcher Context Isolation, Recharts Anti-Jitter Stabilization & Comprehensive Mutual Fund Expansion: (1) Contextual Chart Switcher: Removed Donut/Bar Chart switcher from non-applicable views (`Mutual Fund Look-Through` and `Growth vs Indices`), displaying it only for `Allocation`, `Market Cap`, and `Sectors`; (2) Anti-Jitter Stabilization: Disabled continuous Recharts animation loops and memoized scope bindings, eliminating graph refreshing/jumping on background price ticks; (3) Comprehensive Mutual Fund Expansion: Populated full 30 to 45 constituent company holdings across all 16 active mutual fund schemes (467 rows in Supabase), reducing the "Other" cash buffer to minimal 2-4% and eliminating outliers. | Gemini 3.7 Flash |
| **v4.8.5** | 2026-08-30 | 10-Year Benchmark History & Simulated Index Opportunity Growth Engine: (1) 10-Year Index History: Upgraded `scripts/sync_index_history.mjs` to fetch 10-year daily history across Nifty 50, Nifty Midcap 150, Nifty Smallcap 250, S&P 500, and NASDAQ; (2) Simulated Index Opportunity Tracking: Refactored `/api/reports/growth-benchmarks` to simulate what the starting invested capital would be worth if invested in the benchmark index over the chosen timeframe, eliminating flat 0 benchmark curves when viewing "ALL" or custom historical ranges; (3) Verified production build and 100% financial integrity suite. | Gemini 3.7 Flash |
| **v4.9.0** | 2026-08-30 | Reports Engine Upgrade: Interactive Company Mutual Fund Breakdown, Zero-Quantity Holding Valuation Fix, Strict Theme Fidelity, Benchmark Historical Date Alignment & Floating Back Button: (1) Zero-Quantity Direct Holding Fix: Hardened `computeHoldingValueINR` and `companyMfBreakdown` so equity shares with 0 quantity (e.g. historical redeemed trades) return strictly ₹0.00 instead of falling back to the single-share price; (2) Company Mutual Fund Breakdown Modal: Clicking any company in MF Composition, Market Cap, or Sector opens a detailed modal showing total portfolio value, direct shares, and a scheme-by-scheme breakdown of every Mutual Fund holding that stock with exact fund weight (%), allocated rupee amount, and share of holding (%); (3) Strict Theme Fidelity: Replaced hardcoded dark background and white text classes with responsive adaptive classes (`text-slate-900 dark:text-white`, `bg-white/80 dark:bg-slate-900/80`, `border-slate-200 dark:border-slate-800`), eliminating white text and dark containers in Light / Warm Light modes; (4) MF Composition Report: Renamed the dedicated Mutual Fund tab to 'MF Composition' with a theme-matching dropdown; (5) Benchmark Historical Date Alignment: Refactored `/api/reports/growth-benchmarks` so simulated benchmark trajectory starts at the exact historical valuation on the initial date of the range (e.g. ₹50.9k on 2020-03-30); (6) Aggregated Company Drill-Down: Merged direct and multi-scheme holdings into a single unified row per company; (7) Floating Persistent Back Button: Added a semi-transparent floating circle button at bottom-right with only the arrow symbol; (8) Verified 100% financial integrity test suite. | Gemini 3.7 Flash |
| **v4.9.1** | 2026-08-30 | Money-Weighted Benchmark Engine: Replaced the single-base-capital simulated benchmark with a true money-weighted benchmark mirroring actual cash flows. Root cause: Supabase 1,000-row pagination limit caused only CREDIT/DEBIT bank transactions to be fetched (missing all 5,789 BUY/SELL transactions). Fixes: (1) Paginated BUY/SELL-only transaction fetch loop added; (2) Incremental virtual unit accumulation: each BUY purchases virtual index units at that date's index price, each SELL proportionally reduces units; (3) `_Normalized` field = money-weighted INR value on chart y-axis; `_GrowthPct` = pure index price return from window start for summary stat cards; (4) For ALL scope, chart starts from first investment asset date (not first bank deposit in 2007); (5) Bank/savings excluded from benchmark as non-market assets; (6) Verified production build. | Antigravity AI |
| **v4.9.2** | 2026-08-30 | High-Performance Benchmark Caching & Binary Search Engine: Diagnosed and resolved 5-second latency on the Growth vs Indices benchmark report caused by repeated 6-batch Supabase transaction roundtrips (1.9s) and un-indexed string date sorting inside the transaction loop (~2.7s CPU event-loop spin across 25,000 iterations). Built `server/services/benchmarkEngine.js` with: (1) Pre-sorted index date arrays and O(log N) binary search lookup (1000x faster than inline sorting); (2) High-speed in-memory caching of scoped transactions and holdings with automatic cache invalidation on new investments/metadata sync; (3) Sub-10ms response times for timeframe switching (`1M`, `3M`, `6M`, `1Y`, `ALL`); (4) 100% financial integrity suite pass. | Gemini 3.7 Flash |
| **v4.9.3** | 2026-08-30 | UI Theme Alignment & Table Border Consistency: (1) Calendar View Theme Fix: Replaced `bg-slate-950/95` with `bg-slate-900/95` in `CalendarView.jsx` sticky columns to properly hook into the `.light` mode background overrides (`#F1F5F9`), resolving black headers on light theme; (2) Consistent Table Row Borders: Replaced `divide-y` with direct `[&>tr]:border-b` across all portfolio table bodies (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, `NpsView`, `DividendsView`, `DatabaseStudioView`, `CalendarView`, `HoldingsTable`) to ensure the first row correctly falls back to the header's `border-bottom` and eliminates the visual missing top-border illusion caused by CSS table border collapse. | Antigravity AI |
| **v4.9.4** | 2026-08-30 | Data Cleaning & UX Formatting: (1) Fixed sector override bug where missing metadata sectors saved as `'Unknown'` were overwriting valid user-entered database sectors; (2) Hid the `'Unknown'` sector text entirely from Exited shares, Indian Stocks, US Stocks, and Mutual Funds to prevent UI clutter; (3) Cleaned US Stock names by stripping out verbose legal/registry terms (e.g., "Common Stock", "Class A", "Registry Share") to leave only the recognizable company name. | Antigravity AI |
| **v4.9.5** | 2026-08-30 | UI & Daemon Fixes: (1) Fixed issue where US Stock names were not actively stripped because the backend daemon was not actively listening on port 5000 (restarted daemon process); (2) Removed `border-l-2` from all portfolio table rows across `UsStocksView`, `NpsView`, `MutualFundsView`, and `IndianStocksView` to eliminate the left-edge gray vertical line and match the borderless right edge. | Antigravity AI |
| **v4.9.6** | 2026-08-30 | Exited Shares Metadata Sync: (1) Modified `scripts/sync_asset_metadata.mjs` to fetch metadata from screener/APIs for *all* holdings, removing the active (`quantity > 0`) filter, so exited shares now have their sectors and market cap populated. (2) Added an automated asynchronous background trigger in `POST /api/holdings` to run `syncAssetMetadata()` immediately upon adding any new share, ensuring future exited or active shares automatically pull their metadata without manual intervention. | Antigravity AI |
| **v4.9.7** | 2026-08-30 | High-Speed Metadata Engine Upgrade: Replaced slow sequential Screener web scraping with concurrent, high-throughput Yahoo Finance primary API (`yahoo-finance2`) integration in `scripts/sync_asset_metadata.mjs`. Completed full metadata sync across all 343 assets (including previously missing exited shares such as `AARTIDRUGS`, `AARTIPHARM`, `ACMESOLAR`, `ADORWELD`). Verified 100% financial integrity pass. | Antigravity AI |
| **v4.9.8** | 2026-08-30 | Portfolio Views Header Redesign & Minimalist Layout Alignment: (1) Removed generic "As of [Date]" text from Mutual Funds, NPS, Indian Equity, and US Equity banners; (2) Placed "Schemes / Stocks Up-to-Date" status indicator badges directly below portfolio titles; (3) Added a minimalist, icon-only Refresh button directly to the right of each portfolio title text with interactive spin animations; (4) Moved the "Recurring SIP" button from the top banner to the left of the search window inside the Mutual Funds table controls bar; (5) Wired real-time price & NAV refresh across all 4 portfolio views; (6) Verified 100% financial integrity test pass. | Gemini 3.7 Flash |
| **v4.9.9** | 2026-08-30 | Indian & US Stocks Table Row Refresh Status Badges & Active Count Normalization: (1) Added individual quote refresh status pills (`Today (Date)` / `As of Date`) next to ticker and sector in `IndianStocksView.jsx` and `UsStocksView.jsx` matching Mutual Funds and NPS styling; (2) Unified `isUpToDate` date matching across all 4 views; (3) Fixed `upToDateCount` calculation in all 4 views to strictly filter active holdings (`quantity > 0`), ensuring header status indicators reflect accurate ratios; (4) Verified 100% financial integrity pass. | Gemini 3.7 Flash |
| **v5.0.0** | 2026-08-30 | Header Clutter Cleanup: Removed the redundant USD/INR exchange rate badge from the US Equity banner in `UsStocksView.jsx`, preserving global exchange rate display in the top navigation bar. | Gemini 3.7 Flash |
| **v5.1.0** | 2026-08-31 | Multi-Colour Gradient Hero Banners & Consolidated Performance Report Integration: (1) Upgraded Indian Equity (`IndianStocksView.jsx`), US Equity (`UsStocksView.jsx`), Mutual Funds (`MutualFundsView.jsx`), and NPS (`NpsView.jsx`) hero banners to use large multi-colour fading gradient typography matching EPF and Bank Account styles; (2) Replaced the two-compartment split box in asset headers with clean active valuation readouts, invested cost, unrealized gain, and active XIRR % pills; (3) Created a dedicated Consolidated Performance report in `ReportsView.jsx` (`PERFORMANCE` sub-tab) featuring executive KPI cards, asset class comparison grid, and an institutional performance ledger comparing active vs realized vs combined lifetime metrics across all portfolio categories; (4) Verified production build and 100% financial integrity test suite pass. | Gemini 3.7 Flash |
| **v5.2.0** | 2026-09-02 | Daily Automated EOD Sync & Cloud Valuation History Integration: (1) Automated EOD Workflow: Created `.github/workflows/daily_eod_sync.yml` running daily at 23:45 UTC (05:15 IST) to fetch settled market quotes and persist updated EOD records to Supabase `pnl_history`; (2) Seamless EOD Log Merging: Refactored `/api/daily-pnl` in `server/index.js` to dynamically merge new records from `pnl_history` with the local EOD cache; (3) Historical NPS Integration: Enhanced `scripts/rebuild_portfolio_eod.mjs` with `fetchNpsHistoricalNav` to accurately price historical NPS positions; (4) Incremental Market Price Fetch: Added `--incremental` flag to `scripts/load_historical_prices.mjs` to fetch only missing market days; (5) Aligned dashboard day P&L in `/api/summary` to compute relative to verified previous day closing net worth from historical logs. | Antigravity AI |
| **v5.3.0** | 2026-09-05 | Holding Detail Actual Chart Event Indicators, Axis Standardization & Bank Account Controls: (1) Restored Actual Chart Event Indicators: Re-enabled `ActualEventDot` with action-specific color rings and event tooltips in `HoldingDetailModal.jsx`; (2) Tooltip & Axis Precision: Standardized axis formatting (`formatAxisValue`) and fixed fractional precision across Tracker and Actual charts, and hid Actual Chart for bank accounts; (3) Bank Account State & Modals: Added active/closed filter tabs, account reopening with current balance prompt, and closing with custom themed confirmation modal in `BankView.jsx` and `App.jsx`; (4) Theme-Aware Transaction Tokens: Migrated transaction type buttons in `AddInvestmentView.jsx` and `src/index.css` to semantic CSS theme variables (`--tx-buy`, `--tx-sell`, `--tx-bonus`, `--tx-dividend`, `--tx-split`); (5) Calendar Table Trend Contrast: Upgraded period-over-period trend color styling in `CalendarView.jsx` table view; (6) Status Badge De-Cluttering: Removed redundant row-level status pills across equity, MF, and NPS views. | Antigravity AI |
| **v5.3.1** | 2026-09-05 | EPF View Minimalist De-Cluttering & Dynamic Metric Integration: (1) Removed static interest rate pill badge (`FY 2025-26: 8.25% p.a.`) and filler descriptive tagline from EPF hero header; (2) Removed redundant `Inspect Timeline` button and simplified action button in member account card to clean `View`; (3) Removed static screenshot rows 2 and 3 (~50% corpus assumptions, static compounding statement); (4) Replaced static cards with dynamic live metric cards (1-Year Growth `+₹7.04L (+18.05%)`, Est. Annual Yield `+₹3.80L`, and Last Credit `+₹62,508.00` on 12-06-2026); (5) Verified production build and 100% financial integrity test suite pass. | Gemini 3.8 Flash |
| **v5.3.2** | 2026-09-05 | EPF Holding Detail Minimalist UI & Actual Chart Removal: (1) Removed redundant `EPF` bold header, `EPF-RETIREMENT` badge, and `Employee Provident Fund` subtitle from the `HoldingDetailModal.jsx` for EPF assets; (2) Disabled the 'Actual Chart' tab for EPF assets as it is redundant for non-market EOD tracking assets. | Gemini 3.1 Pro (Low) |
| **v5.3.3** | 2026-09-05 | EPF Logo Enhancements: (1) Standardized the EPF icon across the sidebar to use the `Building2` icon, aligning it visually with the EPF page hero header; (2) Implemented automatic fetching of the official EPFO logo from Wikimedia Commons in `domain.js`, eliminating the basic `EP` text fallback in the `HoldingDetailModal.jsx` header. | Antigravity AI |
| **v5.3.4** | 2026-09-05 | Liabilities & Debt View Modernization and Minimalist De-Cluttering: (1) Removed redundant '2 Active Obligations' subtext from hero banner; (2) Removed 'Monthly EMI' from hero header and from both individual liability cards; (3) Cleaned loan title by removing '(SBI Bank)' to display clean 'Housing Loan'; (4) Removed interest rate ('8.5% p.a.' / '0.0% APR') and 'Due' day subheaders from both Loan and Credit Card instruments; (5) Implemented 'Active Obligations' and 'Closed Obligations' segmented toggle matching BankView; (6) Added dedicated Close and Reopen account/card action buttons with interactive modal confirmations, supporting balance zeroing on closure and balance re-entry on reopen via PUT /api/liabilities/:id. | Gemini 3.8 Flash |
| **v5.3.5** | 2026-09-05 | Refactored liabilities UI in HoldingDetailModal to dynamically display the lender name and logo instead of generic tags. Removed irrelevant Actual Chart toggle for debt accounts and cleaned up subtitle text for a strictly minimalist presentation. | Antigravity AI |
| **v5.4.0** | 2026-09-06 | Dynamic Housing Loan Amortization Calendar, Interactive Chart & Prepayment Engine: (1) Data Ingestion: Ingested 153 actual settled records from Investment.xlsx ('Loan Amortization') into Supabase 'loan_amortization' table and data/loan_amortization.json (₹69.98L sanctioned, ₹51.65L EMI paid, ₹11.38L prepayments, ₹37.69L interest paid, ₹44.64L current balance); (2) Dynamic Calculation Engine: Built server/services/loanEngine.js projecting future monthly schedules dynamically up to ₹0 balance with daily interest calculation, regular ₹60k EMI, and user-added dynamic entries; (3) Backend API: Added GET /api/loan/amortization, POST /api/loan/amortization/entry, DELETE /api/loan/amortization/entry/:id, and attached amortization payload to GET /api/holding/:id/detail for loans; (4) Frontend Component: Built LoanAmortizationSection.jsx with executive KPI tiles, Recharts Balance Payoff Trajectory / Annual Breakdown bar charts, Prepayment What-If Simulator, dynamic Add Entry modal, and Amortization Calendar Table with Settled and Projected filters; (5) Liabilities Integration: Added Amortization Schedule button to Housing Loan card in LiabilitiesView and embedded LoanAmortizationSection with sub-tab controls in HoldingDetailModal; (6) Verified 100% financial integrity test suite pass and clean production build. | Antigravity AI |
| **v5.4.1** | 2026-09-06 | Housing Loan Amortization User Refinements: (1) Clean Lender Title: Stripped '(SBI)' and 'SBI' occurrences across LiabilitiesView and HoldingDetailModal to cleanly show 'State Bank of India'; (2) Single Navigation Path: Removed duplicate 'Amortization Schedule' button from the Housing Loan card since the 'View' button already opens the detail modal; (3) Subtitle Renaming: Renamed 'Home Loan' to 'Housing Loan' in HoldingDetailModal; (4) Contractual EMI vs. Monthly Payment: Distinguished contractual EMI (₹52,653.00) from active monthly payment (₹60,000.00) in separate KPI tiles with inline installment edit modal and 'Update Monthly Installment / Payment' in Add Entry; (5) Exact Decimal Precision: Replaced all abbreviated currency amounts (44.64L, 44.97L) with exact rupee formatting (₹44,64,447.00, ₹69,98,345.00, ₹44,96,758.00); (6) Table Entry Actions: Added Edit and Delete actions to Amortization Calendar table rows with PUT /api/loan/amortization/entry/:id; (7) Fixed ReferenceError for `formatMoney` in HoldingDetailModal; (8) Verified 100% financial integrity test suite pass and clean production build. | Antigravity AI |
| **v5.4.2** | 2026-09-06 | Housing Loan Detail Modal Sub-Tab Ordering & Delete Modal Standardization: (1) Positioned `Daily Balance History` as the primary left button and `Amortization Schedule & Chart` on the right in `HoldingDetailModal.jsx`, and removed the calendar icon for a clean aesthetic; (2) Replaced native `window.confirm` with the project's standard themed modal popup (`.modal-surface`, backdrop blur, animated transitions, and explicit cancel/delete actions) in `LoanAmortizationSection.jsx`; (3) Replaced remaining instances of 'Home Loan' with 'Housing Loan' across `AddInvestmentView.jsx`, `data/liabilities.json`, and project documentation; (4) Sanitized lender name in Supabase liabilities table to cleanly store 'State Bank of India'; (5) Verified 100% financial integrity invariance pass and clean production build. | Antigravity AI |
| **v5.5.0** | 2026-09-06 | Dividends View Overhaul & Sidebar Logo Standardization: (1) Standardized NPS sidebar logo to use `ShieldCheck`, aligning it with the NPS main page header; (2) Standardized Dividends main page banner logo to use `Coins`, matching the sidebar; (3) Removed the Yield box and all "FX" labels in Dividends view (US card title updated to clean "US ($)"); (4) Overhauled Dividends ledger table to match Indian & US Equity format: integrated `HoldingLogo`, cleaned corporate/stock suffixes (removing "Common Stock", "Class A", etc.), and added asset drill-down to `HoldingDetailModal`; (5) Added market filter tabs (`All`, `Indian Equity`, `US Equity`), search bar, and sortable columns (Stock Name, Market, Payout, INR Credited, Date); (6) Removed redundant FX Rate column from table; (7) Verified 100% financial integrity test suite pass and clean production build. | OpenRouter (Delegated) / Antigravity AI |
| **v5.5.1** | 2026-09-06 | Dividends Dual-Currency Toggle Sync & Add Entry Modal Integration: (1) Synced Dividends metric cards with global currency toggle: in INR mode, US card displays INR as primary metric (`₹7,92,292.38`) with dollar equivalent below (`≈ $9,023.95`), and vice versa when USD mode is toggled; (2) Added emerald `Add Entry` button in Dividends banner; (3) Created dedicated `AddDividendModal.jsx` matching Indian/US Equity dividend transaction entry design: category toggle, stock search autocomplete with `HoldingLogo`, `DIVIDEND` amber badge, payout amount, payment date, live USD/INR exchange rate with converted INR preview; (4) Connected to `POST /api/add-investment` updating `dividends` table and seamlessly synchronizing with `HoldingDetailModal`, `OverviewView`, and equity pages; (5) Verified 100% financial integrity test suite pass and clean production build. | Antigravity AI |
| **v5.5.5** | 2026-09-06 | Removed Redundant 0.0% Allocation Pill in Reports: Removed the confusing and unneeded `0.0% Allocation` badge from the header of Asset Class Performance cards in `ReportsView.jsx`. Allocation weights are already comprehensively analyzed in the dedicated Allocation Donut and Bar charts with full look-through. Verified clean build and 100% financial integrity test suite pass. | Gemini 3.8 Flash |
| **v5.6.0** | 2026-09-06 | Universal Border Completion & Project-Wide Table Sort and Filter: (1) Universal Border Completion: Diagnosed and resolved cut-off/clipped borders on Ranked Progress list cards and cards in scroll containers caused by outset `ring-2` rendering beyond overflow bounds; added `.reports-subcard.is-selected` with explicit theme-adaptive inset borders and box-shadow in `index.css`, converted card selection rings to `ring-2 ring-inset` across `ReportsView.jsx`, `CalendarView.jsx`, and `CalendarHeatmap.jsx`, and added `p-1.5 custom-scrollbar` container padding to guarantee complete, unclipped borders on all sides; (2) Project-Wide Table Search & Column Sorting: Implemented real-time search filtering and clickable column sorting across all tabular displays in the application lacking them: (a) `ReportsView.jsx` Consolidated Category Performance Ledger (Search + Sort by Asset Class, Active Val, Active Cost, Unrealized P&L, Realized P&L, Lifetime Cost, Net Return, Abs ROI %, Annualized XIRR); (b) `ReportsView.jsx` Mutual Fund Look-Through Constituent Holdings (Search + Sort by Company, Sector, Cap Tier, Fund Weight %, Allocated Value); (c) `ReportsView.jsx` Market Cap Drill-Down (Search + Sort by Stock, Source, Contribution %, Allocated Value); (d) `ReportsView.jsx` Sector Drill-Down (Search + Sort by Company, Source, Cap Tier, Allocated Value); (e) `ReportsView.jsx` Company Mutual Fund Breakdown Modal (Search + Sort by Scheme, Fund Weight %, Allocated Value, Share of Holding %); (f) `OverviewView.jsx` Asset Class Performance table (Search filter bar); (g) `HoldingDetailModal.jsx` Transaction Ledger (Search filter bar + transaction type filter pills); (h) `AssetDividendDetailModal.jsx` Itemized Distribution Ledger (Search filter bar + Sort by Date, Original Payout, FX Rate, INR Credited, Cumulative Total); (i) `LoanAmortizationSection.jsx` Schedule table (Search filter bar + Sort by Date, Type, Opening Balance, EMI, Bulk Payment, Interest, Principal, Closing Balance, Rate); (3) Verified production build and 100% financial integrity invariance suite pass. | Antigravity AI |
| **v5.10.0** | 2026-09-06 | Multi-Asset Historical Auto-Tracking Engine Repair & Zero-Data-Gap Continuous Synchronization: (1) Root Cause Diagnosis: Resolved issue where Mutual Funds and NPS schemes were frozen/untracked on 2nd Sept and subsequent dates due to (a) omission of NPS in `scripts/load_historical_prices.mjs`, (b) baseline truncation flaw in `scripts/rebuild_portfolio_eod.mjs` where existing database logs prevented re-evaluating past dates upon late NAV publication, and (c) unpaginated database queries in `server/db.js`; (2) Ingestion & Historical Engine Hardening: Added `fetchNpsHistorical` and included NPS schemes across historical price sync engines; (3) Post-Baseline Re-evaluation: Refactored `rebuild_portfolio_eod.mjs` to re-evaluate all post-baseline dates (from 2026-08-08 through current day) from settled historical quotes and NAV feeds, dynamically healing any previously missing market days; (4) Database Pagination Guard: Upgraded `server/db.js` (`select` and `selectWhere`) with automatic pagination loops across 100% of rows (all 11,205 transactions, 397 holdings, liabilities); (5) Historical Parity Backfill: Rebuilt all 6,920 daily logs, backfilled settled NAVs for 2nd, 3rd, 4th, 5th, and 6th September across Mutual Funds and NPS, and synchronized with Supabase `pnl_history` and `data/portfolio_eod_logs.json`; (6) Verified 100% financial integrity test suite pass across all invariance assertions. | Antigravity AI |
| **v5.11.0** | 2026-09-06 | Bank & EOD Asset Ledger Refactor, Running Balance Accumulation & Badge Parity: (1) Diagnosed and resolved UI misconception in `HoldingDetailModal.jsx` where Bank, EPF, and Liability transaction ledgers mislabeled historical deposit/withdrawal transactions as 'Daily Balance History' and displayed individual transaction amounts under 'EOD Balance (₹)'; (2) Fixed false negative indicator bug where `isPos = tx.type === 'BUY'` caused all Bank and EPF deposits, credits, and withdrawals to render with a negative red `↓ -₹X` badge; (3) Built chronological running balance engine accumulating deposits, credits, withdrawals, debits, loan repayments, and borrow events into an exact post-transaction account balance column (`Running Balance (₹)`); (4) Expanded `TxBadge` with comprehensive badges for `CREDIT`, `DEPOSIT`, `DEBIT`, `WITHDRAWAL`, `CONTRIBUTION`, `INTEREST`, `BORROW`, `EMI_PAYMENT`, `CHARGE`, `PAYMENT`; (5) Enabled dynamic transaction type filter pill tabs across all holdings; (6) Verified production build and 100% financial integrity test suite pass. | Antigravity AI |
| **v5.12.0** | 2026-09-06 | Exhaustive raw data audit, NPS duplicate merge, CLEDUCATE sell correction, VIYASH missing stock ingestion, and zero balance precision hardening. | Antigravity AI |
| **v5.22.0** | 2026-09-12 | Official Protean CRA Direct NPS Scheme Alignment: Re-mapped active NPS Direct holdings to official PFRDA Multiple NAV codes (SM008019, SM003027, SM002027), backfilled 12 missing market days of official Protean daily NAVs, preserved redeemed POP schemes, and updated recalculator to retain lifetime average cost on closed positions. | Antigravity AI |
| **v5.23.0** | 2026-09-12 | Robust Universal On-Demand Refresh Engine & Non-Trading Day Parity: Normalized date parsing in fetchNpsNavFallback, upgraded refresh mechanisms to target last completed trading day on weekends/holidays, updated isUpToDate indicators across portfolio views, and refined profile dropdown UI. | Antigravity AI |
| **v5.24.0** | 2026-09-12 | Dynamic Multi-Asset & Multi-Year Market Calendar Engine: Built dynamic trading day and market holiday engine (server/services/marketCalendar.js) evaluating NSE/BSE/AMFI/NPS and NYSE/NASDAQ across arbitrary future years without hardcoding. Codified Rule 13 (Strict Anti-Hardcoding Protocol). | Antigravity AI |
| **v5.25.0** | 2026-09-12 | Mutual Funds and NPS 4-Decimal NAV Precision Upgrade: Upgraded database, server endpoints, and frontend UI to store, calculate, and display NAVs and unit prices with 4 decimal places for Mutual Funds and NPS. Extended formatMoney and added formatNAV in ThemeAuthContext.jsx. Updated MutualFundsView, NpsView, HoldingDetailModal, and HoldingsTable. Re-synchronized 4-decimal avg_buy_price across 47 fund holdings. | Antigravity AI |
| **v5.26.0** | 2026-09-13 | Reliability Hardening, Code Splitting, NPS Backfill, and Restore Lifecycle Tracking: (1) EOD Rebuild: Removed 90-row upsert cap in rebuild_portfolio_eod.mjs; all rebuilt logs now upserted to Supabase pnl_history in full 500-row batches with gap detection logging for any missing trading days; (2) DB Cache Warmup: Added pnl_history (last 365 records) to server/db.js warmCache() preventing cold-start Supabase egress on Dashboard and Calendar; (3) Integrity Script: Overhauled verify_all_assets_integrity.mjs with paginated future-row query, full NPS scan (no limit), dynamic date boundary from first record (no hardcoded dates), and smart NPS delta assertion that correctly skips pre-16:00 IST and weekend sessions; (4) Restore Job Lifecycle: Added in-memory restoreJobs Map to server/index.js with GET /api/cloud-backups/restore/status polling endpoint; RestoreBackupModal.jsx now shows live status banner (Restoring -> Rebuilding EOD -> Succeeded/Failed) instead of closing immediately; (5) Code Splitting: Converted all 14 view components to React.lazy() imports with Suspense boundary and ViewLoader fallback in App.jsx, splitting the monolithic 1.3 MB bundle into 14+ independent async chunks; (6) NPS Historical Backfill: Created scripts/backfill_nps_navs.mjs to fetch complete historic NAV series for all 14 Direct NPS scheme codes from npsnav.in and upsert to nps_daily_navs table with deduplication; (7) Dependency Cleanup: Removed Rule 8 npsnav.in restriction; npsnav.in is now an allowed and documented fallback source for historical NPS NAVs when Protean CRA data is unavailable; (8) Master Plan: Replaced stale data/implementation_plan.md (v5.19.1) with consolidated v5.25.0+ master implementation plan. | Antigravity AI |
| **v5.27.0** | 2026-09-13 | Security, Authentication, Branding, and Operational Safety: enforced JWT authentication with environment-managed secrets, removed demo and empty-password fallbacks, added validated account registration, added a responsive Ladder-branded login/register experience using the project logo, protected loan/report routes, restricted Database Studio table and column access, fixed mutation cache invalidation, made backup reads fail closed, validated restore snapshots, blocked concurrent restores, and reduced aggressive frontend polling. | GitHub Copilot |







---

## Mandatory End-to-End Dependency Mapping & Cross-Functional Impact Protocol

> [!CAUTION]
> **CRITICAL ARCHITECTURAL DIRECTIVE - ZERO DATA DISCREPANCY & UNIFIED CALCULATION ENGINE**:
> 1. **SINGLE REAL-TIME SOURCE OF TRUTH**: All pages (Dashboard, Calendar, Portfolios, Detail Modals, Reports) MUST update dynamically in real time and share the exact same underlying calculation engine (`computePortfolioValuation` and `liveQuoteCache`). Never implement separate, ad-hoc, or divergent calculation formulas or scripts for individual pages.
> 2. **DYNAMIC ZERO-SCRIPT SYNCHRONIZATION**: Today's current day metrics (Net Worth, Total Assets, Total Liabilities, Day P&L, and Category Breakdowns) across all views (including Calendar Heatmap and Holding Detail timelines) MUST compute dynamically on-the-fly from live database holdings and price feeds. No manual or background batch scripts should ever be required for the current day to be in sync.
> 3. **MANDATORY CROSS-PAGE AUDIT BEFORE COMPLETION**: Before completing any task, delivering a response, or presenting data to the user, the developer/agent **MUST autonomously verify every single affected view and section** (Dashboard, Calendar, Indian Equity, US Equity, Mutual Funds, NPS, Bank, EPF, Liabilities, Dividends, Reports, and Holding Detail Modals) to confirm that all records, totals, and breakdowns match with exact 1-to-1 parity down to the cent.
> 4. **MANDATORY AUTOMATED INTEGRITY SUITE**: Before handing over any task, the developer/agent MUST execute `node scripts/verify_financial_integrity.mjs` and obtain a 100% PASS across row counts, balance sheet equations, API parity, and weekend invariance assertions.
> 5. **ZERO TOLERANCE FOR OUT-OF-SYNC PAGES OR UNVERIFIED HEURISTICS**: Any change made in one place (holdings, transactions, prices, dividends, or liabilities) MUST immediately and autonomously be reflected across all applicable pages and views. No view can be left out of sync, and financial numbers must never be guessed or heuristically approximated.

To guarantee that changes made in one area (such as corporate actions, transaction calculations, real-time quotes, or exchange rates) are automatically reflected across the entire application without requiring manual user intervention, all developers and AI agents MUST adhere to this **6-Pipeline Cross-Functional Impact Checklist**:

```
+----------------------------------------------------------------------------------------------------+
|                                    THE 6 CORE DATA PIPELINES                                       |
+----------------------------------------------------------------------------------------------------+
| 1. Ingestion & Database Layer      | Supabase (holdings, transactions, dividends), Book1.xlsx,     |
|                                    | validation engine (/api/add-investment).                      |
+------------------------------------+---------------------------------------------------------------+
| 2. Real-Time Price & FX Engine     | priceEngine.js, liveQuoteCache, /api/fx-rate, ThemeAuthContext|
|                                    | (continuous 3s stream), live badge in TopNavbar.              |
+------------------------------------+---------------------------------------------------------------+
| 3. Asset Analysis & Dense Timeline | HoldingDetailModal.jsx, /api/holding/:id/detail, FIFO lot     |
|                                    | engine, Tracker Chart, Actual Chart, centered corporate bars. |
+------------------------------------+---------------------------------------------------------------+
| 4. Portfolio Summaries & KPIs      | /api/summary, /api/holdings, OverviewView hero cards, asset   |
|                                    | allocation, floating decimal precision (2-decimal float).     |
+------------------------------------+---------------------------------------------------------------+
| 5. Historical EOD & Calendar       | data/portfolio_eod_logs.json, scripts/rebuild_portfolio_eod.mjs|
|    Heatmap                         | /api/daily-pnl, CalendarView.jsx (19-year daily wealth logs).  |
+------------------------------------+---------------------------------------------------------------+
| 6. Visual Theme & UI Integrity     | High-contrast Light Mode cards/pills, Dark Mode luminescence, |
|                                    | DD-MM-YYYY dates, non-repetitive text, mobile responsiveness. |
+----------------------------------------------------------------------------------------------------+
```

### Mandatory Pre-Completion Audit Steps:
1. **Trace Downstream Consumers**: When modifying any calculation or transaction logic, identify which of the 6 pipelines consume that data.
2. **Autonomous Synchronization**: If database transactions, prices, or split/bonus rules are updated, **automatically execute `scripts/rebuild_portfolio_eod.mjs`** to keep the 6,900-day Calendar Heatmap logs in exact parity.
3. **Multi-Endpoint API Validation**: Test and compare single-holding endpoints (`/api/holding/:id/detail`), portfolio summaries (`/api/summary`), and historical logs (`/api/daily-pnl`) to ensure zero variance before finalizing.
4. **Theme Contrast Audit**: Test new UI elements in both Light Theme (`Clean Light`) and Dark Theme (`Obsidian Dark`) to prevent low-contrast or grayed-out artifacts.

---

## Mandatory Regression Testing & Quality Assurance Protocol

To ensure existing functionality is never broken when new features, refactors, or bug fixes are introduced, all developers and AI agents MUST adhere to the following mandatory regression testing protocol:

1. **Pre-Deployment Build Verification**:
   - Run `npm run build` before finalizing any changes to ensure zero JSX, syntax, type, or bundling errors exist across all components.

2. **Scope Impact & Component Dependency Analysis**:
   - Before modifying any shared component, context provider (e.g. `ThemeAuthContext`), or API service, inspect all consuming files to ensure imported functions, props, and hooks are fully declared and preserved.

3. **Core Workflow Regression Verification**:
   - **Search & Autocomplete**: Verify typing in `TopNavbar.jsx` search input returns filtered asset/liability results without throwing runtime errors or blanking out the screen.
   - **Navigation & Routing**: Test switching between all primary views (Overview, Calendar, Indian Equity, US Equity, Mutual Funds, NPS, Banks, EPF, Liabilities, Dividends, Reports, Database Studio, Excel Tools).
   - **Detail Modal & Drill-down**: Click portfolio table rows across all asset classes to verify `HoldingDetailModal.jsx` opens cleanly with full metrics, charts, and transaction ledgers.
   - **Theme & Currency Toggles**: Switch between currency (`INR` / `USD`) and themes (Obsidian, Midnight, Sunset, Clean Light, Warm Sand, Nordic Frost) to verify UI styling rendering.
   - **Live Price Engine & Sync**: Ensure background sync (`/api/refresh-prices`) and tick updates run cleanly without console exceptions.

4. **Fault Tolerance & Component Isolation**:
   - Key layout containers must be wrapped in React `<ErrorBoundary>` blocks so that any localized runtime exception is caught gracefully with a "Try Again" recovery UI, preventing full application blanking.

---

## Mandatory Developer Directives & Financial Integrity Rules

1. **Before Editing**: Always read `LADDER.md` to understand existing component dependencies, API contracts, and styling standards.
2. **Unified Real-Time Source of Truth & Zero Discrepancy**: All pages (Dashboard, Calendar, Portfolios, Detail Modals, Reports) must share the exact same underlying calculation engine (`computePortfolioValuation` and `liveQuoteCache`). Dynamic zero-script synchronization for current day metrics.
3. **After Editing**: Update both `README.md` and the **Change Log & Maintenance History** table in `LADDER.md` with the new version, date, concise description of changes, and author/agent.
4. **Delete Operations**: Always require explicit user confirmation displaying the asset or schedule name before deleting any record.
| **v3.2.0** | 2026-08-16 | Standardized decimal point alignment & Portal modal fixes: (1) Standardized `formatMoney` inside `ThemeAuthContext.jsx` to enforce exactly 2 decimal places for INR currency globally, updating Indian Stocks, Mutual Funds, US Stocks, and Liabilities views. (2) Restructured the popup modal in `CalendarView.jsx` to render via React Portals (`createPortal`) directly into `document.body` with `AnimatePresence` nested internally. This fixes the Framer Motion layout shift and centers the modal relative to the viewport. (3) Customized modal backdrop with a lighter semi-transparent shade (`bg-black/40` with 2px blur) to prevent darkening the layout in light themes. (4) Re-designed summary cards with a sleek left accent bar (`border-l-4`) using the high-contrast `glass-subcard` token. | Antigravity AI |
| **v3.3.0** | 2026-08-16 | Calendar UX 5-Point Refinements & Instrument Drill-Down Accordion: (1) Renamed `Housing Loan (SBI)` to `Housing Loan` (removed SBI suffix). (2) Formatted loan balance amount in standard white/black text color matching other asset categories instead of Red. (3) Built interactive `>` chevron accordion on category rows, expanding to reveal underlying instruments and their specific valuations/deltas. (4) Eliminated `...` truncation on daily calendar cards by reducing grid columns and tightening character spacing (`tracking-tighter`). (5) Formatted `Win Rate` stats card to enforce 2 decimal places (`37.50%`). | Antigravity AI |
| **v3.4.0** | 2026-08-16 | Reverted Dropdown Accordion & Polished Summary Section Spacing & Theme Colors: (1) Reverted modal category dropdown accordion per user request, restoring a clean uncluttered list of changed balances. (2) Tightened vertical spacing between daily cards grid divider and Summary header (`mt-3 pt-3 space-y-3`), removing large empty blank gaps. (3) Enlarged Summary date range font (`text-xs sm:text-sm font-extrabold text-slate-400 font-mono`) for better legibility. (4) Updated Summary metric boxes (`NET WORTH CHANGE`, `ASSETS GROWTH`, `LIABILITIES DELTA`) to use `.glass-subcard` styling, ensuring backgrounds adapt to active light/dark themes. | Antigravity AI |
| **v3.5.0** | 2026-08-16 | Global icon and label refinement: (1) Replaced Indian Equities icon with `CandlestickChart`. (2) Renamed all instances of "Equities" to "Equity" across the project. (3) Differentiated confusing icons: Asset Allocation uses `Donut`, Reports uses `BarChart3`, NPS uses `Shield`, and EPF uses `Wallet`. (4) Standardized master context file naming from `LADDER.MD` to `LADDER.md`. (5) Integrated a live, persistent Date and Time clock stamp into `TopNavbar.jsx` visible across all pages. | Antigravity AI |
| **v3.5.1** | 2026-08-16 | Follow-up UI corrections: (1) Changed EPF icon from `Wallet` to `Briefcase` for better relevance. (2) Changed all instances of "Liabilities" to "Liability" globally across components, page headers, and API display names. (3) Fixed remaining backend API labels for Indian Equity and US Equity in `server/index.js`. | Antigravity AI |
| **v3.5.2** | 2026-08-16 | Enhanced Net Worth chart in Dashboard (`OverviewView.jsx`) with extended calendar range options (`2Y`, `3Y`, `5Y`, `10Y`) to match the `CalendarView` feature parity. Adjusted `ALL` range to calculate from inception up to 20 years. | Antigravity AI |
| **v3.5.3** | 2026-08-16 | Standardized Reports icon to use `PieChart` in `Sidebar.jsx` and cleaned up `ReportsView.jsx` header text to just "Report" without sub-taglines. | OpenRouter (Delegated) |
| **v3.6.0** | 2026-08-16 | Upgraded `ReportsView.jsx` to support dynamic asset filtering (Combined Portfolio, Consolidated Equity, Fixed Income). Added a new Market Cap chart and dynamic Sector filtering. Implemented benchmark selector (Nifty 50, S&P 500, Custom) for the Portfolio Growth trajectory chart. Updated backend `/api/holdings` routes in `server/index.js` to accept and persist `market_cap` metadata natively. | Antigravity AI |
| **v3.7.0** | 2026-08-16 | Major architecture refactor for Reports and Database. Created `asset_metadata_seed.sql` to instantiate the `asset_metadata` table in Supabase, separating Sector and Capitalisation mappings (Mega, Large, Mid, Small, Micro) from holdings. Updated `ReportsView.jsx` with strict tabbed architecture separating Equity charts (Market Cap, Sectors) from Fixed Income/NPS. Expanded benchmarks to Nifty 50, Nifty Midcap 150, Nifty 250, S&P 500, NASDAQ. | Antigravity AI |
| **v3.8.1** | 2026-08-21 | Made dashboard performance asset class rows clickable to navigate to their respective portfolio pages. Colored Assets in Net Worth hero card green. | Antigravity AI |
| **v3.8.2** | 2026-08-21 | Rendered HoldingDetailModal using React Portals to prevent viewport layout shift and scrolling when clicking bottom items. Added a global "Back" button to TopNavbar for easier navigation back to the Dashboard. | Antigravity AI |
| **v3.8.3** | 2026-08-21 | Moved global Back button to the bottom of the screen as a floating action button. Made portfolio data rows entirely clickable to open the detail modal. Reverted logo API to Hunter as Clearbit API was inaccessible. | Antigravity AI |
| **v3.8.4** | 2026-08-21 | Upgraded logo resolution algorithm to cascade across `icon.horse` (high-res favicons), `Google Favicon API` (128px), and finally `logos.hunter.io` to ensure robust, crisp, and high-quality logo rendering for all portfolio views. | Antigravity AI |
| **v3.8.6** | 2026-08-21 | Enhanced historical pricing engine: Implemented dynamic fetch of exact USD/INR daily exchange rates via Yahoo Finance (INR=X) for perfectly accurate historic conversion of US stocks. Added dynamic symbol remapping fallback for renamed Indian tickers (e.g. TATAMOTORS -> TMPV.NS, SWANENERGY -> 503310.BO). Fixed Calendar Heatmap `/api/daily-pnl` route to read high-fidelity exact EOD `total_wealth` values instead of legacy approximations. | Antigravity AI |
| **v3.9.0** | 2026-08-21 | Replaced sparse chart timeline rendering with a new 'Dense Timeline Engine' in `server/index.js`. The previous charting logic throttled datapoints to a maximum of 300, and fell back to sparse transaction-dates-only when Yahoo Finance failed to fetch historical prices. The new engine programmatically iterates through every single calendar date from the first transaction to the present day, calculating and carrying forward the last known valuation to create a mathematically flawless, high-fidelity daily charting dataset. This resolves the issue of Recharts skipping weekends and drawing rigid, non-interactive straight lines across transaction gaps. | OpenRouter (Delegated) |
| **v4.0.0** | 2026-08-21 | Added 'Actual Chart' functionality to `HoldingDetailModal.jsx` and updated Dense Timeline Engine to output pure asset prices alongside invested/value metrics. The Holding Detail Modal now features a tab toggle between 'Tracker Chart' (Invested vs Market Value) and 'Actual Chart' (pure historical price of the stock/scheme). The Actual Chart includes scatter-plot event overlays for BUY (green), SELL (red), DIVIDEND (amber), and BONUS (cyan) dates. Both charts now share a unified date range filter (`1M`, `3M`, `6M`, `1Y`, `ALL`, and `CUSTOM` picker) with responsive dynamic y-axis scaling. Built a custom tooltip for the Actual Chart to cleanly display the exact transaction quantities and amounts on event dates. | Antigravity AI |
| **v4.0.1** | 2026-08-21 | Refined Actual Chart tooltips: Implemented a 'magnetic' tooltip logic that snaps to nearby events within a 9-day window to solve high-density X-axis hover sensitivity. Switched active tracker dot to a distinct white/grey style to prevent confusion with BUY events. Fixed duplicate dividend displaying on tooltips caused by redundant table processing in `server/index.js`. Standardized global date formats to `DD-MM-YYYY` across all chart ticks, tooltips, and transaction ledgers. | Antigravity AI |
| **v4.0.2** | 2026-08-26 | Removed serial numbers (`#`) column from transaction ledgers in `HoldingDetailModal.jsx` across all portfolio asset types to streamline UI. Adjusted table layout and empty state colspan. | Gemini 3.1 Pro (Low) |
| **v4.0.3** | 2026-08-26 | Refactored investment metrics: `Total Invested` now includes BUY charges and excludes BONUS logic. `Total Redeemed` now subtracts SELL charges. Added `Current Invested` and `Total Charges` (with Buy/Sell breakdown) to `HoldingDetailModal`. Dividends are now accurately added to `Realized P&L` and registered as positive cashflows in the XIRR calculation array. Fixed a residual cost basis bug in the dense timeline that occurred when a holding was completely sold. | Gemini 3.1 Pro (Low) |
| **v4.0.4** | 2026-08-26 | Complete redesign of Performance Summary metrics in HoldingDetailModal. Replaced flat identical card grid with a two-tier visual hierarchy: (1) a featured Total P&L hero card with dynamic theme-aware gradient background (emerald/rose dark-glow in dark mode, subtle emerald/rose tinted surfaces in light themes), radial glow, and inline Unrealized/Realized breakdown separated by a vertical divider; (2) compact metric tiles with colored left accent strips (blue/indigo/cyan/white/amber/orange/green-red) for Invested, Redeemed, Cost Basis, Market Value, Dividends, Charges, and XIRR. | Gemini 3.6 Flash |
| **v4.0.5** | 2026-08-27 | Implemented comprehensive clutter-free UI updates across all portfolio views and HoldingDetailModal: (1) Cleaned up page banners across Indian Stocks, US Stocks, Mutual Funds, and NPS views, removing redundant sorting/mechanism labels and adding Invested Value stats alongside Active Value and Gain; (2) Upgraded Price columns across all portfolio tables (Indian Equity, US Equity, Mutual Funds, NPS, universal HoldingsTable) to display live prices with today's price movement (green/red change amount and percentage); (3) Upgraded HoldingDetailModal with a prominent live price and day change badge in the header, plus an institutional Market Snapshot bar displaying Open, Prev Close, Day High, Day Low, and an animated interactive 52-Week Range indicator bar with full light/dark theme adaptation. | Antigravity AI |
| **v4.0.6** | 2026-08-27 | Visual distinction and light mode aesthetic upgrade: (1) Added prominent `ACTIVE` and `EXITED`/`REDEEMED` status pill badges to all portfolio table rows across Indian Stocks, US Stocks, Mutual Funds, NPS, and HoldingsTable; (2) Styled closed/exited rows with desaturated logos, muted typography, and clean dashes for inactive investment values while active rows pop with vibrant high-contrast accents; (3) Added live price quotes and today's percentage changes to TopNavbar search autocomplete dropdown; (4) Enhanced light theme styling across table headers, input surfaces, and filter tabs for a luxury fintech visual experience. | Antigravity AI |
| **v4.0.7** | 2026-08-27 | Real-time live quote auto-engine and banner alignment upgrade: (1) Aligned banner stat boxes across US Stocks, Indian Stocks, Mutual Funds, and NPS views with matching top, middle, and bottom baselines across both Invested and Active Value columns; (2) Upgraded `priceEngine.js` to continuously pull real-time `regularMarketPrice` quotes during active US & Indian market hours; (3) Added automated 20-second background polling and window focus/visibility listeners in `App.jsx` and recurring 30-second live background scheduler in `server/index.js`, so all total portfolio values, gains, losses, and XIRR update dynamically with live market movements. | Antigravity AI |
| **v4.0.8** | 2026-08-27 | Parallelized high-speed live quote ticker engine: (1) Diagnosed price latency caused by sequential looping over 350+ holdings blocking the event loop; (2) Implemented `refreshActiveHoldingsPrices()` with parallel `Promise.all` fetching for active US stocks and concurrent batches for active Indian stocks (< 1-2s total execution time); (3) Introduced in-memory `liveQuoteCache` delivering real-time quotes to `/api/holdings` and `/api/summary` with zero latency; (4) Upgraded `App.jsx` polling interval to 5 seconds and server ticker loop to 10 seconds for real-time market ticking. | Antigravity AI |
| **v4.1.0** | 2026-08-28 | Removed 'All Data' filter tab across Indian Stocks, US Stocks, Mutual Funds, and NPS views, simplifying status navigation to 'Active Positions' and 'Fully Redeemed'. Overhauled Fully Redeemed row aesthetics with 100% opacity, crisp typography, full-color logos, and EXITED/REDEEMED status badges. Implemented dynamic table headers and closed position metrics displaying Shares/Units Sold, Avg Buy Price/NAV, Avg Sell Price/NAV, Invested Cost Basis, Total Redeemed Proceeds, and Realized P&L. | Antigravity AI |
| **v4.1.1** | 2026-08-28 | Enhanced quote timestamp tracking: (1) Added quoteDate/asOfDate extraction to `priceEngine.js` (from Yahoo Finance `regularMarketTime`, AMFI date, and Protean NPS `navDate`); (2) Displayed prominent `As of [Date]` timestamp badges in `HoldingDetailModal.jsx` header and across portfolio view page banners (Indian Equity, US Equity, Mutual Funds, NPS). | Antigravity AI |
| **v4.1.2** | 2026-08-28 | Accelerated live quote ticker engine: (1) Reduced backend `refreshActiveHoldingsPrices()` interval from 10s down to 3s in `server/index.js`; (2) Reduced frontend background polling in `App.jsx` from 5s down to 2s for near-instant tick updates during live market hours. | Antigravity AI |
| **v4.1.3** | 2026-08-28 | Resolved database connection error: Relaunched backend Express API server process (`node server/index.js`) on port 5000 connected to Supabase Cloud PostgreSQL database. Verified `/api/summary`, `/api/holdings` (397 holdings), and `/api/liabilities` returning HTTP 200 OK. | Antigravity AI |
| **v4.1.4** | 2026-08-28 | Fixed quote date resolution accuracy: (1) Added `formatCleanQuoteDate` parser in `priceEngine.js`; (2) Ensured Mutual Funds (AMFI) and NPS (Protean) return the exact published NAV date (e.g. 27 Aug 2026) rather than falling back to current system date; (3) Verified Axis Small Cap Fund returns 27 Aug 2026 while NSE stocks return 28 Aug 2026. | Antigravity AI |
| **v4.1.5** | 2026-08-28 | Universal quote date validation across all asset portfolios: (1) Added `formatQuoteBadgeDate` standardizer in `dateFormatter.js`; (2) Verified that Indian Stocks, US Stocks, Mutual Funds, NPS schemes, and EOD assets accurately extract and display their true underlying exchange/feed quote dates (e.g. 27 Aug 2026 for AMFI MFs vs 28 Aug 2026 for NSE/BSE stocks). | Antigravity AI |
| **v4.1.6** | 2026-08-28 | Fixed modal header alignment & exchange timezone accuracy: (1) Reordered `HoldingDetailModal.jsx` header so price, day change, and quote date are strictly anchored at the right before the close button across ALL portfolio categories; (2) Shifted US Stock FX badge and USD/INR toggle to the left of the price block; (3) Added exchange timezone awareness (`America/New_York` for US stocks) ensuring US quotes display true US trading session date (27 Aug 2026); (4) Fixed NPS fallback to extract official CRA published NAV date (27 Aug 2026). | Antigravity AI |
| **v4.1.7** | 2026-08-28 | Search bar runtime bug fix & Mandatory Regression Testing Protocol: (1) Fixed unhandled ReferenceError in `TopNavbar.jsx` by adding missing `formatMoney` destructuring from `useThemeAuth()`; (2) Built `ErrorBoundary.jsx` and wrapped top navigation header and primary view renderer to prevent full-screen blanking on runtime exceptions; (3) Documented mandatory Regression Testing and QA Protocol in `LADDER.md`. | Antigravity AI |
| **v4.2.0** | 2026-08-28 | Corporate Actions & Multi-Layer Validation Engine: (1) Diagnosed and fixed Stock Split bug in `load_all_indian_stocks.mjs` and `server/index.js` where stock splits (e.g. 10:1 split in WEBELSOLAR) were previously ignored, causing artificial over-sell deficits and bogus realized losses; (2) Implemented retroactive FIFO buy lot scaling, split multiplier calculations, and split-adjusted cost basis tracking; (3) Added same-day transaction priority sorting (BUY/BONUS -> SPLIT -> SELL -> SELL ALL -> DIVIDEND) eliminating 28 same-day selling race conditions; (4) Enhanced `HoldingDetailModal.jsx` transaction ledger to render dedicated indigo SPLIT badges (+X shares, descriptive ratio notes, and dash for price/amount) and cyan BONUS badges (+X shares, 0 cost basis); (5) Implemented multi-tier validation engine in backend `/api/add-investment` and frontend (`AddInvestmentView.jsx`, `AddAssetModal.jsx`) with datatype checks, null assertions, live over-sell guardrails, and real-time split/bonus position projection previews for active holdings. | Antigravity AI |
| **v4.2.1** | 2026-08-28 | Real-Time Live Ticker & USD/INR Forex Stream Upgrade: (1) Diagnosed and resolved live ticker freeze on screen: `/api/holdings` was previously omitting `current_price: currentPriceNum` in returned holding records, causing the frontend to retain stale database prices until navigation; (2) Integrated real-time live ticking USD/INR Forex feed (`INR=X`) into `priceEngine.js` `fetchFxRate()`, replacing static 24-hour daily snapshots; (3) Added dedicated real-time pulsing USD/INR Forex pill badge to `TopNavbar.jsx`; (4) Synchronized `/api/summary` portfolio valuation calculation with in-memory `liveQuoteCache` for instantaneous net worth, asset totals, and P&L tick updates; (5) Replaced overlapping `setInterval` with a self-scheduling non-overlapping ticker loop and optimized active Indian stock batching to complete in < 1.5 seconds. | Antigravity AI |
| **v4.2.2** | 2026-08-28 | Exact Decimal Precision & Live Modal Ticker Integration: (1) Fixed `.00` precision truncation across all Net Worth hero metrics (Net Worth, Assets, Liabilities, Invested, Gain, Day P&L) in `/api/summary` by replacing `Math.round()` with exact floating-point 2-decimal numbers (`Number(val.toFixed(2))`), restoring accurate paise/cent figures; (2) Integrated 2-second live polling into `HoldingDetailModal.jsx` so open detail views dynamically stream price ticks, day changes, and valuations in real-time; (3) Updated `/api/holding/:id/detail` to derive current quotes and valuations directly from in-memory `liveQuoteCache`. | Antigravity AI |
| **v4.2.3** | 2026-08-28 | Corporate Action Separator Bars & Split-Adjusted Chart Trajectory Fix: (1) Fixed historical chart distortion for stocks with stock splits and bonus issues (e.g. WEBELSOLAR, GAIL, FCL, TEMBO): historical buy transactions for open stocks are now stored and calculated in split-adjusted quantity and unit price from their acquisition date, preserving exact total invested amounts while aligning with split-adjusted historical market quotes; (2) Replaced artificial +shares ledger rows with clean, full-width Corporate Action event separator bars in `HoldingDetailModal.jsx` displaying date, corporate action type (Stock Split / Bonus Issue), and ratio; (3) Eliminated verbose clutter and notes from corporate action displays; (4) Upgraded dense timeline construction in `/api/holding/:id/detail` to track smooth, unbroken market value trajectories without artificial 10x drops or spikes. | Antigravity AI |
| **v4.2.4** | 2026-08-28 | Light/Dark Theme High-Contrast Polish & Global Real-Time FX Polling: (1) Enhanced Corporate Action event bars in `HoldingDetailModal.jsx` with centered alignment, crisp high-contrast theme styling (vibrant indigo/cyan backgrounds and dark text in light mode, luminous glow in dark mode), and bonus shares received text (e.g. `+100 Shares Received as Bonus`) replacing ratio text; (2) Fixed FX pill and currency toggle buttons in modal header to display high-contrast styling in light theme, eliminating grayed-out look; (3) Added dedicated `/api/fx-rate` backend endpoint and integrated continuous 3-second live Forex polling in `ThemeAuthContext.jsx` so live USD/INR exchange rate updates dynamically across all dashboard headers, modals, and conversion widgets. | Antigravity AI |
| **v4.2.5** | 2026-08-28 | Natural Date Alignment & Non-Repetitive Corporate Action Layout: (1) Restructured Corporate Action rows in `HoldingDetailModal.jsx` so the transaction Date is left-aligned in its natural first-column position matching the table header, while the corporate action badge and concise detail are centered across the remaining columns; (2) Removed repetitive wording so "Stock Split" and "Bonus" are not duplicated (e.g. `[Stock Split] Ratio 1:10` and `[Bonus Issue] +100 Shares Credited`). | Antigravity AI |
| **v4.2.6** | 2026-08-28 | Live Chart Tracker Extension & Real-Time Endpoint Anchoring: (1) Diagnosed why chart timelines appeared flat/stopped after August 21: offline historical daily price files were snapshot up to 21 Aug, causing the dense timeline to carry forward flat prices for August 22-28; (2) Updated `server/index.js` dense timeline engine to dynamically bind today's date directly to live ticking quotes from `liveQuoteCache` / Yahoo Finance, restoring active chart curve movement right up to the current second; (3) Appended live balance points to EOD asset timelines (Bank, EPF, Loans). | Antigravity AI |
| **v4.2.7** | 2026-08-28 | Calendar Heatmap & Daily PnL Synchronization: (1) Re-executed `scripts/rebuild_portfolio_eod.mjs` across all 6,900 daily EOD records (2007-2026), incorporating split-adjusted and bonus-adjusted stock positions from Supabase; (2) Verified that the Calendar Heatmap (`CalendarView.jsx` and `/api/daily-pnl`) displays smooth daily wealth and PnL trajectories on corporate action dates (e.g. Nov 2025 WEBELSOLAR split and Sep 2022 GAIL bonus) with zero artificial valuation spikes. | Antigravity AI |
| **v4.2.8** | 2026-08-28 | Actual & Tracker Chart High-Contrast Palette Upgrade: (1) Replaced the emerald green price line with an Electric Sky Blue palette (`#38bdf8` in Dark Theme, `#0284c7` in Light Theme) across both the Actual Chart (Asset Price curve) and Tracker Chart (Market Value area/stroke) in `HoldingDetailModal.jsx`; (2) Eliminates visual collision with emerald green BUY event dots (`#10b981`), allowing BUY signals, SELL signals (`#f43f5e`), DIVIDEND dots (`#f59e0b`), and BONUS badges to stand out with distinct clarity against the price curve. | Antigravity AI |
| **v4.2.9** | 2026-08-28 | Corporate Action Royal Purple & Fuchsia Pink Palette Upgrade: (1) Replaced cyan/indigo styles for corporate actions with Royal Purple (`#a855f7`) for Stock Splits and Vibrant Fuchsia Pink (`#ec4899`) for Bonus Issues across scatter event dots, tooltip badges, and transaction ledger rows in `HoldingDetailModal.jsx`; (2) Creates full visual separation between the Sky Blue price curve, Green BUY dots, Red SELL dots, Amber Dividend dots, Purple Split markers, and Pink Bonus markers in both Light and Dark themes. | Antigravity AI |
| **v4.3.0** | 2026-08-28 | Bonus Issue Warm Brown / Bronze Palette Upgrade: (1) Replaced pink styling for Bonus Issues with Warm Saddle Brown / Bronze (`#b45309`) across scatter event dots, tooltip badges, and transaction ledger rows in `HoldingDetailModal.jsx`; (2) Completely eliminates any proximity confusion between pink and bright red SELL signals (`#f43f5e`), giving Bonus Issues an earthy, distinct visual signature. | Antigravity AI |
| **v4.3.1** | 2026-08-28 | US Stocks Corporate Action & Dividend Ledger Parity: (1) Diagnosed and fixed omission in `load_all_us_stocks.mjs` where stock splits from `Book2.xlsx` (e.g. Arista Networks ANET 4:1 Split on 2024-12-04) were omitted during US equities ingestion; (2) Implemented retroactive FIFO buy lot scaling, split multiplier calculations, and split-adjusted cost basis tracking for US stocks matching Indian stocks parity; (3) Added same-day event priority sorting (BUY -> SPLIT -> SELL -> DIVIDEND) in US stocks ingestion; (4) Updated `/api/holding/:holdingId/detail` in `server/index.js` to interleave and format dividends (`DIVIDEND` type) chronologically into the Transaction Ledger with amber badges and dual USD/INR currency amounts; (5) Re-ingested all 12 US stocks and re-executed `scripts/rebuild_portfolio_eod.mjs` across all 6,900 daily records. | Antigravity AI |
| **v4.3.2** | 2026-08-28 | Combined Portfolio Performance Banners: Restored and upgraded the display of Combined Cost Basis and Overall P&L in the portfolio category views (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, `NpsView`). The new design introduces a distinct glowing combination block that aggregates Active Cost Basis, Redeemed Cost Basis, Unrealized P&L, and Realized P&L without cluttering the UI, displaying both Absolute % and XIRR % returns dynamically. | Antigravity AI |
| **v4.3.3** | 2026-08-28 | Clean & Aligned Category Banner & Tab Bar Refactor: (1) Streamlined the top banner across all 4 category views (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, `NpsView`) to feature a single, high-contrast Combined Performance card (`Combined Cost` and `Overall Return` with Absolute & XIRR percentages), eliminating redundant subtexts, extra cards, and layout clutter; (2) Relocated contextual Active / Redeemed metrics into the filter tab bar row as an inline summary badge (`Invested`, `Current Value`, `P&L` for Active; `Cost`, `Redeemed`, `Realized P&L` for Redeemed), ensuring perfect grid alignment with search boxes and filter tabs. | Antigravity AI |
| **v4.3.4** | 2026-08-28 | Ultra-Minimal Integrated Micro-Bar Architecture (Concept 3 Rollout): (1) Redesigned the header across `IndianStocksView`, `UsStocksView`, `MutualFundsView`, and `NpsView` to achieve maximum vertical breathing room: top banner contains only the live asset title badge and primary action button; (2) Implemented an ultra-sleek integrated micro-bar in the table controls row that seamlessly anchors the segmented tab switch (`Active` vs `Redeemed`) side-by-side with a high-density, clutter-free financial stat strip showing `Combined Cost`, `Net Return (Abs % | XIRR %)`, and live contextual tab metrics (`Invested → Value` or `Cost → Redeemed`); (3) Responsive layout dynamically flexes on all screen sizes with zero horizontal or vertical overflow. | Antigravity AI |
| **v4.3.5** | 2026-08-28 | Unified High-End Micro-Bar Styling (Pixel-Exact Preview Parity): (1) Polished the integrated control & performance bar in `IndianStocksView`, `UsStocksView`, `MutualFundsView`, and `NpsView` into a single cohesive glassmorphic container (`bg-slate-900/80 rounded-2xl border border-slate-800/80`); (2) Removed redundant duplicate contextual text, unifying segmented tabs (`Active` vs `Redeemed`), an elegant vertical divider, high-contrast financial stats (`Combined: Cost • Return [ +Abs% | XIRR% ]`), and integrated search box in one harmonious horizontal bar matching the visual preview mockup. | Antigravity AI |
| **v4.3.6** | 2026-08-28 | Dual-Compartment Split Header Card (Option C Implementation): (1) Implemented a single, high-contrast dual-compartment glass card positioned directly to the left of the primary action button across all 4 category views (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, and `NpsView`); (2) Left compartment displays `COMBINED` badge, complete cost basis formatted to 2 decimal places, net return, `Abs: +X.XX%`, and `XIRR: +X.XX%`; (3) Right compartment dynamically switches between `ACTIVE` (invested, current market value, net gain, and `Abs: +X.XX%`) and `REDEEM` (historical cost basis, realized redeemed proceeds, net realized P&L, and `Abs: +X.XX%`) when tabs are toggled; (4) Fully aligned micro-typography with zero clutter across light and dark themes. | Antigravity AI |
| **v4.3.7** | 2026-08-28 | Split Card Centered Headers & Color-Coded Precision Polish: (1) Updated both `COMBINED` and `ACTIVE` / `REDEEM` headers to be centered badges atop each compartment across `IndianStocksView`, `UsStocksView`, `MutualFundsView`, and `NpsView`; (2) Standardized fields to clean stacked rows: `Cost : <value>` and `Return : <value>` with exact 2-decimal precision; (3) Added independent color-coding (emerald for profit, rose for loss) to both `Abs: +X.XX%` and `XIRR: +X.XX%` on both sides; (4) Verified layout alignment and successful build. | Antigravity AI |
| **v4.3.8** | 2026-08-29 | Robust Multi-Tier XIRR Engine & US Equity Currency Toggle Fix: (1) Fixed missing `DollarSign` import and wired `toggleCurrency` in `UsStocksView.jsx`; (2) Diagnosed and eliminated mathematical artifacts in XIRR caused by pooling unsynchronized closed holding cashflows lacking exit SELL transactions; (3) Upgraded `/api/summary` in `server/index.js` to compute holding-level weighted XIRRs, exposing distinct `activeXirrPct`, `closedXirrPct`, and `xirrPct` (Combined); (4) Connected individual category views to display true Active XIRR (+18.93% for Indian Stocks) and Combined XIRR (+7.52%), eliminating negative rate anomalies on profitable portfolios. | Antigravity AI |
| **v4.3.9** | 2026-08-29 | Uniform Header Action Alignment & Global roiPct Resolution: (1) Fixed `roiPct is not defined` crash in `NpsView.jsx` and `MutualFundsView.jsx` by explicitly declaring `roiPct`; (2) Relocated `USD ($) / INR (₹)` currency toggle in `UsStocksView.jsx` from the top header to the table controls bar beside the search input; (3) Aligned top-right split returns card and primary action button (`+ Add Asset`) across all four asset categories (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, `NpsView`) with pixel-perfect consistency; (4) Validated clean build with 0 warnings/errors. | Antigravity AI |
| **v4.4.0** | 2026-08-29 | Price Column Typography & Alignment Precision Upgrade: (1) Fixed baseline misalignment and text wrapping in Price column where day changes and Unicode arrow symbols broke onto multiple lines; (2) Replaced unicode arrow characters with crisp Lucide `ArrowUp` and `ArrowDown` icons with `stroke-[3]` and `shrink-0` locked to the exact text baseline; (3) Applied `whitespace-nowrap inline-flex` across `UsStocksView.jsx` and `IndianStocksView.jsx` ensuring numbers, currency symbols, and percentages maintain horizontal alignment. | Antigravity AI |
| **v4.4.1** | 2026-08-29 | Holding Detail Modal KPI Terminology Clarification: (1) Replaced confusing and overlapping labels in `HoldingDetailModal.jsx` (`Invested`, `Redeemed`, `Cost Basis`, `Market Value`) with clear, unambiguous, intuitive terms: `Total Bought` (all historical purchase capital), `Total Sold` (all historical sale proceeds), `Current Cost` (cost basis of active shares currently held), and `Current Value` (live market valuation of active shares); (2) Verified clean build with 0 errors. | Antigravity AI |
| **v4.4.2** | 2026-08-29 | Calendar Spreadsheet Table View & Interactive View Switcher: (1) Added seamless view switcher in `CalendarView.jsx` allowing users to toggle between Heatmap Grid View and a Spreadsheet Table View; (2) Implemented animated, sortable table displaying Date (with weekday), Net Worth, Daily P&L, % Change, Total Assets, Total Liabilities, and Changed Categories activity indicators; (3) Enabled column sorting (Date, Net Worth, Daily P&L, % Change, Assets, Liabilities) defaulting to descending chronological order; (4) Maintained full drill-down parity: clicking any table row opens the exact daily valuation and balance change inspector modal; (5) Verified clean build with 0 errors. | Antigravity AI |
| **v4.4.3** | 2026-08-29 | Continuous Daily EOD Sync & Full portfolio.xlsx Multi-Column Spreadsheet View: (1) Diagnosed calendar cut-off on 21 Aug: historical price tables ended on Aug 21, and `rebuild_portfolio_eod.mjs` omitted bank/EPF/loan carry-forwards, dropping calendar net worth to 33L; (2) Upgraded `rebuild_portfolio_eod.mjs` to reconstruct all 6,912 daily records from inception (2007) right up to the current date (2026-08-29), carrying forward bank savings, EPF, and liabilities, aligning calendar net worth (₹1.884 Cr) with the dashboard; (3) Redesigned the Calendar Table view into an exact clone of `portfolio.xlsx` with frozen sticky Date column, category header groupings (Bank Savings, Investment Portfolios, Total Assets, Liabilities & Debt, Net Wealth & Performance), and all individual columns (`HDFC`, `IndusInd`, `IDFC`, `RBL`, `SBI`, `Savings`, `Mutual Funds`, `Indian Stocks`, `US Stocks`, `NPS`, `EPF`, `Total Assets`, `Loan`, `Credits`, `Debt`, `Net Wealth`, `Daily P&L`, `% Change`); (4) Restored true previous-day lookups in `/api/daily-pnl` so clicking any date reveals the exact asset and liability balance movements recorded on that day. | Antigravity AI |
| **v4.4.4** | 2026-08-29 | Automated Real-Time Daily Pricing Ingestion & Dynamic Multi-Asset Valuation Sync: (1) Fixed root cause of stagnant prices after 21 Aug by building and executing `scripts/sync_daily_prices.mjs` which dynamically ingests live daily market quotes for 86 Indian stocks (Yahoo Finance .NS/.BO), 11 US stocks, 16 Mutual Funds (MFAPI daily NAV), 3 NPS schemes (Protean/NPSNAV daily NAV), and daily USD/INR FX rates up to 29 Aug 2026; (2) Rebuilt EOD logs yielding true daily changing net worth (e.g. 25 Aug: ₹1.844 Cr [-₹1.13L], 26 Aug: ₹1.854 Cr [+₹1.00L], 27 Aug: ₹1.858 Cr [+₹34.3k], 28 Aug: ₹1.860 Cr [+₹21.7k], 29 Aug: ₹1.860 Cr [-₹1.7k]), perfectly matching the ₹1.86 Cr dashboard value; (3) Restarted backend server process so `/api/daily-pnl` serves all 18 portfolio columns without zeros; (4) Enhanced date click inspector with dual-tab view ('Changes' vs 'All Balances') to review both daily market movements and complete portfolio snapshot. | Antigravity AI |
| **v4.4.5** | 2026-08-29 | Weekend Carry-Forward Alignment & Unified Assets Table Header Refactor: (1) Corrected US stock & market asset valuation on weekends: aligned US market closing session (Friday night / Saturday early AM IST) so that Saturday and Sunday calendar entries properly carry forward Friday's finalized valuation without phantom weekend FX rate noise (29 Aug now cleanly reflects ₹1.860 Cr with ₹0.00 daily change); (2) Restructured the Spreadsheet Table Header in `CalendarView.jsx` to unify `Total Assets` inside a single top-level `ASSETS` header (spanning all 12 asset columns: 6 bank accounts + 5 investment portfolios + Total Assets), creating perfect symmetry with `LIABILITIES & DEBT` (spanning Loan + Credits + Debt); (3) Verified clean production build with 0 errors. | Antigravity AI |
| **v4.4.6** | 2026-08-29 | Supabase Cloud Database Historical EOD Migration & Complete Excel Decoupling: (1) Upgraded Supabase `pnl_history` table schema with all 18 granular asset/liability breakdown columns and unique date constraint; (2) Migrated all 6,912 daily historical records (2007 through 2026-08-29) from the spreadsheet directly into Supabase PostgreSQL Cloud; (3) Reconfigured EOD rebuild engine (`rebuild_portfolio_eod.mjs`) to load base records and persist daily updates directly to Supabase `pnl_history`, severing runtime dependency on `portfolio.xlsx` so the spreadsheet can be deleted cleanly without data loss. | Antigravity AI |
| **v4.4.7** | 2026-08-29 | Daily EOD Pricing Reconciliation & Mandatory Universal Page Synchronization Rule: (1) Updated `data/historical_prices.json` with latest settled Friday closing prices (2026-08-28) across active holdings; (2) Rebuilt all 6,912 historical EOD logs via `scripts/rebuild_portfolio_eod.mjs` and synchronized with Supabase `pnl_history` (29 Aug 2026 EOD net worth now precisely reflects ₹18,663,893.69, fully aligned with Dashboard Net Worth); (3) Enforced mandatory Cross-Functional Data Integrity rule across `LADDER.md` and `.agents/AGENTS.md` requiring all updates in any portfolio view or database table to immediately sync with all dependent views (Calendar, Dashboard, Holding Detail Modals, Category Banners, Reports). | Antigravity AI |
| **v4.4.8** | 2026-08-29 | Calendar Table View Action Column Removal: Removed redundant 'Action / View' column and header from the Calendar spreadsheet table in `CalendarView.jsx`. Entire row remains clickable for opening day valuation breakdown modal. | Antigravity AI |
| **v4.4.9** | 2026-08-29 | Non-Trading Day & Weekend Precision Alignment: (1) Fixed floating-point precision summation discrepancy between 28 Aug and 29 Aug in `scripts/rebuild_portfolio_eod.mjs`; (2) Weekend non-trading days now carry forward Friday's exact valuation, total assets, liabilities, and net worth with exactly `₹0.00` daily change (0.00%); (3) Synchronized with Supabase `pnl_history`; (4) Production build verified with 0 errors. | Antigravity AI |
| **v4.5.0** | 2026-08-29 | Zero-Variance Mathematical Parity Across Dashboard & Calendar: (1) Standardized per-holding 2-decimal rounded accumulation in `scripts/rebuild_portfolio_eod.mjs` to match `/api/holdings` and `/api/summary` exactly; (2) Rebuilt all 6,912 EOD logs; (3) Verified exact 1-to-1 zero-paise match (`₹18,663,893.70` Net Worth, `₹23,162,513.86` Total Assets, `₹44,98,620.16` Total Liabilities) across Dashboard, Calendar, and Supabase. | Antigravity AI |
| **v4.5.1** | 2026-08-29 | Centralized Shared Single-Source-of-Truth Valuation Engine: (1) Created `server/services/portfolioCalculator.js` (`computeHoldingValueINR` and `computePortfolioValuation`) as the single mathematical authority; (2) Refactored `server/index.js` (`/api/summary`, `/api/holdings`) and `scripts/rebuild_portfolio_eod.mjs` to consume the same shared module; (3) Zero mathematical divergence across all endpoints and views; (4) Verified build with 0 errors. | Antigravity AI |
| **v4.5.2** | 2026-08-29 | Dashboard Net Worth Chart True EOD Integration & Redundant FX Badge Cleanup: (1) Removed redundant `$1 = ₹95.38` badge from the Net Worth hero card in `OverviewView.jsx`; (2) Replaced the synthetic noise/math formula in the Net Worth History chart with true historical daily EOD logs fetched from `/api/daily-pnl` (e.g. 2007-2026 real daily records), ensuring the chart displays exact historical portfolio valuations across all range presets (`1M`, `3M`, `6M`, `1Y`, `2Y`, `3Y`, `5Y`, `10Y`, `ALL`); (3) Production build verified with 0 errors. | Antigravity AI |
| **v4.5.3** | 2026-08-29 | Dark Theme Native Date Picker Visibility Fix: Applied `[color-scheme:dark]` CSS property to all `<input type="date">` elements across `OverviewView.jsx`, `CalendarView.jsx`, and `AddInvestmentView.jsx` to ensure native browser calendar icons are inverted and clearly visible in dark mode. | Antigravity AI |
| **v4.5.4** | 2026-08-29 | Dynamic Positive/Negative Coloring for Net Worth Chart: Updated `OverviewView.jsx` to dynamically compare the start and end values of the selected timeframe. The area chart's stroke, fill gradient, and tooltip text now turn Red (`#EF4444`) if the final portfolio value is lower than the starting value of the range, and Emerald (`#10B981`) if it is higher. | Antigravity AI |
| **v4.5.5** | 2026-08-29 | Removed Hardcoded Daily PnL Fallbacks: Fixed a bug in `server/index.js` (`/api/summary`) where the daily PnL percentage was hardcoded to `0.82%` and the absolute change was falling back to an incorrect calculation (`totalGainINR * 0.008`) if the database fetch returned empty. The endpoint now correctly defaults to local `portfolio_eod_logs.json` as a fallback and accurately reports `0.00` change for non-trading weekend days. | Antigravity AI |
| **v4.5.6** | 2026-08-29 | Reinstated 2-Decimal Precision on Category Metrics: Fixed an issue in `server/index.js` where the `/api/summary` endpoint was using `Math.round()` on category `investedINR`, `currentINR`, `realizedINR`, and `unrealizedINR`. Replaced with strict `.toFixed(2)` float precision to ensure the dashboard's Performance table correctly renders fractional paise instead of defaulting to `.00`. | Antigravity AI |
| **v4.5.7** | 2026-08-29 | Pre-May 12 2023 Indian Equity Charges Population & Parity Sync: (1) Parsed `charges.xlsx` and populated pre-2023-05-12 transaction charges across 365 Indian Equity transactions in Supabase; (2) Handled multiple transactions on the same date by allocating total charges to the transaction with the highest trade value (`quantity * price`); (3) Applied confirmed symbol aliases (`HDFC` -> `HDFCBANK`, `MAHINDCIE` -> `CIEINDIA`, `HBLPOWER` -> `HBLENGINE`, `ZOMATO` -> `ETERNAL`, `GET&D` -> `GVT&D`, `SKIPPERPP` -> `SKIPPER`); (4) Recalculated `total_charges` across 105 affected holdings and re-synchronized all 6,912 daily EOD logs via `scripts/rebuild_portfolio_eod.mjs` and Supabase `pnl_history`; (5) Verified production build. | Gemini 3.7 Flash |
| **v4.5.8** | 2026-08-29 | Post-May 12 2023 Missing Indian Equity Charges Full Population: (1) Parsed remaining 735 records from `charges.xlsx` and populated charges for 615 previously unpopulated post-May 12 2023 Indian Equity transactions in Supabase; (2) Preserved already populated charges (24 transactions); (3) Re-aggregated and updated `total_charges` across 147 affected holdings; (4) Rebuilt all 6,912 daily EOD logs via `scripts/rebuild_portfolio_eod.mjs` and synced with Supabase `pnl_history`; (5) Verified production build. | Gemini 3.7 Flash |
| **v4.5.9** | 2026-08-29 | Dashboard Net Worth Chart DD-MM-YYYY Hover Date & 0-Baseline Reference Line: (1) Enhanced Net Worth area chart tooltip in `OverviewView.jsx` to render the complete `DD-MM-YYYY` calendar date on hover (e.g. `24-06-2026`); (2) Added a subtle dashed `ReferenceLine` at `y=0` when the selected timeframe's Y-domain spans across or below 0; (3) Added negative sign formatting to Y-axis tick values and tooltips; (4) Production build verified with 0 errors. | Gemini 3.7 Flash |
| **v4.6.0** | 2026-08-29 | Net Worth Chart Clean 0 Y-Axis Tick: (1) Removed the text label (`₹0 Baseline`) from the reference line in `OverviewView.jsx`; (2) Explicitly injected `0` into the Y-axis `ticks` array when the domain spans $\le 0$ and $\ge 0$, rendering a clean `0` tick directly on the vertical axis; (3) Verified production build. | Gemini 3.7 Flash |
| **v4.6.1** | 2026-08-29 | Calendar Multi-Granularity Engine (Daily / Monthly / Yearly): (1) Added a segmented Granularity selector `[ Daily | Monthly | Yearly ]` to `CalendarView.jsx`; (2) Implemented robust chronological aggregation mapping across all 18 portfolio asset/liability columns with exact period-end snapshots and true cumulative period P&L / % return calculations; (3) Enabled full interactive Heatmap Grid and Spreadsheet Table view rendering at monthly and annual scale; (4) Upgraded drill-down popup modal to dynamically render period labels and period category changes; (5) Production build verified with 0 errors. | Gemini 3.7 Flash |
| **v4.7.0** | 2026-08-29 | Institutional Portfolio Engine Upgrades: (1) Safety & Rollback: Created paginated backup and restore engine (`scripts/dump_db_snapshot.mjs` and `scripts/restore_db_snapshot.mjs`) generating verified snapshots past Supabase limits; (2) Universal Recalculation Engine: Built `recalculateHoldingState` in `server/services/recalculator.js` handling full chronological FIFO replay, corporate actions (bonus, stock split), inflows/outflows, and integrated with `/api/transactions/:id` and `/api/db-table-update`; (3) US Equity UX: Added explicit USD ($) trade price notice and live converted INR preview in `AddInvestmentView.jsx`; (4) Multi-Pass NPS & MF Engine: Built Protean CRA daily scraper, created `nps_daily_navs` table in Supabase, added `GET /api/nav/nps/:schemeCode`, added on-demand Refresh NAVs button and status badges to `MutualFundsView.jsx` and `NpsView.jsx`, and scheduled hourly GitHub Actions workflow `.github/workflows/daily_nav_sip_sync.yml`; (5) Delta-Ledger Migration: Backfilled 4,996 historical delta transactions for Bank accounts, EPF, Loans, and Credit Cards from 2007-2026 EOD logs into Supabase `transactions` table with foreign key reference `liability_id`, enabling full chronological balance reversibility; (6) Recurring SIP Engine: Created `sips` table in Supabase, `/api/sips` CRUD routes, automated execution engine (`processDueSips`), and interactive `SipManagerModal` in `MutualFundsView.jsx`; (7) Re-synchronized all 6,912 daily EOD logs via `scripts/rebuild_portfolio_eod.mjs` and Supabase `pnl_history`; (8) Verified production build. | Gemini 3.7 Flash |
| **v4.7.1** | 2026-08-29 | Sync Engine Bug Fix & Search Bar Width Expansion: (1) Fixed a bug in `server/services/priceEngine.js` where `fetchProteanNpsNavBatch` was returning a wrapper object rather than the `navMap` instance, causing `proteanNavMap.get is not a function` during price sync; (2) Expanded the top navigation search bar container in `TopNavbar.jsx` from `w-48 md:w-64` to `w-72 sm:w-80 md:w-96 lg:w-[380px]`, preventing placeholder text truncation (`Search tickers, investments, liabilities...`); (3) Tested live sync endpoint (`/api/refresh-prices`) and verified production build with 0 errors. | Gemini 3.7 Flash |
| **v4.7.2** | 2026-08-29 | Bank, EPF, Loan & Credit Card Transaction-Based UI Ingestion: (1) Upgraded `AddInvestmentView.jsx` to replace legacy static balance input forms with modern transaction-based forms for Bank Accounts (`Deposit/Credit` vs `Spend/Debit`), EPF (`Contribution` vs `Withdrawal`), Loans (`EMI Payment` vs `Borrow`), and Credit Cards (`Expense/Charge` vs `Bill Payment`); (2) Added live before/after balance and outstanding debt projection pills with dynamic formatting; (3) Added description/notes fields for transaction category tagging; (4) Verified production build with 0 errors. | Gemini 3.7 Flash |
| **v4.7.3** | 2026-08-29 | Audit-Driven Bug Fixes and Engine Hardening: (1) Fixed critical bug in `DELETE /api/transactions/:id`, `PUT /api/transactions/:id`, and `POST /api/db-table-update` where liability-linked transactions (loans, credit cards) were not triggering `recalculateHoldingState` because only `holding_id` was checked while `liability_id` was ignored; (2) Hardened `recalculator.js` to query transactions by `holding_id` / `liability_id` only, removing fragile `symbol`/`name` OR-matching that could cross-contaminate unrelated records; (3) Added `OPENING_BALANCE` support for liabilities in the recalculator; (4) Improved SIP engine: added `end_date` auto-closure, removed dangerous NAV fallback to 100, added skip-tracking for unavailable NAVs; (5) Fixed form reset after successful submission to use `getDefaultTxType()` per portfolio; (6) Added context-specific success messages per portfolio type; (7) Restricted GitHub Actions cron to NAV declaration window hours only (8 runs/day instead of 24); (8) Verified production build with 0 errors. | Claude Opus 4.6 |
| **v4.7.4** | 2026-08-30 | Price Sync Refactor & UI Polish: (1) Diagnosed Node.js crashes caused by sequential network requests timing out in `refreshAllHoldingsPrices`; (2) Refactored backend `priceEngine.js` to process US Equities, Indian Equities (in concurrent batches of 15), Mutual Funds, and NPS via parallel `Promise.all` with localized `try/catch` blocks, preventing cascading errors; (3) Updated frontend `TopNavbar.jsx` with a 3-second temporary emerald `Synced` UI checkmark post-refresh; (4) Wired real-time `quote_date` from `liveQuoteCache` back to `/api/holdings` output; (5) Fixed "Latest" badge rendering logic across `MutualFundsView.jsx`, `NpsView.jsx`, and `HoldingDetailModal.jsx` ensuring exact timestamp matching. | Antigravity AI |
| **v4.7.5** | 2026-08-30 | GitHub Actions Speedup & Cloud Sweep Parallelization: (1) Diagnosed latency in `syncAllMissingNavs()` caused by repetitive Protean CRA ZIP downloads and sequential MF requests; (2) Added 30-minute in-memory cache for `fetchProteanNpsNavBatch()` and parallelized `Promise.all` execution across all Mutual Funds and NPS schemes, slashing script runtime from 1m 21s down to 4.1s; (3) Upgraded GitHub Actions workflow `.github/workflows/daily_nav_sip_sync.yml` with `actions/cache@v4` on `node_modules` and deterministic `npm ci --omit=dev`, eliminating redundant npm package resolution overhead. | Antigravity AI |
| **v4.7.6** | 2026-08-30 | Universal Real-Time Single Source of Truth Valuation Engine: (1) Resolved architectural data sync discrepancy between Dashboard Net Worth and Calendar current day value by refactoring `/api/daily-pnl` to inject the live, real-time snapshot for the current date using `computePortfolioValuation` with live price feeds (`liveQuoteCache`), identical to `/api/summary`; (2) Updated `/api/summary` to compute `dayPnlINR` and `dayPnlPct` dynamically relative to yesterday's closing wealth; (3) Updated `/api/holding/:holdingId/detail` for Bank, EPF, and Debt instruments to dynamically reflect live database balances on today's timeline; (4) Added 5-second background polling in `CalendarView.jsx` ensuring all views remain in 100% dynamic lockstep without requiring batch scripts; (5) Verified exact 1-to-1 parity across Dashboard and Calendar. | Gemini 3.7 Flash |
| **v4.7.7** | 2026-08-30 | Bank Ledger Deduplication, Recalculator Query Pagination & Weekend Valuation Parity: (1) Diagnosed and resolved +₹8.59L artificial jump on Aug 30 caused by duplicate delta transactions and 1,000-row Supabase query truncation in `recalculator.js`; (2) Added chunked paginated transaction fetching to `server/services/recalculator.js` removing the 1,000-row limit; (3) Cleanly purged duplicate delta transactions and re-executed `scripts/migrate_balance_history_to_transactions.mjs`, reconciling HDFC Savings balance to exact ₹10,619.89; (4) Updated `data/historical_prices.json` with exact settled Friday closing prices (e.g. OIL @ ₹482.85, NPS NAVs); (5) Re-synchronized all 6,913 daily EOD logs via `scripts/rebuild_portfolio_eod.mjs`; (6) Verified Saturday (Aug 29) and Sunday (Aug 30) accurately report exactly ₹0.00 daily P&L and 0 changes with exact ₹18,708,675.57 Net Worth across all views. | Gemini 3.7 Flash |
| **v4.8.0** | 2026-08-30 | Reports Engine Overhaul: Market Cap, MF Look-Through, Sector Drill-Down, and Real-Time Benchmarks: (1) High-Contrast Tooltips: Replaced Recharts default tooltip with the executive dashboard glassmorphism popover featuring bold typography, color dots, formatted currency, and 2-decimal percentage readouts; (2) Top 10 + Other Grouping: Eliminated pie chart truncation by aggregating holdings beyond Top 10 into an `Other (X assets)` slice so 100% of allocation is accounted for; (3) Live Internet Market Cap Engine: Created `asset_metadata` Supabase table and automated sync script (`scripts/sync_asset_metadata.mjs`) categorizing all stocks from internet data into Mega, Large, Mid, Small, and Micro Cap; (4) Mutual Fund Look-Through: Created `mutual_fund_holdings` Supabase table and sync script (`scripts/sync_mf_holdings.mjs`) mapping underlying company holdings, weights, and allocated rupee values across all active schemes; (5) Interactive Sector Drill-Down: Enabled click-to-inspect on sector bars/slices opening a full breakdown of constituent companies, sources, and allocations; (6) Real-Time Growth Benchmarks: Fetched 2 years of daily historical data for Nifty 50, Nifty Midcap 150, Nifty Smallcap 250, S&P 500, and NASDAQ (`scripts/sync_index_history.mjs`), rendering date-by-date normalized trajectory with alpha calculation; (7) Dynamic Chart Switcher: Added toggles between Donut/Pie, Ranked Bar Chart, and Detailed List. | Gemini 3.7 Flash |
| **v4.8.1** | 2026-08-30 | Minimalist Fintech Polish, Scope-Aware Growth Trajectories, Ranked Progress Lists & Nightly Sync: (1) Minimalist Text Rule: Removed all verbose paragraphs and subtitles across headers and cards, establishing strict zero-unnecessary-text rule in `.agents/AGENTS.md`; (2) Calendar Date Range Picker: Added custom start/end date popover alongside preset pills (`1M`, `3M`, `6M`, `1Y`, `ALL`) in Growth vs Indices; (3) Floating Button Removal & Inline Back Button: Removed intrusive floating bottom button that blocked text, adding an inline `← Back` button in `TopNavbar.jsx`; (4) Scope-Aware Growth Engine: Updated `/api/reports/growth-benchmarks` to dynamically compute historical trajectories for selected Equity Hub scopes (e.g. Indian Stocks only vs All); (5) Ranked Progress List: Replaced overlapping horizontal Recharts bar charts with institutional animated ranked progress bars; (6) Fixed theme borders and card clipping; (7) Created `.github/workflows/nightly_metadata_sync.yml` running every night at 23:00 IST (17:30 UTC). | Gemini 3.7 Flash |
| **v4.8.2** | 2026-08-30 | 3D Interactive Depth Graph, Visible Benchmark Lines, Unified Sector Normalization & Full MF Expansion: (1) Light Theme Contrast: Upgraded text and border contrast across all filter buttons, tabs, and cards; (2) Removed Serial Numbers & Stabilized Layout: Removed `#1`, `#2` tags and eliminated list re-render jumping; (3) Benchmark Tracking Line: Converted chart to `ComposedChart` ensuring visible dashed benchmark lines with custom color markers; (4) 3D Interactive Graph: Replaced third toggle with animated 3D isometric extruded depth bars with lighting gradients and spring tilt physics; (5) Full Mutual Fund Company Expansion: Populated complete constituent company holdings across all active funds and grouped cash/debt items into `Cash, Debt & Other`; (6) Custom Glassmorphism Dropdown: Replaced plain rectangle `<select>` with luxury animated scheme selector; (7) Unified Broad Sectors: Implemented unified sector normalizer across US Stocks, Indian Stocks, and Mutual Funds eliminating "Mutual Funds" pseudo-sector. | Gemini 3.7 Flash |
| **v4.8.3** | 2026-08-30 | Pure Light Theme Surface Refactor & Clean Bar Chart Mode: (1) Fixed Light Theme Surfaces: Refactored `ReportsView.jsx` to use pure white cards (`bg-white`), crisp slate borders (`border-slate-200`), dark charcoal text (`text-slate-900`), and dark mode selectors (`dark:bg-slate-900`), eliminating dark muddy containers in Light mode; (2) Clean Bar Chart: Replaced 3D view with institutional Recharts Bar Chart mode (`Donut` vs `Bar Chart`) with non-squished names, clean Y-axis in Lakhs, and hover tooltips; (3) Verified production build and 100% financial integrity test suite. | Gemini 3.7 Flash |
| **v4.8.4** | 2026-08-30 | Chart Switcher Context Isolation, Recharts Anti-Jitter Stabilization & Comprehensive Mutual Fund Expansion: (1) Contextual Chart Switcher: Removed Donut/Bar Chart switcher from non-applicable views (`Mutual Fund Look-Through` and `Growth vs Indices`), displaying it only for `Allocation`, `Market Cap`, and `Sectors`; (2) Anti-Jitter Stabilization: Disabled continuous Recharts animation loops and memoized scope bindings, eliminating graph refreshing/jumping on background price ticks; (3) Comprehensive Mutual Fund Expansion: Populated full 30 to 45 constituent company holdings across all 16 active mutual fund schemes (467 rows in Supabase), reducing the "Other" cash buffer to minimal 2-4% and eliminating outliers. | Gemini 3.7 Flash |
| **v4.8.5** | 2026-08-30 | 10-Year Benchmark History & Simulated Index Opportunity Growth Engine: (1) 10-Year Index History: Upgraded `scripts/sync_index_history.mjs` to fetch 10-year daily history across Nifty 50, Nifty Midcap 150, Nifty Smallcap 250, S&P 500, and NASDAQ; (2) Simulated Index Opportunity Tracking: Refactored `/api/reports/growth-benchmarks` to simulate what the starting invested capital would be worth if invested in the benchmark index over the chosen timeframe, eliminating flat 0 benchmark curves when viewing "ALL" or custom historical ranges; (3) Verified production build and 100% financial integrity suite. | Gemini 3.7 Flash |
| **v4.9.0** | 2026-08-30 | Reports Engine Upgrade: Interactive Company Mutual Fund Breakdown, Zero-Quantity Holding Valuation Fix, Strict Theme Fidelity, Benchmark Historical Date Alignment & Floating Back Button: (1) Zero-Quantity Direct Holding Fix: Hardened `computeHoldingValueINR` and `companyMfBreakdown` so equity shares with 0 quantity (e.g. historical redeemed trades) return strictly ₹0.00 instead of falling back to the single-share price; (2) Company Mutual Fund Breakdown Modal: Clicking any company in MF Composition, Market Cap, or Sector opens a detailed modal showing total portfolio value, direct shares, and a scheme-by-scheme breakdown of every Mutual Fund holding that stock with exact fund weight (%), allocated rupee amount, and share of holding (%); (3) Strict Theme Fidelity: Replaced hardcoded dark background and white text classes with responsive adaptive classes (`text-slate-900 dark:text-white`, `bg-white/80 dark:bg-slate-900/80`, `border-slate-200 dark:border-slate-800`), eliminating white text and dark containers in Light / Warm Light modes; (4) MF Composition Report: Renamed the dedicated Mutual Fund tab to 'MF Composition' with a theme-matching dropdown; (5) Benchmark Historical Date Alignment: Refactored `/api/reports/growth-benchmarks` so simulated benchmark trajectory starts at the exact historical valuation on the initial date of the range (e.g. ₹50.9k on 2020-03-30); (6) Aggregated Company Drill-Down: Merged direct and multi-scheme holdings into a single unified row per company; (7) Floating Persistent Back Button: Added a semi-transparent floating circle button at bottom-right with only the arrow symbol; (8) Verified 100% financial integrity test suite. | Gemini 3.7 Flash |
| **v4.9.1** | 2026-08-30 | Money-Weighted Benchmark Engine: Replaced the single-base-capital simulated benchmark with a true money-weighted benchmark mirroring actual cash flows. Root cause: Supabase 1,000-row pagination limit caused only CREDIT/DEBIT bank transactions to be fetched (missing all 5,789 BUY/SELL transactions). Fixes: (1) Paginated BUY/SELL-only transaction fetch loop added; (2) Incremental virtual unit accumulation: each BUY purchases virtual index units at that date's index price, each SELL proportionally reduces units; (3) `_Normalized` field = money-weighted INR value on chart y-axis; `_GrowthPct` = pure index price return from window start for summary stat cards; (4) For ALL scope, chart starts from first investment asset date (not first bank deposit in 2007); (5) Bank/savings excluded from benchmark as non-market assets; (6) Verified production build. | Antigravity AI |
| **v4.9.2** | 2026-08-30 | High-Performance Benchmark Caching & Binary Search Engine: Diagnosed and resolved 5-second latency on the Growth vs Indices benchmark report caused by repeated 6-batch Supabase transaction roundtrips (1.9s) and un-indexed string date sorting inside the transaction loop (~2.7s CPU event-loop spin across 25,000 iterations). Built `server/services/benchmarkEngine.js` with: (1) Pre-sorted index date arrays and O(log N) binary search lookup (1000x faster than inline sorting); (2) High-speed in-memory caching of scoped transactions and holdings with automatic cache invalidation on new investments/metadata sync; (3) Sub-10ms response times for timeframe switching (`1M`, `3M`, `6M`, `1Y`, `ALL`); (4) 100% financial integrity suite pass. | Gemini 3.7 Flash |
| **v4.9.3** | 2026-08-30 | UI Theme Alignment & Table Border Consistency: (1) Calendar View Theme Fix: Replaced `bg-slate-950/95` with `bg-slate-900/95` in `CalendarView.jsx` sticky columns to properly hook into the `.light` mode background overrides (`#F1F5F9`), resolving black headers on light theme; (2) Consistent Table Row Borders: Replaced `divide-y` with direct `[&>tr]:border-b` across all portfolio table bodies (`IndianStocksView`, `UsStocksView`, `MutualFundsView`, `NpsView`, `DividendsView`, `DatabaseStudioView`, `CalendarView`, `HoldingsTable`) to ensure the first row correctly falls back to the header's `border-bottom` and eliminates the visual missing top-border illusion caused by CSS table border collapse. | Antigravity AI |
| **v4.9.4** | 2026-08-30 | Data Cleaning & UX Formatting: (1) Fixed sector override bug where missing metadata sectors saved as `'Unknown'` were overwriting valid user-entered database sectors; (2) Hid the `'Unknown'` sector text entirely from Exited shares, Indian Stocks, US Stocks, and Mutual Funds to prevent UI clutter; (3) Cleaned US Stock names by stripping out verbose legal/registry terms (e.g., "Common Stock", "Class A", "Registry Share") to leave only the recognizable company name. | Antigravity AI |
| **v4.9.5** | 2026-08-30 | UI & Daemon Fixes: (1) Fixed issue where US Stock names were not actively stripped because the backend daemon was not actively listening on port 5000 (restarted daemon process); (2) Removed `border-l-2` from all portfolio table rows across `UsStocksView`, `NpsView`, `MutualFundsView`, and `IndianStocksView` to eliminate the left-edge gray vertical line and match the borderless right edge. | Antigravity AI |
| **v4.9.6** | 2026-08-30 | Exited Shares Metadata Sync: (1) Modified `scripts/sync_asset_metadata.mjs` to fetch metadata from screener/APIs for *all* holdings, removing the active (`quantity > 0`) filter, so exited shares now have their sectors and market cap populated. (2) Added an automated asynchronous background trigger in `POST /api/holdings` to run `syncAssetMetadata()` immediately upon adding any new share, ensuring future exited or active shares automatically pull their metadata without manual intervention. | Antigravity AI |
| **v4.9.7** | 2026-08-30 | High-Speed Metadata Engine Upgrade: Replaced slow sequential Screener web scraping with concurrent, high-throughput Yahoo Finance primary API (`yahoo-finance2`) integration in `scripts/sync_asset_metadata.mjs`. Completed full metadata sync across all 343 assets (including previously missing exited shares such as `AARTIDRUGS`, `AARTIPHARM`, `ACMESOLAR`, `ADORWELD`). Verified 100% financial integrity pass. | Antigravity AI |
| **v4.9.8** | 2026-08-30 | Portfolio Views Header Redesign & Minimalist Layout Alignment: (1) Removed generic "As of [Date]" text from Mutual Funds, NPS, Indian Equity, and US Equity banners; (2) Placed "Schemes / Stocks Up-to-Date" status indicator badges directly below portfolio titles; (3) Added a minimalist, icon-only Refresh button directly to the right of each portfolio title text with interactive spin animations; (4) Moved the "Recurring SIP" button from the top banner to the left of the search window inside the Mutual Funds table controls bar; (5) Wired real-time price & NAV refresh across all 4 portfolio views; (6) Verified 100% financial integrity test pass. | Gemini 3.7 Flash |
| **v4.9.9** | 2026-08-30 | Indian & US Stocks Table Row Refresh Status Badges & Active Count Normalization: (1) Added individual quote refresh status pills (`Today (Date)` / `As of Date`) next to ticker and sector in `IndianStocksView.jsx` and `UsStocksView.jsx` matching Mutual Funds and NPS styling; (2) Unified `isUpToDate` date matching across all 4 views; (3) Fixed `upToDateCount` calculation in all 4 views to strictly filter active holdings (`quantity > 0`), ensuring header status indicators reflect accurate ratios; (4) Verified 100% financial integrity pass. | Gemini 3.7 Flash |
| **v5.0.0** | 2026-08-30 | Header Clutter Cleanup: Removed the redundant USD/INR exchange rate badge from the US Equity banner in `UsStocksView.jsx`, preserving global exchange rate display in the top navigation bar. | Gemini 3.7 Flash |
| **v5.1.0** | 2026-08-31 | Multi-Colour Gradient Hero Banners & Consolidated Performance Report Integration: (1) Upgraded Indian Equity (`IndianStocksView.jsx`), US Equity (`UsStocksView.jsx`), Mutual Funds (`MutualFundsView.jsx`), and NPS (`NpsView.jsx`) hero banners to use large multi-colour fading gradient typography matching EPF and Bank Account styles; (2) Replaced the two-compartment split box in asset headers with clean active valuation readouts, invested cost, unrealized gain, and active XIRR % pills; (3) Created a dedicated Consolidated Performance report in `ReportsView.jsx` (`PERFORMANCE` sub-tab) featuring executive KPI cards, asset class comparison grid, and an institutional performance ledger comparing active vs realized vs combined lifetime metrics across all portfolio categories; (4) Verified production build and 100% financial integrity test suite pass. | Gemini 3.7 Flash |
| **v5.2.0** | 2026-09-02 | Daily Automated EOD Sync & Cloud Valuation History Integration: (1) Automated EOD Workflow: Created `.github/workflows/daily_eod_sync.yml` running daily at 23:45 UTC (05:15 IST) to fetch settled market quotes and persist updated EOD records to Supabase `pnl_history`; (2) Seamless EOD Log Merging: Refactored `/api/daily-pnl` in `server/index.js` to dynamically merge new records from `pnl_history` with the local EOD cache; (3) Historical NPS Integration: Enhanced `scripts/rebuild_portfolio_eod.mjs` with `fetchNpsHistoricalNav` to accurately price historical NPS positions; (4) Incremental Market Price Fetch: Added `--incremental` flag to `scripts/load_historical_prices.mjs` to fetch only missing market days; (5) Aligned dashboard day P&L in `/api/summary` to compute relative to verified previous day closing net worth from historical logs. | Antigravity AI |
| **v5.3.0** | 2026-09-05 | Holding Detail Actual Chart Event Indicators, Axis Standardization & Bank Account Controls: (1) Restored Actual Chart Event Indicators: Re-enabled `ActualEventDot` with action-specific color rings and event tooltips in `HoldingDetailModal.jsx`; (2) Tooltip & Axis Precision: Standardized axis formatting (`formatAxisValue`) and fixed fractional precision across Tracker and Actual charts, and hid Actual Chart for bank accounts; (3) Bank Account State & Modals: Added active/closed filter tabs, account reopening with current balance prompt, and closing with custom themed confirmation modal in `BankView.jsx` and `App.jsx`; (4) Theme-Aware Transaction Tokens: Migrated transaction type buttons in `AddInvestmentView.jsx` and `src/index.css` to semantic CSS theme variables (`--tx-buy`, `--tx-sell`, `--tx-bonus`, `--tx-dividend`, `--tx-split`); (5) Calendar Table Trend Contrast: Upgraded period-over-period trend color styling in `CalendarView.jsx` table view; (6) Status Badge De-Cluttering: Removed redundant row-level status pills across equity, MF, and NPS views. | Antigravity AI |
| **v5.3.1** | 2026-09-05 | EPF View Minimalist De-Cluttering & Dynamic Metric Integration: (1) Removed static interest rate pill badge (`FY 2025-26: 8.25% p.a.`) and filler descriptive tagline from EPF hero header; (2) Removed redundant `Inspect Timeline` button and simplified action button in member account card to clean `View`; (3) Removed static screenshot rows 2 and 3 (~50% corpus assumptions, static compounding statement); (4) Replaced static cards with dynamic live metric cards (1-Year Growth `+₹7.04L (+18.05%)`, Est. Annual Yield `+₹3.80L`, and Last Credit `+₹62,508.00` on 12-06-2026); (5) Verified production build and 100% financial integrity test suite pass. | Gemini 3.8 Flash |
| **v5.3.2** | 2026-09-05 | EPF Holding Detail Minimalist UI & Actual Chart Removal: (1) Removed redundant `EPF` bold header, `EPF-RETIREMENT` badge, and `Employee Provident Fund` subtitle from the `HoldingDetailModal.jsx` for EPF assets; (2) Disabled the 'Actual Chart' tab for EPF assets as it is redundant for non-market EOD tracking assets. | Gemini 3.1 Pro (Low) |
| **v5.3.3** | 2026-09-05 | EPF Logo Enhancements: (1) Standardized the EPF icon across the sidebar to use the `Building2` icon, aligning it visually with the EPF page hero header; (2) Implemented automatic fetching of the official EPFO logo from Wikimedia Commons in `domain.js`, eliminating the basic `EP` text fallback in the `HoldingDetailModal.jsx` header. | Antigravity AI |
| **v5.3.4** | 2026-09-05 | Liabilities & Debt View Modernization and Minimalist De-Cluttering: (1) Removed redundant '2 Active Obligations' subtext from hero banner; (2) Removed 'Monthly EMI' from hero header and from both individual liability cards; (3) Cleaned loan title by removing '(SBI Bank)' to display clean 'Housing Loan'; (4) Removed interest rate ('8.5% p.a.' / '0.0% APR') and 'Due' day subheaders from both Loan and Credit Card instruments; (5) Implemented 'Active Obligations' and 'Closed Obligations' segmented toggle matching BankView; (6) Added dedicated Close and Reopen account/card action buttons with interactive modal confirmations, supporting balance zeroing on closure and balance re-entry on reopen via PUT /api/liabilities/:id. | Gemini 3.8 Flash |
| **v5.3.5** | 2026-09-05 | Refactored liabilities UI in HoldingDetailModal to dynamically display the lender name and logo instead of generic tags. Removed irrelevant Actual Chart toggle for debt accounts and cleaned up subtitle text for a strictly minimalist presentation. | Antigravity AI |
| **v5.4.0** | 2026-09-06 | Dynamic Housing Loan Amortization Calendar, Interactive Chart & Prepayment Engine: (1) Data Ingestion: Ingested 153 actual settled records from Investment.xlsx ('Loan Amortization') into Supabase 'loan_amortization' table and data/loan_amortization.json (₹69.98L sanctioned, ₹51.65L EMI paid, ₹11.38L prepayments, ₹37.69L interest paid, ₹44.64L current balance); (2) Dynamic Calculation Engine: Built server/services/loanEngine.js projecting future monthly schedules dynamically up to ₹0 balance with daily interest calculation, regular ₹60k EMI, and user-added dynamic entries; (3) Backend API: Added GET /api/loan/amortization, POST /api/loan/amortization/entry, DELETE /api/loan/amortization/entry/:id, and attached amortization payload to GET /api/holding/:id/detail for loans; (4) Frontend Component: Built LoanAmortizationSection.jsx with executive KPI tiles, Recharts Balance Payoff Trajectory / Annual Breakdown bar charts, Prepayment What-If Simulator, dynamic Add Entry modal, and Amortization Calendar Table with Settled and Projected filters; (5) Liabilities Integration: Added Amortization Schedule button to Housing Loan card in LiabilitiesView and embedded LoanAmortizationSection with sub-tab controls in HoldingDetailModal; (6) Verified 100% financial integrity test suite pass and clean production build. | Antigravity AI |
| **v5.4.1** | 2026-09-06 | Housing Loan Amortization User Refinements: (1) Clean Lender Title: Stripped '(SBI)' and 'SBI' occurrences across LiabilitiesView and HoldingDetailModal to cleanly show 'State Bank of India'; (2) Single Navigation Path: Removed duplicate 'Amortization Schedule' button from the Housing Loan card since the 'View' button already opens the detail modal; (3) Subtitle Renaming: Renamed 'Home Loan' to 'Housing Loan' in HoldingDetailModal; (4) Contractual EMI vs. Monthly Payment: Distinguished contractual EMI (₹52,653.00) from active monthly payment (₹60,000.00) in separate KPI tiles with inline installment edit modal and 'Update Monthly Installment / Payment' in Add Entry; (5) Exact Decimal Precision: Replaced all abbreviated currency amounts (44.64L, 44.97L) with exact rupee formatting (₹44,64,447.00, ₹69,98,345.00, ₹44,96,758.00); (6) Table Entry Actions: Added Edit and Delete actions to Amortization Calendar table rows with PUT /api/loan/amortization/entry/:id; (7) Fixed ReferenceError for `formatMoney` in HoldingDetailModal; (8) Verified 100% financial integrity test suite pass and clean production build. | Antigravity AI |
| **v5.4.2** | 2026-09-06 | Housing Loan Detail Modal Sub-Tab Ordering & Delete Modal Standardization: (1) Positioned `Daily Balance History` as the primary left button and `Amortization Schedule & Chart` on the right in `HoldingDetailModal.jsx`, and removed the calendar icon for a clean aesthetic; (2) Replaced native `window.confirm` with the project's standard themed modal popup (`.modal-surface`, backdrop blur, animated transitions, and explicit cancel/delete actions) in `LoanAmortizationSection.jsx`; (3) Replaced remaining instances of 'Home Loan' with 'Housing Loan' across `AddInvestmentView.jsx`, `data/liabilities.json`, and project documentation; (4) Sanitized lender name in Supabase liabilities table to cleanly store 'State Bank of India'; (5) Verified 100% financial integrity invariance pass and clean production build. | Antigravity AI |
| **v5.5.0** | 2026-09-06 | Dividends View Overhaul & Sidebar Logo Standardization: (1) Standardized NPS sidebar logo to use `ShieldCheck`, aligning it with the NPS main page header; (2) Standardized Dividends main page banner logo to use `Coins`, matching the sidebar; (3) Removed the Yield box and all "FX" labels in Dividends view (US card title updated to clean "US ($)"); (4) Overhauled Dividends ledger table to match Indian & US Equity format: integrated `HoldingLogo`, cleaned corporate/stock suffixes (removing "Common Stock", "Class A", etc.), and added asset drill-down to `HoldingDetailModal`; (5) Added market filter tabs (`All`, `Indian Equity`, `US Equity`), search bar, and sortable columns (Stock Name, Market, Payout, INR Credited, Date); (6) Removed redundant FX Rate column from table; (7) Verified 100% financial integrity test suite pass and clean production build. | OpenRouter (Delegated) / Antigravity AI |
| **v5.5.1** | 2026-09-06 | Dividends Dual-Currency Toggle Sync & Add Entry Modal Integration: (1) Synced Dividends metric cards with global currency toggle: in INR mode, US card displays INR as primary metric (`₹7,92,292.38`) with dollar equivalent below (`≈ $9,023.95`), and vice versa when USD mode is toggled; (2) Added emerald `Add Entry` button in Dividends banner; (3) Created dedicated `AddDividendModal.jsx` matching Indian/US Equity dividend transaction entry design: category toggle, stock search autocomplete with `HoldingLogo`, `DIVIDEND` amber badge, payout amount, payment date, live USD/INR exchange rate with converted INR preview; (4) Connected to `POST /api/add-investment` updating `dividends` table and seamlessly synchronizing with `HoldingDetailModal`, `OverviewView`, and equity pages; (5) Verified 100% financial integrity test suite pass and clean production build. | Antigravity AI |
| **v5.5.5** | 2026-09-06 | Removed Redundant 0.0% Allocation Pill in Reports: Removed the confusing and unneeded `0.0% Allocation` badge from the header of Asset Class Performance cards in `ReportsView.jsx`. Allocation weights are already comprehensively analyzed in the dedicated Allocation Donut and Bar charts with full look-through. Verified clean build and 100% financial integrity test suite pass. | Gemini 3.8 Flash |
| **v5.6.0** | 2026-09-06 | Universal Border Completion & Project-Wide Table Sort and Filter: (1) Universal Border Completion: Diagnosed and resolved cut-off/clipped borders on Ranked Progress list cards and cards in scroll containers caused by outset `ring-2` rendering beyond overflow bounds; added `.reports-subcard.is-selected` with explicit theme-adaptive inset borders and box-shadow in `index.css`, converted card selection rings to `ring-2 ring-inset` across `ReportsView.jsx`, `CalendarView.jsx`, and `CalendarHeatmap.jsx`, and added `p-1.5 custom-scrollbar` container padding to guarantee complete, unclipped borders on all sides; (2) Project-Wide Table Search & Column Sorting: Implemented real-time search filtering and clickable column sorting across all tabular displays in the application lacking them: (a) `ReportsView.jsx` Consolidated Category Performance Ledger (Search + Sort by Asset Class, Active Val, Active Cost, Unrealized P&L, Realized P&L, Lifetime Cost, Net Return, Abs ROI %, Annualized XIRR); (b) `ReportsView.jsx` Mutual Fund Look-Through Constituent Holdings (Search + Sort by Company, Sector, Cap Tier, Fund Weight %, Allocated Value); (c) `ReportsView.jsx` Market Cap Drill-Down (Search + Sort by Stock, Source, Contribution %, Allocated Value); (d) `ReportsView.jsx` Sector Drill-Down (Search + Sort by Company, Source, Cap Tier, Allocated Value); (e) `ReportsView.jsx` Company Mutual Fund Breakdown Modal (Search + Sort by Scheme, Fund Weight %, Allocated Value, Share of Holding %); (f) `OverviewView.jsx` Asset Class Performance table (Search filter bar); (g) `HoldingDetailModal.jsx` Transaction Ledger (Search filter bar + transaction type filter pills); (h) `AssetDividendDetailModal.jsx` Itemized Distribution Ledger (Search filter bar + Sort by Date, Original Payout, FX Rate, INR Credited, Cumulative Total); (i) `LoanAmortizationSection.jsx` Schedule table (Search filter bar + Sort by Date, Type, Opening Balance, EMI, Bulk Payment, Interest, Principal, Closing Balance, Rate); (3) Verified production build and 100% financial integrity invariance suite pass. | Antigravity AI |
| **v5.10.0** | 2026-09-06 | Multi-Asset Historical Auto-Tracking Engine Repair & Zero-Data-Gap Continuous Synchronization: (1) Root Cause Diagnosis: Resolved issue where Mutual Funds and NPS schemes were frozen/untracked on 2nd Sept and subsequent dates due to (a) omission of NPS in `scripts/load_historical_prices.mjs`, (b) baseline truncation flaw in `scripts/rebuild_portfolio_eod.mjs` where existing database logs prevented re-evaluating past dates upon late NAV publication, and (c) unpaginated database queries in `server/db.js`; (2) Ingestion & Historical Engine Hardening: Added `fetchNpsHistorical` and included NPS schemes across historical price sync engines; (3) Post-Baseline Re-evaluation: Refactored `rebuild_portfolio_eod.mjs` to re-evaluate all post-baseline dates (from 2026-08-08 through current day) from settled historical quotes and NAV feeds, dynamically healing any previously missing market days; (4) Database Pagination Guard: Upgraded `server/db.js` (`select` and `selectWhere`) with automatic pagination loops across 100% of rows (all 11,205 transactions, 397 holdings, liabilities); (5) Historical Parity Backfill: Rebuilt all 6,920 daily logs, backfilled settled NAVs for 2nd, 3rd, 4th, 5th, and 6th September across Mutual Funds and NPS, and synchronized with Supabase `pnl_history` and `data/portfolio_eod_logs.json`; (6) Verified 100% financial integrity test suite pass across all invariance assertions. | Antigravity AI |
| **v5.11.0** | 2026-09-06 | Bank & EOD Asset Ledger Refactor, Running Balance Accumulation & Badge Parity: (1) Diagnosed and resolved UI misconception in `HoldingDetailModal.jsx` where Bank, EPF, and Liability transaction ledgers mislabeled historical deposit/withdrawal transactions as 'Daily Balance History' and displayed individual transaction amounts under 'EOD Balance (₹)'; (2) Fixed false negative indicator bug where `isPos = tx.type === 'BUY'` caused all Bank and EPF deposits, credits, and withdrawals to render with a negative red `↓ -₹X` badge; (3) Built chronological running balance engine accumulating deposits, credits, withdrawals, debits, loan repayments, and borrow events into an exact post-transaction account balance column (`Running Balance (₹)`); (4) Expanded `TxBadge` with comprehensive badges for `CREDIT`, `DEPOSIT`, `DEBIT`, `WITHDRAWAL`, `CONTRIBUTION`, `INTEREST`, `BORROW`, `EMI_PAYMENT`, `CHARGE`, `PAYMENT`; (5) Enabled dynamic transaction type filter pill tabs across all holdings; (6) Verified production build and 100% financial integrity test suite pass. | Antigravity AI |
| **v5.12.0** | 2026-09-06 | 360-Degree Raw Source Reconciliations & Master Database Parity: (1) Exhaustive Raw Data Audit: Cross-audited all 33 Mutual Funds, 12 US Stocks, 331 Indian Stocks, 6 Bank Accounts, 1 EPF, and 2 Liabilities against raw source spreadsheets (`portfolio.xlsx`, `Ind_Stocks.csv`, `Ind_Mfs.csv`, `Book2.xlsx`, `NPS/*.csv`, `charges.xlsx`); (2) NPS Scheme Deduplication: Merged duplicate 0-quantity rows for `SM003007` (LIC Scheme G Direct), `SM008002` (HDFC Scheme C Direct), and `SM002003` (UTI Scheme E Direct), re-linking 104 historical transactions to active master records; (3) CLEDUCATE Sell Quantity Correction: Fixed 2025-10-29 sell transaction from 649 to 585 shares per `Ind_Stocks.csv`, bringing remaining holding quantity to exact 97 shares; (4) Missing Exited Stock Ingestion: Ingested `VIYASH` (Viyash Scientific Limited) with 3 historical transactions (status: `REDEEMED`, 0 qty); (5) Zero Balance Display Precision: Hardened `HoldingDetailModal.jsx` with strict nullish coalescing (`??`) for zero-value bank accounts and liabilities; (6) Verified 100% PASS on `node scripts/verify_financial_integrity.mjs`. | Antigravity AI |
| **v1.3.3** | 07-09-2026 | Dividends aggregation and transaction-level management upgrade: (1) Aggregated main Dividends page by scheme with Logo, Clean Name, Market Badge, Payouts Count, Total Payout, Total Credited INR/USD, Latest Date, and Action Delete button; (2) Added transaction-level Edit and Delete actions in AssetDividendDetailModal itemized ledger with inline editing; (3) Added backend endpoints (DELETE /api/dividends/scheme/:idOrSymbol, DELETE /api/dividends/:id, PUT /api/dividends/:id); (4) Upgraded AssetDividendDetailModal with stacked monthly sub-bars inside annual breakdown chart, embedded month badges, unabbreviated exact annual total numbers at top of bars, multi-theme contrast support across all light/dark themes, robust multi-format date tracking across all years, calendar range selector (ALL, 1Y, 3Y, 5Y, Custom), and removed redundant payout badges; (5) Verified dynamic transaction type filtering in HoldingDetailModal and confirmed 100% pass on financial integrity audit. | Antigravity AI |
| **v5.18.0** | 08-09-2026 | Universal Multi-Asset Integrity Suite & Protean CRA Priority: (1) Enforced official Protean CRA scraper (`nps_daily_navs`) as the primary authoritative source of truth for NPS across all scripts; (2) Reconciled 2026-09-07 EOD log with exact finalized AMFI and Protean NAVs; (3) Hardened `rebuild_portfolio_eod.mjs` to target yesterday's closed session and never snapshot an unfinished trading day; (4) Created `scripts/verify_all_assets_integrity.mjs` auditing all 8 asset/liability classes across 6,921 historical records with 100% pass; (5) Added Rules 8 and 9 in `.agents/AGENTS.md` and `LADDER.md`. | Antigravity AI |
| **v5.18.1** | 08-09-2026 | Equity Stocks Price Sync & 07-Sep Calendar Reconciliation: (1) Diagnosed and resolved missing Indian stock daily price updates for 07-Sep in `historical_prices.json` caused by null array elements in Yahoo Finance daily intervals; (2) Added automatic `meta.chartPreviousClose` / `meta.previousClose` fallback across `sync_daily_prices.mjs` and `rebuild_portfolio_eod.mjs`; (3) Upgraded `rebuild_portfolio_eod.mjs` with Step 3 to automatically verify and fetch missing recent equity quotes up to `targetEndDate`. | Antigravity AI |
| **v5.19.0** | 12-09-2026 | Supabase Free Tier Optimization & Cloud 3-Tier Rolling Backup Engine: (1) Resolved runaway storage crisis: dropped unconstrained `trg_audit_holdings` / `trg_audit_liabilities` triggers and truncated `audit_logs` (203,212 rows), shrinking DB storage from 340+ MB (75%) down to 13.5 MB (2.7%); (2) Created automated Cloud 3-Tier Rolling Backup Engine (`scripts/backup_manager.mjs`, `scripts/restore_backup.mjs`) storing complete database snapshots in Supabase Storage (`ladder_backups` bucket) with strict 3-backup retention and 0 impact on DB quotas; (3) Built interactive Cloud Backup & Snapshot Sync card in `ExcelToolsView.jsx` with one-click cloud backup and safe point-in-time database restoration; (4) Eliminated egress bottleneck: implemented reactive in-memory caching (`dbCache`) in `server/db.js` with instant mutation invalidation, cutting monthly egress by 99% (<50 MB/month); (5) Established Rules 11 & 12 in `.agents/AGENTS.md` and `LADDER.md` mandating egress control and storage protection; (6) Verified 100% pass across financial integrity and multi-asset audit suites. | Antigravity AI |
| **v5.19.1** | 12-09-2026 | Compressed Cloud Backup, 10-Day Retention, Profile Menu Integration & 08:25 AM IST Automation: (1) Lossless Gzip Compression: Upgraded `scripts/backup_manager.mjs` and `scripts/restore_backup.mjs` with zlib gzip level 9 compression (`.json.gz`), reducing raw backup payloads from 15.21 MB down to 1.04 MB (93.2% space reduction) with 100% data integrity; (2) 10-Day Retention Policy: Replaced 3-backup cap with unlimited backups within 10 days, automatically pruning snapshots older than 10 days from Supabase Storage and local disk; (3) Profile Dropdown UI Integration: Added 'Backup Database Now' (with loading state and toast feedback) and 'Restore Database' buttons directly inside the user profile menu in `TopNavbar.jsx`; (4) Standalone Modal Architecture: Built `RestoreBackupModal.jsx` utilizing standard project tokens (`modal-surface`, `reports-card`), viewport centering, ESC key dismiss, cloud snapshot listings, and safe confirmation dialogs; (5) Daily Auto-Scheduler: Implemented daily automated backup scheduled for 08:25 AM IST (02:55 UTC) in `server/index.js` and created `.github/workflows/daily_backup.yml` for cloud execution; (6) Verified clean production build and 100% financial integrity test pass. | Antigravity AI |
| **v5.20.0** | 12-09-2026 | Robust NPS NAV Pipeline — Holiday Calendar, Already-Synced Detection & Date-Verified Protean Scraper: (1) Added `NSE_HOLIDAYS_2026` in `server/services/priceEngine.js` covering all 16 official NSE trading holidays for 2026; (2) Exported `isTradingDay(dateISO)` helper returning false for weekends and NSE holidays — `syncAllMissingNavs` exits immediately on non-trading days with `skipped: true` and zero network calls; (3) Added `areTodayNavsAlreadySynced(schemeCodes, targetDate)` which queries `nps_daily_navs` in Supabase before touching Protean CRA, eliminating redundant ZIP downloads when all schemes are already captured for the trading day; (4) `fetchProteanNpsNavBatch` now reads the NAV date embedded in the ZIP's `.out` CSV content and only upserts to Supabase if that date matches the last trading day — if Protean has not yet published today, upsert is skipped and the cache is marked stale; (5) npsnav.in is used only as a dated fallback: accepted if it returns today's trading date, otherwise carry-forward from Protean's last-known NAV is used for live display only; (6) Updated GitHub Actions `daily_nav_sip_sync.yml` to cover the full Protean publication window (21:07–23:37 IST) plus morning catch-up runs (09:07–10:07 IST); (7) `scripts/sync_navs_and_sips.mjs` surfaces skip reason in CI logs with exit code 0 on non-trading days; (8) Verified clean production build (0 errors). | Antigravity AI |
| **v5.21.0** | 12-09-2026 | Comprehensive System Hardening, Historical Replay Parity & Robustness Enhancements: (1) Chronological Transaction Timeline Replay: Refactored `rebuild_portfolio_eod.mjs` to replay bank, EPF, and liability delta transactions from Supabase chronologically across all 6,925 historical EOD dates; (2) Unified Daily Sync Orchestrator: Created master CLI `scripts/daily_sync.mjs` orchestrating price ingestion, EOD rebuild, financial integrity verification, and cloud backup with duration timing; (3) Contextual Quick-Add Drawer: Added "+ Add Transaction" button and pre-filled transaction form drawer in `HoldingDetailModal.jsx` for all categories; (4) SIP Execution & Skips History: Added "Execution & Skips" tab in `SipManagerModal.jsx` with persistent logging in `data/sip_history.json`, API route `GET /api/sips/history`, and `isTradingDay` holiday deferrals in `sipEngine.js`; (5) Startup Cache Pre-Warming: Added `warmCache()` in `server/db.js` for zero cold-cache query storms; (6) Authoritative NPS History & Pagination Guard: Paginated `nps_daily_navs` queries in `rebuild_portfolio_eod.mjs` and updated `fetchNpsHistoricalNav` to query Supabase exclusively with carry-forward, eliminating npsnav.in fallback; (7) Invariance & Audit Hardening: Generalized `verify_all_assets_integrity.mjs`, synchronized weekend P&L parity across `/api/summary` and `/api/daily-pnl`, added benchmark sync staleness badge in `ReportsView.jsx`, and added last backup indicator in `TopNavbar.jsx`; (8) Verified 100% pass on integrity test suites and clean Vite build. | Antigravity AI |
| **v5.21.1** | 12-09-2026 | Custom Application Logo: Replaced the CSS animated logo in `Sidebar.jsx` with the user's custom uploaded logo image. | Antigravity AI |
| **v5.22.0** | 12-09-2026 | Official Protean CRA Direct NPS Scheme Alignment & Invariance Audit: (1) Re-mapped active NPS Direct holdings and post-01-Apr-2026 transactions to official PFRDA Direct scheme codes (`SM008019`, `SM003027`, `SM002027`); (2) Backfilled 12 missing market days of official daily NAVs from Protean CRA archive ZIPs (`NAV_File_DDMMYYYY.zip`) into `nps_daily_navs`; (3) Updated active NPS valuation to verified official Protean CRA Direct NAVs (`₹5,10,232.39`); (4) Preserved 11 historical closed schemes with their official POP codes, exact transaction ledger, lifetime cost basis, and realized P&L; (5) Upgraded `recalculator.js` to preserve lifetime average buy cost for redeemed positions so historical cost basis and ROI % are accurately displayed across all asset classes; (6) Rebuilt all 6,925 historical portfolio EOD records in `pnl_history` and `data/portfolio_eod_logs.json`; (7) Verified 100% pass across all financial integrity and multi-asset audit test suites. | Antigravity AI |
| **v5.23.0** | 12-09-2026 | Robust Universal On-Demand Refresh Engine & Non-Trading Day Parity: (1) Resolved NPS refresh bug where on-demand sync was skipping on non-trading days/weekends by ensuring syncAllMissingNavs targets the most recent trading day (Friday) and only skips if already synced in Supabase; (2) Fixed date format discrepancy in fetchNpsNavFallback, normalizing fallback date to ISO YYYY-MM-DD so npsnav.in fallback matches lastTradingDay and upserts cleanly into nps_daily_navs; (3) Hardened refreshActiveHoldingsPrices and refreshAllHoldingsPrices to check fallback when Protean CRA batch is stale or missing the latest session, persisting confirmed fallback records to Supabase; (4) Enhanced isUpToDate and getQuoteDateLabel across all 4 portfolio views (NpsView, IndianStocksView, MutualFundsView, UsStocksView) to recognize the latest completed market session (e.g. Friday on weekends) as up-to-date with emerald badges and clean 'Latest (Date)' labels; (5) Standardized TopNavbar Profile dropdown styling (theme-adaptive VP avatar, removed redundant line and button border, renamed Import/Export); (6) Verified 100% pass across financial integrity tests and clean production build. | Antigravity AI |
| **v5.24.0** | 12-09-2026 | Dynamic Multi-Asset Market Calendar Engine & Rule 13 (Strict Anti-Hardcoding Protocol): (1) Created dynamic multi-asset market calendar service `server/services/marketCalendar.js` dynamically computing trading days, market holidays, previous trading days, and next trading days for Indian markets (NSE/BSE/AMFI/NPS) and US markets (NYSE/NASDAQ) across current and future years (2026, 2027, 2028+); (2) Algorithmic holiday computation: Gauss Easter algorithm for Good Friday, observation rules for weekend-falling holidays, and multi-year gazetted festival calendars; (3) Replaced all static holiday arrays and single-year assumptions in `priceEngine.js` and `sipEngine.js` with dynamic calendar engine calls; (4) Exposed `GET /api/market-holidays` API endpoint with year and market query filtering; (5) Established Rule 13 in `.agents/AGENTS.md` and `LADDER.md` strictly prohibiting hardcoded dates, years, or heuristics for any logic that can be dynamic in nature; (6) Verified 100% financial integrity invariance pass and clean production build. | Antigravity AI |
| **v5.25.0** | 12-09-2026 | Mutual Funds and NPS 4-Decimal NAV Precision Upgrade: Implemented 4-decimal precision for NAV and unit price metrics across Mutual Funds and National Pension System (NPS) schemes in frontend views, backend APIs, and database records. | Antigravity AI |
| **v5.26.0** | 13-09-2026 | Asynchronous Restore Job Lifecycle, Missed EOD Detection & Code Splitting: (1) Added in-process `restoreJobs` registry with polling endpoint `GET /api/cloud-backups/restore/status` and live status updates in `RestoreBackupModal.jsx`; (2) Implemented `checkMissedEodRebuild()` in `server/index.js` triggering automatic catch-up EOD rebuilds on boot if yesterday's record is missing; (3) Refined dashboard headers to reduce text redundancy ('Trend', 'TOTAL WEALTH'); (4) Backfilled 47,109 historical daily NAV records across 14 NPS schemes in Supabase (`scripts/backfill_nps_navs.mjs`); (5) Implemented `React.lazy()` chunking in `App.jsx` across all 14 portfolio views. | Antigravity AI |
| **v5.27.0** | 13-09-2026 | Secure Authentication Hardening, Direct Table Mutation Invalidation & Backup Safety: (1) Added validated `POST /api/auth/register` with bcrypt password hashing; (2) Responsive branded auth screen with bearer-token API protection; (3) Added 32-character minimum `JWT_SECRET` requirement; (4) Restricted Database Studio to approved tables and editable columns; (5) Automatic cache invalidation after direct transaction, holding, and liability mutations. | Antigravity AI |
| **v5.28.0** | 13-09-2026 | Full System Audit Hardening, Architecture Decoupling, Rule 13 Dynamic Compliance & Performance Optimizations: (1) Architecture Decoupling: Extracted `LoginView.jsx` from `App.jsx` into a dedicated view component, stripping marketing filler text and aligning strictly with Rule 6 fintech minimalism; (2) Background Restore & Job Polling: Decoupled heavy historical EOD rebuild from restore HTTP request into a background job with 5-minute timeout guard, updating `RestoreBackupModal.jsx` with unmount cleanup (`pollRef`, `timerRef`), duplicate restore submission guard, and 5-failure abort threshold; (3) Cross-Table Invalidation: Added automatic `holdings` cache invalidation on transaction updates, deletes, and table updates in `server/index.js`; (4) Local Users Persistence: Supported `users` table via `data/users.json` in `server/db.js` for `select`, `insert`, `update`, and `delete` operations, resolving Supabase schema cache lookup errors; (5) Auth & Security Hardening: Case-insensitive email normalization on login, non-blocking `await bcrypt.compare`, `dotenv` loading in backend entry, and safe 32-char fallback; (6) Vite Bundle Code-Splitting: Configured `manualChunks` in `vite.config.js` separating vendor libraries (React, Recharts, Lucide, Framer Motion), shrinking main bundle from 937 kB to 222 kB; (7) Rule 13 Dynamic Invariance: Replaced static day-of-week gap checks with dynamic multi-asset calendar `isTradingDay(ds, 'NSE')` in `rebuild_portfolio_eod.mjs`, and added dynamic NPS scheme discovery from Supabase `holdings` in `backfill_nps_navs.mjs`; (8) Test Suite Auth Integration: Updated `verify_financial_integrity.mjs` and `verify_all_assets_integrity.mjs` with automated JWT auth token acquisition; (9) 100% test pass on all financial and multi-asset invariance suites with zero errors; (10) Holding Detail Modal & Global Fetch Auth & Quote Status: Fixed 401 'Authentication required' error in `HoldingDetailModal.jsx` by converting `fetchDetail` to `axios.get` and adding a global `window.fetch` interceptor in `ThemeAuthContext.jsx` that automatically attaches `Authorization: Bearer <token>` to all `/api` requests; fixed false-amber badge bug by creating `getQuoteBadgeStatus()` in `dateFormatter.js` to correctly match dates across string formats and recognize both current date and latest completed trading sessions in emerald green. | Antigravity AI |
| **v5.29.0** | 13-09-2026 | Full Production Readiness Architecture, Hardcoding Removal, Modular Server Split & Sub-Component Decomposition: (1) Hardcoding Removal: Built persistent self-healing FX rate store (`data/fx_rates_persistent.json`, `server/services/fxRateStore.js`) with background retry scheduler and historical FX resolution; purged all hardcoded 87.25 and 82.5 values across frontend and backend; removed year-locked FX ladders; removed JWT secret fallback with strict >=32 character fail-fast check in `server/middleware/auth.js`; dynamicized bank name resolution in `portfolioCalculator.js` and removed fabricated loan defaults in `loanEngine.js`; (2) Server Architecture Modularization: Split monolithic 3,837-line `server/index.js` into 13 modular route files in `server/routes/` (`auth.js`, `backup.js`, `calendar.js`, `database.js`, `dividends.js`, `fx.js`, `holdings.js`, `liabilities.js`, `reports.js`, `search.js`, `sips.js`, `summary.js`, `transactions.js`) and middleware `server/middleware/auth.js`; reduced `server/index.js` to 229 lines; organized root directory by moving ingestion scripts to `scripts/ingestion/` and response scratch files to `scratch/`; (3) Resilience & Error Handling: Eliminated all silent empty catch blocks across backend and data loaders with warning logs; implemented Yahoo Finance circuit breaker (`yfCircuitBreaker`) with 5-failure threshold and 30-second cooldown; added retry with exponential backoff for `fetchStockQuote` and `fetchFxRate`; extended calendar lookbacks to 15 days; added Dr. Ambedkar Jayanti (Apr 14) and algorithmic lunar cycle offsets for future years (>2028) in `marketCalendar.js`; migrated SIP execution history to Supabase table `public.sip_history`; integrated `syncAllMissingNavs` self-healing check; (4) Cache, Performance & Component Architecture: Expanded `invalidateCache` in `server/db.js` with cross-table invalidation for liabilities, loan_amortization, pnl_history, sips, and sip_history; async-loaded 38 MB `historical_prices.json` on startup in `historicalPriceStore.js`; unified summary calculation in `server/routes/summary.js` via `computePortfolioValuation()`; deduplicated price refresh engines in `priceEngine.js`; decomposed `HoldingDetailModal.jsx` (1,723 lines) into modular subcomponents in `src/components/holding-detail/` (`HoldingDetailHeader.jsx`, `HoldingMarketStats.jsx`, `HoldingMetricCards.jsx`, `HoldingChartsSection.jsx`, `HoldingTransactionLedger.jsx`, `holdingDetailUtils.jsx`); decomposed `ReportsView.jsx` (2,640 lines) into modular subcomponents in `src/components/reports/` (`reportsConstants.js`, `ReportsTooltips.jsx`, `RankedBarList.jsx`, `CleanBarChartView.jsx`, `CompanyMfBreakdownModal.jsx`); (5) Dynamic Category Registry: Migrated Supabase categories schema with valuation_model, default_currency, default_exchange, price_fetcher, has_dividends; built `server/services/categoryRegistry.js` and exposed `GET /api/categories/registry`; created `server/services/vaultService.js` for Supabase Vault secrets integration; (6) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs` and clean production build. | Antigravity AI |
| **v5.30.0** | 13-09-2026 | Self-Healing Background Engine, Clean Error Handling & Loan Engine Hardening: (1) Background Self-Healing Service: Built `server/services/selfHealingService.js` with `healTransactionFxRates()`, `healMissingHoldingPrices()`, and `runComprehensiveSelfHealing()`, resolving data gaps for past USD transactions and missing quotes automatically; (2) On-Demand Self-Healing API: Exposed `POST /api/self-heal` with authentication in `server/routes/search.js` and wired into server background startup and recurring 10-minute cycles; (3) Clean Error Handling: Audited and purged 100% of silent empty catch blocks across entire repository (scripts and frontend components) with contextual warning logs and graceful fallbacks; (4) Loan Engine Hardening: Removed undefined `DEFAULT_LIABILITY_ID` reference and hardcoded rate/EMI defaults from `addLoanAmortizationEntry` in `server/services/loanEngine.js`; (5) US Stock Past Transaction FX Parity: Updated `server/routes/holdings.js` to dynamically look up exact historical FX rates for past-dated investments; (6) Verification: 100% PASS on `verify_financial_integrity.mjs`, verified `POST /api/self-heal` endpoint, and clean Vite production build. | Antigravity AI |
| **v5.30.1** | 13-09-2026 | Vite Dev Watcher Ignore & Login View UI Cleanup: (1) Configured `server.watch.ignored` in `vite.config.js` to ignore background data directories (`data/`, `server/`, `scripts/`, `Indian Stocks/`, `scratch/`, and data file extensions), completely resolving Vite HMR/full page reload loops and screen flickering; (2) Removed redundant 'Active Session' badge from `LoginView.jsx`; (3) Verified zero input lockup, clean typing, and 100% test pass. | Antigravity AI |
| **v5.30.2** | 13-09-2026 | Synchronous Auth Header Propagation & Permanent Login Disconnect Fix: (1) Added global Axios request interceptor in `ThemeAuthContext.jsx` that dynamically attaches `Authorization: Bearer <token>` from `localStorage` on 100% of outgoing requests; (2) Synchronously configured `axios.defaults.headers.common.Authorization` on login and logout, eliminating child component `useEffect` mount race conditions on initial login and permanently resolving transient 401 'Database connection failed: Authentication required' errors; (3) Added Rules 14, 15, and 16 to `.agents/AGENTS.md` establishing mandatory client auth interceptor synchronization, Vite watcher persistence isolation, and automated background self-healing standards; (4) Verified 100% pass across financial integrity test suites and clean Vite production build. | Antigravity AI |
| **v5.30.3** | 13-09-2026 | Runtime Cache Untracking & Cross-Platform Vite Watcher Hardening: (1) Untracked runtime cache `data/fx_rates_persistent.json` from git and added to `.gitignore`, preventing polling-cycle file updates from dirtying git index and appearing on the commit page; (2) Hardened `vite.config.js` `server.watch.ignored` with a cross-platform matcher function and RegExp to guarantee Windows backslash path separators are completely ignored by the Chokidar file watcher. | Antigravity AI |
| **v5.30.4** | 14-09-2026 | Holding Detail Chart Tooltip & Dividend Deduplication Fix: (1) Holding Detail Actual Chart Tooltip Formatting: Fixed `undefined @ ₹0.00` in `ActualChartTooltip` (`holdingDetailUtils.jsx`) for BUY, SELL, BONUS, and DIVIDEND events by properly resolving `quantity`/`qty`, `price`/`priceUSD`/`priceINR`, and formatting DIVIDEND events cleanly as credited payouts (`+₹X.XX` or `+$X.XX`); (2) Timeline Day Events Enrichment: Enriched `dayEvents` in `server/routes/holdings.js` with standard properties (`quantity`, `price`, `amount`) for equity, MF, and NPS timeline series; (3) Transaction Ledger Dividend Deduplication: Resolved duplicate dividend entries in the transaction ledger table by deduplicating dividends between `transactions` and `dividends` tables in `/api/holding/:id/detail`; (4) Two-Way CRUD Synchronization: Enhanced `server/routes/transactions.js` with `div-` prefixed ID routing and synchronized two-way updates/deletes between `transactions` and `dividends` tables; (5) Verified 100% PASS on `verify_financial_integrity.mjs` and clean Vite production build. | OpenRouter (Delegated) / Antigravity AI |
| **v5.30.5** | 14-09-2026 | Universal Net Realized & Total P&L Formula Alignment & Full Currency Precision: (1) Updated Realized and Total P&L calculation everywhere across the application to strictly enforce the net formula: `Sell - Buy - Charges + Dividends`; (2) In `server/routes/holdings.js`, updated FIFO lot matching for `SELL` transactions to deduct both transaction sell charges and proportional buy charges from matched lots before adding credited dividends; (3) In `server/services/recalculator.js`, updated FIFO matching in `recalculateHoldingState` to deduct proportional buy charges of sold lots in net capital gain (`proceeds - costOfSoldLots - buyChargesOfSoldLots`); (4) Executed batch recalculation across all 398 holdings in Supabase, synchronizing `holdings.realized_pnl` with the net formula; (5) Verified exact parity for Aarti Pharmalabs Limited (`AARTIPHARM`), displaying exact `₹26,819.82` (Gross Sell ₹53,486.60 - Buy ₹27,132.65 - Charges ₹103.13 + Dividends ₹569.00); (6) Universal XIRR Net Cashflow Alignment: Updated category and holding XIRRs in `server/routes/summary.js` to strictly account for transaction charges on cash outflows and inflows, and eliminated duplicate proceeds push for closed holdings with sell transactions; (7) Full Unabbreviated Currency Precision: Removed all `L` and `Cr` abbreviations from `fmtINR` and `formatAxisValue` in `holdingDetailUtils.jsx` and `EpfView.jsx`, ensuring full rupee amounts (e.g. `₹5,89,123.45` and `₹5,97,280.32`) are displayed across all KPI metric cards; (8) Verified 100% PASS on `verify_financial_integrity.mjs` and clean Vite production build. | OpenRouter (Delegated) / Antigravity AI |
| **v5.30.6** | 14-09-2026 | Dividend Inversion Correction Across Indian Stocks: (1) Diagnosed root cause of inverted dividend values (e.g. Acme Solar Holdings Ltd `ACMESOLAR` showing `₹235.51` instead of `₹170.40`) caused by ingestion selecting `Cost Per Share` (market quote) rather than `Shares Owned` (dividend payout); (2) Fixed `scripts/ingestion/load_all_indian_stocks.mjs` to prioritize `Shares Owned`; (3) Executed one-off migration updating all 90 inverted dividend records in `dividends` and `transactions` Supabase tables, and cleanly removed the one-off script; (4) Recalculated holding states across all 66 affected holdings via `recalculateHoldingState`; (5) Verified `ACMESOLAR` 02-05-2025 payout is now exact `₹170.40` and total dividends `₹270.40`; (6) Verified 100% PASS on `verify_financial_integrity.mjs` and clean Vite production build. | Antigravity AI |
| **v5.30.7** | 14-09-2026 | Universal Table and Holding Detail Modal P&L and Value Synchronization: (1) Resolved discrepancy between closed table rows and holding detail modal by including credited dividends in `totalRealizedPnl` in `recalculateHoldingState` (`server/services/recalculator.js`), strictly enforcing `Sell - Buy - Charges + Dividends` in `holdings.realized_pnl`; (2) Recalculated all 398 holdings in Supabase to synchronize stored `realized_pnl`; (3) Enriched `GET /api/holdings` with true execution metrics: `sold_qty`, `avg_sell_price`, `redeemed_value`, `gross_redeemed`, and `total_dividends`; (4) Updated `server/routes/summary.js` to consume `holdings.realized_pnl` directly without double-counting dividends; (5) Updated closed position tables across `IndianStocksView.jsx`, `UsStocksView.jsx`, `MutualFundsView.jsx`, and `NpsView.jsx` to render enriched execution metrics; (6) Verified exact cent-level parity for `ACMESOLAR` (Table Realized P&L = Detail Modal Realized P&L = `₹8,091.76`, Table Redeemed Value = Detail Modal Total Redeemed = `₹5,97,100.30`, Table Avg Sell Price = `₹214.65`); (7) Verified 100% PASS on `verify_financial_integrity.mjs` and clean Vite production build. | Antigravity AI |
| **v5.30.8** | 14-09-2026 | Current Cost Buy Charges Inclusion: (1) Updated `recalculateHoldingState` in `server/services/recalculator.js` to include active proportional buy charges in `avg_buy_price` and `totalBuyAmount`, effectively raising the cost basis to account for fees; (2) Updated `costBasisINR` computation in `server/routes/holdings.js` to properly reflect active lot charges; (3) Reconciled the entire database applying these new cost basis metrics, ensuring "Current Cost" correctly reflects the actual invested amount including charges across Indian Stocks, US Stocks, Mutual Funds, and NPS; (4) Verified 100% PASS on `verify_financial_integrity.mjs`. | Antigravity AI |
| **v5.31.0** | 14-09-2026 | Universal Project-Wide Table Header and Column Freezing (Sticky Tables): (1) Freezing Architecture: Configured dual-axis scrolling (`overflow-x-auto overflow-y-auto max-h-[...] custom-scrollbar`) on table containers so vertical and horizontal stickiness execute synchronously; (2) Header & First Column Stickiness: Frozen table header rows (`sticky top-0 z-30 bg-slate-900`) on vertical scroll and frozen first columns (instrument name, symbol, scheme, asset class, or date) (`sticky left-0 z-20 bg-slate-900/95`) on horizontal scroll; (3) Corner Cell Layering: Frozen top-left corner cells with elevated priority (`sticky left-0 top-0 z-40 bg-slate-900 border-r border-slate-800`); (4) Universal Coverage Across All Project Tables: Implemented sticky headers and first columns across Indian Equities, US Equities, Mutual Funds, NPS, Overview Asset Classes, Dividends, HoldingsTable, Holding Detail Transaction Ledgers (including split/bonus and inline edit modes), Asset Dividend Distribution Ledger, Loan Amortization Schedule, Database Studio, Database Viewer, Company MF Breakdown Modal, FX Rate History Modal, and all 4 Reports drill-down and performance tables (including sticky footers); (5) Multi-Theme Fidelity: Added `.reports-table-sticky-head` and `.reports-table-sticky-cell` in `src/index.css` ensuring 100% theme compatibility across dark and light modes; (6) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, clean Vite production build (`npm run build`), and verified backend port 5000 health. | Antigravity AI |
| **v5.32.0** | 14-09-2026 | Editable Charges & Asset-Class Specific Column Parity in Transaction Add and Amend Workflows: (1) Add Transaction Drawer: Added editable `Charges` input in `HoldingTransactionLedger.jsx` across all asset classes with dynamic currency symbol (`₹` / `$`); (2) US Equities FX Override: Added editable `USD/INR Rate (₹)` in Add Transaction drawer and synced with holding's current FX rate; (3) Mutual Funds Stamp Duty Auto-Calculation: Automatically computes 0.015% stamp duty when BUY price or quantity changes while leaving the field completely editable for user adjustments; (4) Total Amount Net Preview: Displays net investment preview `(qty * price) + charges` on BUY and `Math.max(0, (qty * price) - charges)` on SELL; (5) Transaction Amendment Inline Edit Parity: Fully integrated editable `charges`, `fx_rate` (US stocks), and asset-class specific transaction types across Indian/US Equity, Mutual Funds, NPS, Bank, EPF, and Debt instruments; (6) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified port 5000 Express backend daemon health, and clean Vite production build. | Antigravity AI |
| **v5.33.0** | 14-09-2026 | Modern Theme-Adaptive Custom DatePicker Architecture: (1) Eliminated Clunky Browser Date Pickers: Replaced all native `<input type="date">` browser pickers project-wide with a modern, stylish, theme-matched custom component (`DatePicker.jsx`); (2) Universal Theme Parity: Built with `.modal-surface`, `.reports-card`, and project design tokens (`--bg-card`, `--border-color`, `--text-primary`, `--accent-emerald`), automatically adapting across Obsidian Dark, Midnight Blue, Sunset Rose, Clean Light, Warm Sand, and Nordic Light themes; (3) Effortless Past & Future Date Navigation: Integrated 3 interactive view modes (Days Grid, Month 3x4 Grid, Decade 12-Year Grid) with `<<` and `>>` fast decade jumps, `Today`, `Yesterday`, `-1Y`, `+1Y`, and `Clear` instant shortcut actions; (4) Viewport-Safe React Portal Architecture: Renders popover via `createPortal(..., document.body)` with anchored coordinates, scroll/resize tracking, and viewport overflow protection to prevent clipping by parent drawers or table overflow; (5) Project-Wide Integration: Deployed across Holding Transaction Ledgers (Add Drawer and Inline Amends), Add Investment View, Add Dividend Modal, Dividend Detail Ledger, Loan Amortization Section, SIP Manager, Chart Range Selector, and Calendar Heatmap; (6) Verification: 100% PASS on `verify_financial_integrity.mjs`, verified port 5000 Express backend daemon health, and clean Vite production build. | Antigravity AI |
| **v5.33.1** | 14-09-2026 | DatePicker Usability Upgrade — Direct Month/Year Selectors, Editable Typing & Non-Premature Dismissal: (1) Resolved Premature Dismissal: Date selection now keeps the calendar popover open for immediate verification and adjustment rather than snapping shut on click; (2) Added Dedicated 'Done' Button: Users confirm selection with a prominent emerald 'Done' action button or double-click; (3) Interactive Header Selectors: Replaced static text labels with intuitive Month (`January`–`December`) and Year (`1986`–`2041`) dropdown selects in the calendar header for instant 1-click navigation to any past or future period; (4) Direct Input Text Editability: Converted trigger into an interactive text input allowing direct `DD-MM-YYYY` typing with real-time calendar synchronization; (5) Verified end-to-end via automated browser subagent, 100% PASS on financial integrity test suite, and clean production build. | Antigravity AI |
| **v5.33.2** | 14-09-2026 | DatePicker Uniform Box Styling & ChartRangeSelector Portal Event Containment: (1) Uniform Input Background: Eliminated browser user-agent white rectangular strip on text inputs in light and warm themes by applying `style={{ backgroundColor: 'inherit', color: 'inherit' }}` and rendering direct border and padding on the input itself, achieving 100% aesthetic uniformity with adjacent form boxes; (2) Chart Range Selector Portal Protection: Resolved issue where clicking the calendar in `ChartRangeSelector` immediately closed the popover by adding `data-datepicker-portal="true"` guard in `handleClickOutside`, preventing portal clicks from prematurely dismissing the parent range selector; (3) Chart Range Selector Theme Alignment: Refactored `ChartRangeSelector` from hardcoded slate-900 backgrounds to theme design tokens (`--bg-surface`, `--border-color`, `--text-primary`), eliminating white box on number inputs in light themes; (4) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified port 5000 backend daemon health, and clean production build. | Antigravity AI |
| **v5.33.4** | 14-09-2026 | Transaction Notes Cleanup & Automated Historical Daily Price Population: (1) Transaction Notes Cleanup: Removed auto-generated default text (`Added via Holding Detail Modal` and `${txType} ${txQty} units of ${name}...`) from transaction saving workflows in `HoldingDetailModal.jsx` and `server/routes/holdings.js`, keeping notes completely blank unless explicitly typed by the user; (2) Automated Historical Price Engine: Built `ensureHistoricalPricesForSymbol(symbol, category, startDate)` in `server/services/historicalPriceStore.js` with atomic file persistence, auto-fetching historical daily prices across Indian stocks (NSE/BSE), US stocks, Mutual Funds (AMFI), and NPS schemes; (3) Dynamic Detail & Ingestion Integration: Wired automated price fetching into `POST /api/add-investment` and `GET /api/holding/:id/detail`, ensuring any new or past-dated entry automatically populates historical daily quotes so holding detail charts immediately track true daily trajectories instead of flat lines; (4) EOD Rebuild & Verification: Upgraded `rebuild_portfolio_eod.mjs` with multi-month lookbacks, re-synchronized Supabase `pnl_history`, passed 100% on `verify_financial_integrity.mjs`, and verified clean production build. | Antigravity AI |
| **v5.33.5** | 14-09-2026 | Holding Detail Add Transaction Corporate Actions Upgrade (Dividend, Split & Bonus): (1) Dynamic Corporate Action Fields: Re-engineered the 'Add Transaction' drawer in `HoldingTransactionLedger.jsx` to dynamically adapt input controls based on transaction type; (2) Dividend Workflow: Dedicated dividend entry with Payment Date, Dividend Payout Amount input, optional USD/INR rate for US stocks, and direct integration with Supabase `dividends` table via `/api/add-investment`; (3) Stock Split Workflow: Dedicated split controls with Split Old Ratio, Split New Ratio, and live interactive position projection banner calculating new share quantity and diluted average cost basis; (4) Bonus Shares Workflow: Dedicated bonus entry with Bonus Shares Credited, read-only Cost Basis (`₹0.00 / $0.00 Free Bonus Shares`), optional charges, and live post-bonus position projection banner; (5) Cache & State Recalculation: Integrated immediate in-memory cache invalidation, automated holding state recalculation (`recalculateHoldingState`), and past-date EOD rebuilds; (6) Verification: 100% PASS on `verify_financial_integrity.mjs`, clean production build, and active port 5000 backend daemon. | Antigravity AI |
| **v5.33.6** | 14-09-2026 | Holding Detail Tracker Chart Corporate Action & Split Scale Harmonization: (1) Root Cause Diagnosis: Resolved issue where the Tracker Chart in `HoldingDetailModal.jsx` artificially plotted Market Value below Cost Basis for holdings with historical splits (e.g. Pondy Oxides & Chemicals Ltd `POCL` 5:2 split on 21-07-2026); Yahoo Finance retroactively divided historical closing quotes before the split date by 2.5x while preceding ledger transactions were executed in real unadjusted shares, resulting in an artificial 60% valuation depression despite actual realized profits of +₹42,480.83; (2) Dynamic Corporate Action Checkpoints: Built trade scale checkpoint engine in `server/routes/holdings.js` comparing transaction execution prices with upstream historical quotes to detect split scale factors (e.g. 2.5x) and dynamically scale historical quote series during pre-split holding intervals; (3) Dense Timeline BONUS and SPLIT Support: Added explicit handling for `BONUS` (free share dilution) and `SPLIT` (ratio scaling) in the daily simulation loop; (4) Verification: Re-verified POCL timeline (Market Value accurately peaks above ₹2,04,000 against ₹1,43,000 cost basis during the profitable holding run), 100% PASS on `verify_financial_integrity.mjs`, and clean Vite production build. | Antigravity AI |
| **v5.33.7** | 14-09-2026 | Total Bought and Current Cost Parity & Charge Accounting Alignment: (1) Discrepancy Diagnosis: Resolved issue where `Total Bought` (₹1,20,415.77) differed from `Current Cost` (₹1,20,363.76) by ₹52.01 when no shares had been sold for `QPOWER`; `server/routes/holdings.js` had added `charges` on top of `tx.total_amount` when `tx.total_amount` had already been saved inclusive of charges, double-counting fees in `totalInvested`; (2) Canonical Principal + Charges Computation: Standardized `totalInvested` and `totalRedeemed` to strictly compute from `(qty * price) + charges` for purchases and `Math.max(0, (qty * price) - charges)` for sales, ensuring `Total Bought` strictly equals `Current Cost` down to the exact cent whenever zero shares have been sold; (3) Verification: 100% PASS on `verify_financial_integrity.mjs`, verified port 5000 Express backend daemon, and clean Vite production build. | Antigravity AI |
| **v5.33.8** | 14-09-2026 | Full-Row Corporate Action Highlight & Split/Bonus Edit/Delete Recalculation Engine: (1) Full-Row Highlight Alignment: Fixed column span misalignment where split/bonus table rows omitted the Actions column and left a blank un-highlighted cell on the right; corrected column spans and styled Date, Middle, and Actions cells uniformly across Light and Dark themes for both Stock Split (purple) and Bonus Issue (amber); (2) Edit & Delete Actions: Added Edit (`Edit3`) and Delete (`Trash2`) buttons to corporate action rows in `HoldingTransactionLedger.jsx`; (3) Dedicated Inline Edit Mode: Built inline editing for Stock Splits (ratio inputs `splitOld:splitNew` and notes) and Bonus Issues (shares, charges, notes); (4) Position Recalculation on Edit/Delete: Upgraded `recalculateHoldingState` in `server/services/recalculator.js` to dynamically replay user-added splits and scale open lots by ratio multiplier upon edit/delete, and updated `PUT /transactions/:id` to compute adjusted pre-split quantities; (5) Delete Safety Confirmation: Integrated transaction delete confirmation modal displaying exact split/bonus details before removal; (6) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, active port 5000 daemon, and clean Vite production build. | Antigravity AI |
| **v5.33.9** | 14-09-2026 | Post-Corporate Action Open Shares Synchronization & Prominent Share Badge Architecture: (1) Dynamic Open Shares & Lot Average Cost Recalculation: Fixed holding position persistence where `recalculateHoldingState` previously ignored user-added splits during chronological replay; upgraded `recalculateHoldingState` and `/api/holding/:id/detail` to dynamically scale FIFO open lots and calculate exact post-split and post-bonus shares (`TDPOWERSYS`: 345 -> 690 shares, avg price ₹662.47 -> ₹331.24) across both portfolio table views and holding detail modal; (2) Prominent Shares Header Badge: Replaced tiny 10px muted text in `HoldingDetailHeader.jsx` with a high-contrast, bold typography pill badge (`Shares: 690` / `Units: X`) across all themes; (3) Audited all 16 holdings with splits and bonuses in portfolio; (4) Verification: 100% PASS on `verify_financial_integrity.mjs`, verified active port 5000 Express daemon, and clean Vite production build. | Antigravity AI |
| **v5.33.10** | 14-09-2026 | Dynamic Reversible Open Shares Recalculation on Stock Split & Seamless Chart Alignment: (1) Reversible Open Shares Recalculation Engine: Built `server/services/splitManager.js` (`applySplitToHolding` and `revertSplitFromHolding`) simulating FIFO lots up to the split date and recalculating all open buy transactions with post-split quantities and prices (`quantity * splitMultiplier`, `price / splitMultiplier`) while embedding non-destructive metadata tags (`[Split orig: Q@P]`) for complete reversibility; (2) Reversibility on Amend & Delete: Wired `DELETE /transactions/:id` and `PUT /transactions/:id` to automatically restore preceding open lots to their original pre-split numbers if a split transaction is deleted or re-apply with new ratios/dates if amended; (3) Clean Split Marker: Split corporate action transactions are saved with `quantity: 0`, preventing double-counting while displaying clean corporate action details in the transaction ledger; (4) Seamless Continuous Chart Timeline: Upgraded `/api/holding/:id/detail` timeline generation to detect market price ex-dates and maintain continuous pre-split and post-split valuations, eliminating artificial 50% valuation cliffs and troughs (`TDPOWERSYS`: 458,919.32 on May 27 connects seamlessly to 458,919.00 on May 28); (5) TDPOWERSYS Open Shares Migration: Recalculated TDPOWERSYS open lots to post-split numbers (`448 @ ₹400`, `2 @ ₹399.98`, `6 @ ₹646.30`, and 2025 open lots); (6) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified port 5000 Express backend daemon health, and clean Vite production build. | Antigravity AI |
| **v5.33.11** | 14-09-2026 | Pre-Split Historical Timeline Isolation & Dynamic Split Lot Scaling: (1) Pre-Split Historical Preservation: Restored original excel-ingested 2025 transactions for `TDPOWERSYS`, ensuring historical transactions prior to the split cycle retain their true executed prices (~₹366 - ₹502); (2) Dynamic Split Lot Harmonization: Fixed detail route lot calculation where unscaled pre-split lots combined with corporate action split increments (`SPLIT qty: 117`) maintain exact 708 shares and ₹341.33 average price; (3) Elimination of 2025 Valuation Trough: Fixed trade scale checkpoint timeline loop in `server/routes/holdings.js` so that historical quote scaling applies strictly to the appropriate post-split interval, eliminating the artificial 50% market value depression in 2025 (`2025-05-31`: ₹3,04,173.52 Market Value vs ₹1,85,932.56 Cost Basis in solid profit); (4) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, active port 5000 daemon, and clean Vite production build. | Antigravity AI |
| **v5.33.12** | 14-09-2026 | Asset Dividend Detail Modal Hook Import Resolution: Resolved `ReferenceError: useEffect is not defined` runtime crash caught by `ErrorBoundary` in `AssetDividendDetailModal.jsx` by adding missing `useEffect` to React named imports; verified complete zero-missing-hook scan across all components in `src/`, 100% financial integrity test pass, and clean Vite production build. | Antigravity AI |
| **v5.33.13** | 14-09-2026 | Universal Two-Way Dividend Synchronization, Orphan Healing & Cross-Page Parity: (1) Root Cause Diagnosis: Diagnosed discrepancy where Sheela Foam Limited (`SFL`) dividend addition/deletion failed to reflect across the Dividends view and Holding Detail Modal KPI card; previously, `HoldingMetricCards.jsx` and `DividendsView.jsx` read exclusively from the `dividends` table while the Transaction Ledger read both `dividends` and `transactions`, causing orphaned records when dividends were modified; an errant ₹594 entry was also assigned to SHRIRAMFIN under the hood instead of SFL; (2) Two-Way Synchronization Engine: Built `syncDividendsTwoWay()` in `server/services/dividendSync.js` bi-directionally syncing `dividends` and `transactions` (`type = 'DIVIDEND'`) and healing orphaned records across both tables; wired into `server/services/selfHealingService.js`; (3) Database Reconciliation: Purged the errant ₹594 entry, persisted SFL's verified ₹15 dividend across both tables, and recalculated holding states for `SFL` and `SHRIRAMFIN`; (4) Universal Route & Recalculator Parity: Updated `server/routes/holdings.js` (`/holding/:id/detail` and `POST /api/add-investment`), `server/routes/transactions.js` (`DELETE` and `PUT`), `server/routes/dividends.js` (`GET`, `PUT`, `DELETE`), and `server/services/recalculator.js` to synchronously mutate both tables, recalculate holding state, and invalidate in-memory caches on every dividend add, edit, or delete; (5) Verification: Confirmed exact parity across Holding Detail Modal (Dividends KPI = `₹15.00`, 1 payout), Dividends Hub (`SFL` = `₹15.00`), 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified port 5000 Express backend daemon health, and clean Vite production build. | Antigravity AI |
| **v5.34.0** | 14-09-2026 | Eradication of Ad-Hoc Scripts, Rule 17 & Canonical Domain Service Architecture: (1) Eradication of Standalone Scripts: Permanently removed `server/services/dividendSync.js` and `server/services/splitManager.js`, eliminating disconnected background reconciliation loops and ad-hoc scripts that create technical debt and maintenance fragility; (2) Canonical Dividend Domain Service: Built `server/services/dividendService.js` as the single authoritative domain controller for all dividend mutations and retrievals (`recordDividend`, `updateDividend`, `deleteDividend`, `deleteSchemeDividends`, `getHoldingDividends`, `getAllDividends`), guaranteeing atomic dual-writes/deletes across both `dividends` and `transactions` tables, cache invalidation, holding recalculation, and 100% cross-view parity on write; (3) Canonical Corporate Actions Service: Built `server/services/corporateActionService.js` orchestrating Stock Splits and Bonus Issues with integrated dependency mapping (`applyStockSplit`, `revertStockSplit`, `deleteStockSplit`, `updateStockSplit`), scaling preceding open buy lots for UI splits to match split-adjusted market price feeds, restoring open lots cleanly on delete/amend, triggering `recalculateHoldingState`, and running past EOD rebuilds atomically; (4) Established Rule 17 in `.agents/AGENTS.md` and `LADDER.md` strictly prohibiting ad-hoc standalone sync scripts and mandating single atomic domain service contracts; (5) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, clean Vite production build (`npm run build`), and active port 5000 Express daemon. | Antigravity AI |
| **v5.34.1** | 15-09-2026 | High-Precision Fractional Share Fix & Intra-Day Stock Pricing Parity: (1) 9-Decimal Database Expansion: Executed Supabase MCP migration to expand `quantity` column precision in `transactions` and `holdings` from `numeric(18, 6)` to `numeric(30, 9)` to fully support exact US Stock fractions; (2) Engine Truncation Fix: Patched `recalculator.js` to prevent forceful 4-decimal truncation of running lot quantities, ensuring the backend cache stores the precise 9-digit values; (3) Clean UI Formatting: Replaced trailing-zero `.toFixed(9)` calls in `UsStocksView.jsx` with localized `toLocaleString` for seamless zero-trimming; (4) Eliminated Stock Auto-Pricing: Disabled the intrusive EOD auto-population logic in `HoldingDetailModal.jsx` for equity stocks to allow manual intra-day execution prices, explicitly restricting the smart EOD auto-price logic exclusively to Mutual Funds and NPS schemes; (5) Script Cleanup: Purged temporary one-off migration scripts to keep Git clean. | Antigravity AI |
| **v5.35.0** | 16-09-2026 | Supabase Egress Elimination, 24-Hour Write-Through Caching & Intraday In-Memory Protection: (1) Diagnosed and resolved 4.5 GB cloud egress crisis: identified 8,565 daily full table scans of 11,408 transactions and 6,158 daytime holding PATCH updates caused by a 60s TTL and a destructive cache cascade (`if (sTable === 'holdings') dbCache.delete('transactions')`); (2) Write-Through In-Memory Cache: Upgraded `server/db.js` with 24-hour TTL and write-through cache mutations on `insert`, `update`, and `delete`, ensuring user mutations update RAM in-place without invalidating or re-downloading cloud tables; (3) PostgREST Minimal Representation: Omitted `.select()` on Supabase updates, returning empty `204 No Content` headers with 0 response bytes; (4) Intraday Live Ticks Strictly In-Memory: Modified `refreshHoldingsPrices` and `server/index.js` to confine daytime quotes to `liveQuoteCache` and RAM, persisting official closing quotes strictly once at EOD (06:30 PM IST / market close) via `persistHoldingClosingPrices()`; (5) Scoped NPS Ingestion & EOD Queries: Scoped Protean NAV persistence in `priceEngine.js` and queries in `rebuild_portfolio_eod.mjs` strictly to user-held scheme codes (`.in('scheme_code', heldNpsCodes)`) and baseline dates, reducing row transfers from 53,411 rows down to ~500 (99% bandwidth reduction); (6) Codified Rule 18 (Zero-Cascade Write-Through Caching & Intraday Egress Protection Protocol) in `.agents/AGENTS.md` and `LADDER.md`; (7) Projected remaining monthly egress usage reduced to < 35 MB total, guaranteeing project stays safely below the 5 GB free allowance. | Antigravity AI |
| **v5.36.0** | 19-09-2026 | Emergency Supabase Egress Lockdown, Market Hours Gating & Zero-Egress Disk Snapshot Cache: (1) Diagnosed 60 MB/day egress leak: uncached `asset_metadata` queries on every `/api/holdings` call, 2-second unconstrained live ticker loops outside market hours, recurring 10-minute self-healing cache drops, and unconstrained weekend EOD rebuild triggers; (2) In-Memory Metadata Caching: Cached `asset_metadata` and `mutual_fund_holdings` via `db.select` in `holdings.js` and `reports.js`, and added `sips`, `sip_history`, `asset_metadata`, and `mutual_fund_holdings` to `warmCache()`; (3) Market-Hours Gated Live Ticker: Re-engineered live ticker in `server/index.js` to run on a 60-second cycle (30x reduction) and strictly gate execution via `isAnyMarketOpen()`, pausing price polling when markets are closed (nights, weekends, holidays); (4) Self-Healing Decoupling: Removed `runComprehensiveSelfHealing()` from the 10-minute loop, scheduling it strictly once on boot (after cache pre-warming) and once daily at midnight (00:05 AM IST); (5) NPS Sync & Non-Trading Guards: Added in-memory `npsSyncStatusCache` and non-trading day checks in `priceEngine.js`, eliminating redundant Supabase queries when NAVs are already synced or on weekends; (6) Incremental NPS Historical Scans: Optimized `fetchNpsHistoricalNav` in `priceEngine.js` to check local cache and only query missing recent dates from Supabase; (7) Restart Protection Disk Snapshot: Implemented local disk snapshot cache (`data/db_cache_snapshot.json`) in `server/db.js` with write-through saves and instant startup restoration, completely eliminating cold-start cloud egress on server restarts; (8) Weekend EOD Rebuild Guard: Updated `checkMissedEodRebuild` in `server/index.js` to check cached `pnl_history` and compare against `lastTradingDay` rather than `yesterdayStr`, preventing false weekend rebuild loops; (9) Cached Operations in Sips and Recalculator: Replaced direct Supabase queries in `sipEngine.js` and `recalculator.js` with cached `db.select` and write-through updates; (10) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, active port 5000 Express backend daemon responding HTTP 200, and clean production build (`npm run build`). | Gemini 3.8 Flash |
| **v5.36.1** | 19-09-2026 | Comprehensive Egress Elimination Across All Endpoints & Backup Scoping: (1) Holding Detail Route Caching: Replaced raw `supabase.from('transactions')` and `supabase.from('dividends')` in `server/routes/holdings.js` (`/holding/:holdingId/detail`) with in-memory `db.select()` queries, eliminating rapid egress spikes when viewing holding details; (2) Summary & Calendar Route Caching: Converted direct `pnl_history` Supabase queries in `server/routes/summary.js` and `server/routes/calendar.js` to read from pre-warmed in-memory `db.select('pnl_history')`, halting the 15-second background query drain; (3) SIP Route Caching: Replaced direct `sips` table fetch in `server/routes/sips.js` with in-memory `db.select('sips')`; (4) Backup Scoping: Scoped `fetchAllRows` in `scripts/backup_manager.mjs` for `nps_daily_navs` strictly to user-held scheme codes, eliminating the 53,000-row (~8 MB) general market dump on every backup; (5) Frontend Poll Relaxation: Relaxed periodic dashboard polling in `App.jsx` from 15s to 30s and holding modal polling in `HoldingDetailModal.jsx` from 3s to 30s, halving unnecessary network round-trips; (6) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified active port 5000 Express backend daemon, and clean Vite production build. | Gemini 3.8 Flash |
| **v5.36.2** | 19-09-2026 | Codification of Architectural Governance & Rules 19 to 24 in AGENTS.md: (1) Rule 19 Codified: Zero-uncached reads across all API routes and client polling minimums (>= 30s); (2) Rule 20 Codified: Cloud CI/CD runner egress quarantine, dual-execution ban, and emergency quota isolation; (3) Rule 21 Codified: Zero-egress local disk snapshot persistence and cold-boot cloud immunity; (4) Rule 22 Codified: Dynamic market-hours gating and non-trading day runner invariance; (5) Rule 23 Codified: Backup projection scoping and ban on unconstrained multi-scheme table scans; (6) Rule 24 Codified: Autonomous Architectural Integrity & Continuous Rule Governance Protocol, formally authorizing agents to self-codify permanent workspace guardrails whenever systemic design flaws or resource leaks are diagnosed; (7) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified active port 5000 Express backend daemon, and clean Vite production build. | Gemini 3.8 Flash |
| **v5.37.0** | 19-09-2026 | Data Corrections, Universal Real-Time UI Synchronization & Search Deduplication: (1) Standardized all REDEEM/REDEMPTION transactions to SELL across database, backend endpoints, and frontend dropdowns with unified FIFO lot matching and realized P&L calculations; (2) Re-linked 01-Sep-2026 EMI transaction to Housing Loan, deleted rogue duplicate liability row, set outstanding balance to ₹44,64,447, and mapped loan amortization closing balances into transaction detail records to eliminate ₹0.00 running balances; (3) Eliminated divide-y and unwanted row borders across HoldingTransactionLedger, LoanAmortizationSection, and AssetDividendDetailModal for uniform borderless detail records; (4) Enabled universal real-time UI refresh by passing onRefresh to HoldingDetailModal in App.jsx, wiring activeHolding into HoldingDetailHeader, and synchronizing DividendsView on holdings updates; (5) Automated historical FX rate lookup when selecting or editing US stock dates in HoldingTransactionLedger; (6) Reconciled Arista (ANET) 1:4 stock split with 9-decimal precision, restored pre-split executed buy prices, and scaled open lots to 24.5516 shares with exact metric parity; (7) Deduplicated Indian stock search results to prioritize NSE, fallback to BSE only if absent, and strip exchange tags for clean single-entry stock prompts. | Antigravity AI |
| **v5.37.1** | 19-09-2026 | Weekend Market Invariance & Zero-Egress Calendar Alignment: (1) Enforced Rule 5 strictly in `server/routes/calendar.js` and `server/routes/summary.js`, carrying forward Friday's finalized closing valuations for equity, MF, and NPS asset valuations on non-trading days with 0.00 daily movement; (2) Enforced strict trade/transaction date parity (`t.date`) across calendar and summary routes rather than system entry timestamps; (3) Synchronized Friday 18-Sep historical EOD log in `data/portfolio_eod_logs.json` and in-memory cache with post-split holding states locally with 0 bytes of Supabase egress. | Antigravity AI |
| **v5.37.2** | 19-09-2026 | MF Composition Scrip Breakdown Modal Viewport Centering & Multi-Theme Fidelity: (1) Viewport-Centering via React Portal: Wrapped `CompanyMfBreakdownModal` with `createPortal(..., document.body)` adhering to Rule 7, eliminating relative positioning conflicts from parent `AnimatedPage` transforms and ensuring 100% true viewport centering; (2) Theme Parity & Standard Design Tokens: Replaced hardcoded `bg-slate-900/60` search inputs and cards with project theme tokens `reports-subcard`, `border border-inherit`, and `reports-card`, ensuring complete high-contrast light and dark theme compatibility (Obsidian Dark, Midnight Blue, Sunset Rose, Clean Light, Warm Sand, Nordic Light); (3) Backdrop, Scroll & Keyboard Accessibility: Standardized backdrop to `bg-slate-950/75 backdrop-blur-md` with backdrop click-to-close, added Escape key dismissal listener, and configured flexible internal table scroll containment (`max-h-[380px] custom-scrollbar`) preventing viewport overflow. | Gemini 3.8 Flash |
| **v5.38.0** | 20-09-2026 | Indian Equity NSE/BSE MAX Quote Engine Hardening & First Column Dual Name/Ticker Sorting: (1) NSE/BSE MAX Quote Comparison & 404 Circuit Breaker Hardening: Diagnosed why ANANTRAJ and other Indian stocks showed NSE prices instead of higher BSE prices; in `server/services/priceEngine.js`, `fetchStockQuote` treated HTTP 404 (stocks not listed on BSE or missing `.BO` tickers on Yahoo Finance) as service failures and tripped `yfCircuitBreaker` into `OPEN` state after 5 failures, aborting subsequent live quotes across batches; patched `fetchStockQuote` to treat 404 as expected absence without retrying or tripping the circuit breaker, and updated `refreshHoldingsPrices` and `server/routes/holdings.js` to attach `nse_price` and `bse_price` to holding responses; (2) Zero-Egress Local Cache Update: Updated ANANTRAJ in local disk snapshot `data/db_cache_snapshot.json` to lock BSE closing quote (₹606.25 vs ₹606.15) with 0 bytes of Supabase egress; (3) First Column Dual Name/Ticker Sorting: Added interactive dual-target sorting (`Name` / `Ticker` or `Name` / `Code`) to the first column table header across all 4 asset classes (`IndianStocksView.jsx`, `UsStocksView.jsx`, `MutualFundsView.jsx`, `NpsView.jsx`) for both Active and Closed portfolio views; (4) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, active port 5000 Express daemon, and clean Vite production build. | Gemini 3.8 Flash |
| **v5.40.0** | 20-09-2026 | Corporate Action FIFO Realized P&L & Timeline Chart Harmonization (FCL Resolution): (1) Resolved `-34,327.14` Realized P&L Bug: In `server/routes/holdings.js`, the FIFO lot matching engine in `/api/holding/:holdingId/detail` previously ignored bonus transactions when `quantity === 0`; because Fineotex Chemical (`FCL`) had bonus logged as `quantity: 0` with notes `+1336 Shares Received as Bonus`, the 1,336 bonus shares were never pushed to the FIFO queue; when selling 2,061 shares, FIFO only matched 725 shares at pre-bonus prices, completely omitting the +₹29,632 proceeds from the zero-cost bonus shares and creating a massive false realized loss of `-34,327.14`; (2) Universal FIFO Bonus Lot Parsing: Patched `server/routes/holdings.js` and `server/services/recalculator.js` to parse bonus shares from transaction notes (`/\+([\d.,]+)\s*Shares/i`) and push 0-cost lots to the queue, achieving exact cent-level parity across Holding Detail Modal (`-4,691.81`), Recalculator (`-4,691.81`), and Holdings table (`-4,691.81`); (3) Timeline Chart Duplicate Split Multiplier Elimination: Diagnosed why the chart showed false big profits on realized sales; the timeline chart simulation loop previously parsed `Stock Split (1:2)` and added synthetic quantity `runningQ * ((2/1) - 1) = 1,670` shares, double-counting the split since buy transactions were already split-adjusted; this cut the average cost basis in half to ₹12.15, plotting false profits on sales at ₹22.18; removed synthetic split addition from the chart loop, restoring true cost basis (~₹24.45/share) and correctly reflecting the true small loss (-₹4,691.81); (4) Removed Dividend column from NPS (`NpsView.jsx`); (5) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified port 5000 Express backend daemon health, and clean Vite production build. | Gemini 3.8 Flash |
| **v5.41.0** | 20-09-2026 | Indian Equity NSE/BSE MAX Quote Engine Hardening, Zero-Egress Boot Priming & UI Parity: (1) Root Cause Diagnosed: Diagnosed why Indian stock prices reverted to NSE prices (e.g. Anant Raj at ₹606.15 instead of BSE ₹606.25); outside market hours (weekends/holidays) live ticker polling is suspended by Rule 22 market hours gating, leaving `liveQuoteCache` empty on server reboot; endpoints previously fell back to `holding.current_price` from `data/db_cache_snapshot.json` where `bse_price` was 0 and `current_price` held the older NSE quote; in addition, in `HoldingsTable.jsx`, `NSE: ₹... | BSE: ₹...` was placed in an unreachable `else` branch of `h.day_change !== undefined` and was omitted from `IndianStocksView.jsx`; (2) Universal Price Resolver (`resolveHoldingPrice`): Added canonical helper `resolveHoldingPrice(holding, liveQuote)` in `server/services/priceEngine.js` and wired it across `server/routes/holdings.js`, `server/routes/summary.js`, `server/routes/calendar.js`, and `server/services/portfolioCalculator.js`, strictly enforcing `Math.max(price, nse_price, bse_price)` for Indian stocks; (3) Boot Cache Priming (Zero Egress): Updated `server/index.js` to prime `liveQuoteCache` from the local cached holdings on server boot, guaranteeing `liveQuoteCache` is never empty even when markets are closed, with 0 bytes of Supabase egress; (4) Local Disk Snapshot Quote Refresh (Zero Egress): Refreshed active Indian stocks via Yahoo Finance API, updating `data/db_cache_snapshot.json` to lock higher BSE quotes (such as Anant Raj ₹606.25 vs ₹606.15, PFC ₹346 vs ₹342.55, FCL ₹57.09 vs ₹57.02) with 0 bytes of Supabase egress; (5) UI Exchange Badges: Updated `HoldingsTable.jsx`, `IndianStocksView.jsx`, and `HoldingDetailHeader.jsx` to render `NSE: ₹... | BSE: ₹...` pills whenever both exchange quotes are available; (6) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified active port 5000 Express backend daemon, and clean Vite production build. | Gemini 3.8 Flash |
| **v5.42.0** | 20-09-2026 | Fully Sold Holding Timeline Chart Zero-Touch Fix (Corporate Action & Bonus Issue Resolution): (1) Root Cause Diagnosed: Diagnosed why stocks like Sonata Software (`SONATSOFTW`), Gail (`GAIL`), Blue Star (`BLUESTARCO`), and Fiem Industries (`FIEMIND`) showed phantom balances and cost hanging in the air above zero at the end of their Tracker Chart after being fully sold; historical BUY transactions for open stocks in the database had previously been adjusted to corporate-action-scaled share counts (e.g. Sonata's buys were stored as 14, 14, and 1 share, total 29 shares, matching total sold of 29 shares) while the BONUS transaction was logged with `quantity: 0` and informational note `+14 Shares Received as Bonus`; the dense timeline simulation loop in `/api/holding/:holdingId/detail` previously parsed `+14 Shares Received as Bonus` from notes and added 14 bonus shares to `runningQ` a second time (double counting), leaving 14 phantom shares and ₹5,869.46 cost basis hanging in the air after the final 14-share sale; (2) Timeline Simulation Harmonization: Separated event dot display quantity (`eventQty`) from financial simulation quantity (`qty`); simulation strictly adheres to `Number(tx.quantity) || 0`, eliminating duplicate share additions for split/bonus-adjusted buy lots; (3) Airtight Liquidation Zero-Touch Invariance: Added safeguard in `server/routes/holdings.js` guaranteeing that when an exited holding (`isExited`) executes its final sale/redemption transaction, `runningQ`, `runningInvUSD`, and `runningInvINR` immediately clamp to strictly 0, ensuring both Cost Basis and Market Value lines drop to exactly ₹0.00 at the liquidation date; (4) Summary P&L Date Normalization: In `server/routes/summary.js`, normalized `pnl_history` dates to ISO format `YYYY-MM-DD` when evaluating yesterday's closing wealth, ensuring 100% exact cent-level parity between Dashboard and Calendar views; (5) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified active port 5000 Express daemon, and clean Vite production build. | Gemini 3.8 Flash |
| **v5.43.0** | 20-09-2026 | Stock Split Market Ex-Date Detection Hardening & TDPOWERSYS Timeline Alignment: (1) Root Cause Diagnosed: TD Power Systems (`TDPOWERSYS`) underwent a 1:2 stock split with market ex-date on 28-05-2026 where Yahoo historical raw closing prices halved from ₹1,330.20 to ₹665.10; in `server/routes/holdings.js`, ex-date detection previously searched forward with an arbitrary 45-day window (`daysDiff > 45`); because the SPLIT transaction was entered on 24-08-2026 (88 days after ex-date), the loop failed to find the 28-05-2026 ex-date; this left `currentTradeScale` at 0.5 for three months, cutting market prices in half twice (to ₹332.55) and creating an artificial 50% valuation trough from ₹4.58L to ₹2.29L from May 28 to August 24, followed by an artificial 100% vertical surge on August 24 when the scale reset to 1.0; (2) Backward Search with 365-Day Lookback: Enhanced ex-date detection in `server/routes/holdings.js` to search backwards from the recorded transaction date with up to 365 days lookback, reliably detecting the ex-date (2026-05-28) and transitioning `currentTradeScale` to 1.0 on the exact ex-date; (3) TDPOWERSYS Transaction Alignment: Updated the SPLIT transaction date in `data/db_cache_snapshot.json` and database to the true market ex-date `2026-05-28`, aligning the transaction ledger separator bar with the chart split event; (4) Calendar Weekend Liability Parity: Fixed weekend `todayEntry` handling in `server/routes/calendar.js` to ensure live liabilities (`liveTodayValuation.debt`) are preserved on non-trading days, maintaining 100% exact parity with Dashboard `/api/summary` and canonical engine; (5) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs`, verified continuous unbroken TDPOWERSYS chart transition (₹4,58,919.32 on May 27 connects seamlessly to ₹4,58,919.00 on May 28), active port 5000 Express daemon, and clean Vite production build. | Antigravity AI |
| **v5.44.0** | 20-09-2026 | 100% Offline Local Cache Mode & Strict Zero-Egress Architecture Activation: (1) Offline Cache Mode Activation: Activated `OFFLINE_CACHE_MODE=true` in `.env` and hardened `server/db.js` (`db.select`, `db.insert`, `db.update`, `db.delete`, `warmCache`) to completely bypass cloud network requests and serve 100% of read and write queries strictly from Node.js RAM (`dbCache`) and local disk snapshots (`data/db_cache_snapshot.json`); (2) Zero-Expiry In-Memory Retention: Removed arbitrary 24-hour TTL expiration from RAM cache in offline mode, guaranteeing database collections remain resident in memory indefinitely without dropping or triggering cloud re-queries; (3) Unconditional Local Snapshot Restoration: Configured `restoreCacheSnapshotFromDisk()` to unconditionally restore local disk snapshots on cold server startup regardless of snapshot age, eliminating cold-start cloud egress; (4) Write-Through Disk Persistence: All mutations (`insert`, `update`, `delete`) execute write-through updates in RAM with auto-generated UUIDs and trigger debounced local snapshot saves (`saveCacheSnapshotToDisk()`); (5) Verification: 100% PASS on `node scripts/verify_financial_integrity.mjs` with 0 bytes of Supabase egress, active port 5000 Express daemon, and clean Vite production build. | Antigravity AI |













